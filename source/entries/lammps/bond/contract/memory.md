# FermiLink Optimize Memory

- package_id: lammps
- benchmark_id: lammps-tip4p-long-nve-bonded-hotpath
- benchmark_path: .fermilink-optimize/benchmark.worker.yaml
- optimize_branch: fermilink-optimize/lammps-bond
- started_at_utc: 2026-04-12T05:55:38.343855Z
- last_updated_utc: 2026-04-12T23:04:05.175316Z

## Short-Term Memory (Operational)
### Current objective
- Improve benchmark performance while preserving correctness.
### Latest accepted commit
- commit: bf07f28c4118
- primary_metric: weighted_median_bond_seconds=0.21875
- status: accepted
### Progress log

- [2026-04-12T06:03:57.346190Z] baseline: baseline (e7c0ed95a333, weighted_median_bond_seconds=0.32627)
- [2026-04-12T06:35:23Z] iter_0001: accepted 911f06a50100 as new incumbent after correctness-clean benchmark results improved weighted_median_bond_seconds from 0.32627 to 0.28156 (-13.70% vs incumbent).
- [2026-04-12T06:36:14.700426Z] accepted: Hoist `evflag`/`eflag`/`newton_bond` branches out of the serial `bond_class2` and `angle_harmonic` hot loops with OMP-style specialized `eval` helpers, and use flat `_noalias` typed views for coordinates, forces, and topology lists. [Correctness passed and the candidate cut weighted_median_bond_seconds from 0.32627 to 0.28156 (-13.70%), so it should replace the incumbent.] (911f06a50100, weighted_median_bond_seconds=0.28156)
- [2026-04-12T06:53:52Z] iter_0002: rejected 25f839bdfc42 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.28546`, which was `12.51%` better than baseline but `1.39%` slower than incumbent `911f06a50100`, and it triggered a hard correctness failure in both benchmark cases, so the incumbent remains unchanged.
- [2026-04-12T06:55:03.116086Z] correctness_failure: Reuse per-type coefficients and reciprocals, evaluate the class2 polynomial in a lower-overhead form, reuse force components, and only materialize `ev_tally` force arrays in `bond_class2` and `angle_harmonic` specialized hot-loop instantiations. [Hard correctness failure in both benchmark cases, and the candidate was also 1.39% slower than incumbent on the primary metric.; case test-32r-long etotal relative_delta exceeds threshold (9.3752e-05 > 1e-08); case test-32r-long pe relative_delta exceeds threshold (0.0022341 > 1e-08); case test-32r-long ke relative_delta exceeds threshold (0.0041836 > 1e-08); case test-32r-long temp relative_delta exceeds threshold (0.0041836 > 1e-08); case test-32r-long press abs_delta exceeds threshold (1.38024e+08 > 1e-06); case test-32r-long force_components abs_delta exceeds threshold (0.106059 > 1e-08); case test-32r-long force_components rms_delta exceeds threshold (0.0295306 > 1e-08); case test-64r-short etotal relative_delta exceeds threshold (7.58474e-08 > 1e-08); case test-64r-short pe relative_delta exceeds threshold (4.16662e-08 > 1e-08); case test-64r-short ke relative_delta exceeds threshold (5.10999e-08 > 1e-08); case test-64r-short temp relative_delta exceeds threshold (4.97398e-08 > 1e-08); case test-64r-short press abs_delta exceeds threshold (2880 > 1e-06); case test-64r-short force_components abs_delta exceeds threshold (1.24e-05 > 1e-08); case test-64r-short force_components rms_delta exceeds threshold (1.19468e-06 > 1e-08)] (25f839bdfc42, weighted_median_bond_seconds=0.28546)
- [2026-04-12T07:12:22.165171Z] iter_0003: accepted 4a79c588ea08 as new incumbent after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.27301`, which was `16.32%` better than baseline and `3.04%` better than incumbent `911f06a50100`, with correctness passing in both benchmark cases.
- [2026-04-12T07:12:22.165171Z] accepted: Add a `bond_class2` single-bond-type fast path that dispatches to a dedicated `eval_one_type` helper and hoists constant coefficients out of the inner loop. [Correctness passed with no guardrail errors, and the candidate cut weighted_median_bond_seconds from 0.28156 to 0.27301 (-3.04% vs incumbent, -16.32% vs baseline), so it should replace the incumbent.] (4a79c588ea08, weighted_median_bond_seconds=0.27301)
- [2026-04-12T07:13:31.035999Z] accepted: Add a `bond_class2` single-bond-type fast path that dispatches to a dedicated `eval_one_type` helper and hoists constant coefficients out of the inner loop. [Correctness passed and the single-bond-type `bond_class2` fast path improved weighted_median_bond_seconds from 0.28156 to 0.27301 (-3.04% vs incumbent).] (4a79c588ea08, weighted_median_bond_seconds=0.27301)
- [2026-04-12T07:30:30Z] iter_0004: rejected e5c1ddf090fa after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.27052`, which was `17.09%` better than baseline and `0.91%` better than incumbent `4a79c588ea08`, but it did not clear the controller's `2%` minimum improvement threshold, so the incumbent remains unchanged.
- [2026-04-12T07:30:30Z] rejected: Add an `angle_harmonic` single-angle-type fast path that dispatches to `eval_one_type` and hoists `k[1]`/`theta0[1]` out of the inner loop. [Correctness passed with no guardrail errors, but the primary metric improved from 0.27301 to only 0.27052 (-0.91% vs incumbent), which is below the 2% acceptance threshold.] (e5c1ddf090fa, weighted_median_bond_seconds=0.27052)
- [2026-04-12T07:31:41.978324Z] rejected: Add an `angle_harmonic` single-angle-type fast path that dispatches to `eval_one_type` and hoists `k[1]`/`theta0[1]` out of the inner loop. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 0.91% versus the incumbent, below the 2% acceptance threshold.] (e5c1ddf090fa, weighted_median_bond_seconds=0.27052)
- [2026-04-12T07:50:36Z] iter_0005: rejected 224da0125669 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.27041`, which was `17.12%` better than baseline and `0.95%` better than incumbent `4a79c588ea08`, with correctness passing in both benchmark cases, but it still missed the controller's `2%` minimum improvement threshold so the incumbent remains unchanged.
- [2026-04-12T07:50:36Z] rejected: Cache per-bond force components in bond_class2 and restore the single-angle-type fast path in angle_harmonic. [Correctness passed with no guardrail errors, but the primary metric improved from 0.27301 to only 0.27041 (-0.95% vs incumbent), which is below the 2% acceptance threshold.] (224da0125669, weighted_median_bond_seconds=0.27041)
- [2026-04-12T07:52:08.695972Z] rejected: Cache per-bond force components in bond_class2 and restore the single-angle-type fast path in angle_harmonic. [Correctness passed, but weighted_median_bond_seconds improved only 0.95% versus the incumbent, below the 2% acceptance threshold.] (224da0125669, weighted_median_bond_seconds=0.27041)
- [2026-04-12T14:58:16Z] iter_0006: rejected e639b3949f97 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.27144`, which was `16.81%` better than baseline and `0.58%` better than incumbent `4a79c588ea08`, with correctness passing in both benchmark cases, but it did not clear the controller's `2%` minimum improvement threshold, so the incumbent remains unchanged.
- [2026-04-12T14:58:16Z] rejected: Accumulate consecutive same-`i1` bonds in the single-type `bond_class2` fast path to reuse the leading atom's coordinates and force accumulator across each run. [Correctness passed with no guardrail errors, but the primary metric improved from 0.27301 to only 0.27144 (-0.58% vs incumbent), which is below the 2% acceptance threshold.] (e639b3949f97dddf2d44df07d2cf495f78123aa6, weighted_median_bond_seconds=0.27144)
- [2026-04-12T14:59:51.057432Z] rejected: Accumulate consecutive same-`i1` bonds in the single-type `bond_class2` fast path to reuse the leading atom's coordinates and force accumulator across each run. [Correctness passed, but same-`i1` bond-run accumulation improved weighted_median_bond_seconds by only 0.58% versus the incumbent, below the 2% acceptance threshold.] (e639b3949f97, weighted_median_bond_seconds=0.27144)
- [2026-04-12T15:18:42Z] iter_0007: rejected 47d88a349494 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.27624`, which was `15.33%` better than baseline but `1.18%` slower than incumbent `4a79c588ea08`, with correctness passing in both benchmark cases, so the incumbent remains unchanged.
- [2026-04-12T15:18:42Z] rejected: Restore the `angle_harmonic` single-angle-type fast path and add an explicit same-`i1` two-bond block in the single-type `bond_class2` kernel to reuse the leading atom state while preserving per-bond math and tally order. [Correctness passed, but the candidate regressed weighted_median_bond_seconds from 0.27301 to 0.27624 (+1.18% vs incumbent), so it should not replace the incumbent.] (47d88a34949485d0d2537b85da1c1bc763839085, weighted_median_bond_seconds=0.27624)
- [2026-04-12T15:21:05.980838Z] rejected: Restore the `angle_harmonic` single-angle-type fast path and add an explicit same-`i1` two-bond block in the single-type `bond_class2` kernel to reuse the leading atom state while preserving per-bond math and tally order. [Correctness passed, but iteration 7 regressed the primary metric to 0.27624, which is 1.18% slower than the incumbent 0.27301.] (47d88a349494, weighted_median_bond_seconds=0.27624)
- [2026-04-12T15:41:31Z] iter_0008: rejected e3c2ae9ee96a after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.27111`, which was `16.91%` better than baseline and `0.70%` better than incumbent `4a79c588ea08`, with correctness passing in both benchmark cases, but it still missed the controller's `2%` minimum improvement threshold so the incumbent remains unchanged.
- [2026-04-12T15:41:31Z] rejected: Restore `angle_harmonic` single-angle-type dispatch and tighten one-type bonded hot paths by only materializing angle `ev_tally` arrays when `EVFLAG` is active and only computing `bond_class2` energy-only intermediates in `EFLAG` instantiations. [Correctness passed, but weighted_median_bond_seconds improved only 0.70% versus the incumbent, below the 2% acceptance threshold.] (e3c2ae9ee96a, weighted_median_bond_seconds=0.27111)
- [2026-04-12T15:44:30.538581Z] rejected: Restore `angle_harmonic` single-angle-type dispatch and tighten one-type bonded hot paths by only materializing angle `ev_tally` arrays when `EVFLAG` is active and only computing `bond_class2` energy-only intermediates in `EFLAG` instantiations. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 0.70% versus incumbent 4a79c588ea08, below the 2% acceptance threshold.] (e3c2ae9ee96a, weighted_median_bond_seconds=0.27111)
- [2026-04-12T16:05:54Z] iter_0009: rejected 6fbda0abc94b after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.26892`, which was `17.58%` better than baseline and `1.50%` better than incumbent `4a79c588ea08`, with correctness passing in both benchmark cases, but it still missed the controller's `2%` minimum improvement threshold so the incumbent remains unchanged.
- [2026-04-12T16:05:54Z] rejected: Add a dedicated two-way unrolled `bond_class2` one-type hot path for the dominant no-`evflag`, Newton-on case while preserving exact per-bond math and update order. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 1.50% versus incumbent 4a79c588ea08, below the 2% acceptance threshold.] (6fbda0abc94b8116e90561046832a268d1d97728, weighted_median_bond_seconds=0.26892)
- [2026-04-12T16:07:51.032635Z] rejected: Add a dedicated two-way unrolled `bond_class2` one-type hot path for the dominant no-`evflag`, Newton-on case while preserving exact per-bond math and update order. [Correctness passed, but the two-way unrolled `bond_class2` hot path improved `weighted_median_bond_seconds` by only 1.50% versus the incumbent, below the 2% acceptance threshold.] (6fbda0abc94b, weighted_median_bond_seconds=0.26892)
- [2026-04-12T16:25:54.575828Z] iter_0010: accepted cf4ba8e7204d as new incumbent after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.26621`, which was `18.41%` better than baseline and `2.49%` better than incumbent `4a79c588ea08`, with correctness passing in both benchmark cases.
- [2026-04-12T16:25:54.575828Z] accepted: Combine the correctness-safe `bond_class2` one-type two-way unrolled hot kernel with the correctness-safe `angle_harmonic` one-type dispatch and constant hoisting on top of incumbent `4a79c588ea08`. [Correctness passed with no guardrail errors, and the candidate cut weighted_median_bond_seconds from 0.27301 to 0.26621 (-2.49% vs incumbent, -18.41% vs baseline), so it should replace the incumbent.] (cf4ba8e7204d, weighted_median_bond_seconds=0.26621)
- [2026-04-12T16:29:29.938795Z] accepted: Combine the correctness-safe `bond_class2` one-type two-way unrolled hot kernel with the correctness-safe `angle_harmonic` one-type dispatch and constant hoisting on top of incumbent `4a79c588ea08`. [Correctness passed and weighted_median_bond_seconds improved from 0.27301 to 0.26621 (+2.49% vs incumbent), clearing the 2% promotion threshold.] (cf4ba8e7204d, weighted_median_bond_seconds=0.26621)
- [2026-04-12T16:51:21Z] iter_0011: accepted 672cbe901cae as new incumbent after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.24606`, which was `24.58%` better than baseline and `7.57%` better than incumbent `cf4ba8e7204d`, with correctness passing in both benchmark cases.
- [2026-04-12T16:51:21Z] accepted: Add a dedicated `angle_harmonic` one-type no-`evflag`, Newton-on hot path with scalar force temporaries and two-way exact unrolling. [Correctness passed with no guardrail errors, and the candidate cut weighted_median_bond_seconds from 0.26621 to 0.24606 (-7.57% vs incumbent, -24.58% vs baseline), so it should replace the incumbent.] (672cbe901cae, weighted_median_bond_seconds=0.24606)
- [2026-04-12T16:53:15.447277Z] accepted: Add a dedicated `angle_harmonic` one-type no-`evflag`, Newton-on hot path with scalar force temporaries and two-way exact unrolling. [Correctness passed with no hard rejects, and the dedicated `angle_harmonic` hot path improved `weighted_median_bond_seconds` from 0.26621 to 0.24606 (+7.57% vs incumbent), clearing the 2% promotion threshold.] (672cbe901cae, weighted_median_bond_seconds=0.24606)
- [2026-04-12T17:28:33Z] iter_0012: accepted 0826ffd39444 as new incumbent after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.23436`, which was `28.17%` better than baseline and `4.75%` better than incumbent `672cbe901cae`, with correctness passing in both benchmark cases.
- [2026-04-12T17:28:33Z] accepted: Cache one-type `bond_class2` bond geometry and reuse paired bond data in `angle_harmonic`'s dominant no-`evflag`, Newton-on hot path, with direct fallback when local ordering does not match. [Correctness passed with no hard rejects, and the cross-kernel geometry reuse improved `weighted_median_bond_seconds` from 0.24606 to 0.23436 (+4.75% vs incumbent), clearing the 2% promotion threshold.] (0826ffd39444, weighted_median_bond_seconds=0.23436)
- [2026-04-12T17:30:41.195348Z] accepted: Cache one-type bond_class2 bond geometry and reuse paired bond data in angle_harmonic's dominant no-evflag, Newton-on hot path, with direct fallback when local ordering does not match. [Correctness passed and weighted_median_bond_seconds improved from 0.24606 to 0.23436 (+4.75% vs incumbent), clearing the 2% promotion threshold.] (0826ffd39444, weighted_median_bond_seconds=0.23436)
- [2026-04-12T17:52:57Z] iter_0013: accepted 30584dbe2ab0 as new incumbent after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.22529`, which was `30.95%` better than baseline and `3.87%` better than incumbent `0826ffd39444`, with correctness passing in both benchmark cases.
- [2026-04-12T17:52:57Z] accepted: Replace the one-type bond-to-angle reuse with an angle-oriented cache built in `bond_class2`, then consume it from a dedicated branch-free, two-way-unrolled `angle_harmonic` hot path with direct fallback to the incumbent local-geometry path when bond/angle pairing does not align. [Correctness passed with no hard rejects, and the angle-oriented cross-kernel reuse improved `weighted_median_bond_seconds` from 0.23436 to 0.22529 (+3.87% vs incumbent), clearing the 2% promotion threshold.] (30584dbe2ab0, weighted_median_bond_seconds=0.22529)
- [2026-04-12T17:55:14.956474Z] accepted: Replace the one-type bond-to-angle reuse with an angle-oriented cache built in `bond_class2`, then consume it from a dedicated branch-free, two-way-unrolled `angle_harmonic` hot path with direct fallback to the incumbent local-geometry path when bond/angle pairing does not align. [Correctness passed and weighted_median_bond_seconds improved 3.87% versus incumbent, clearing the 2% promotion threshold.] (30584dbe2ab0, weighted_median_bond_seconds=0.22529)
- [2026-04-12T18:16:53Z] iter_0014: rejected 0890ed81e853 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.26506`, which was `18.76%` better than baseline but `17.65%` slower than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, so the incumbent remains unchanged.
- [2026-04-12T18:16:53Z] rejected: Replace the one-type bond-to-angle geometry cache with a prepared per-angle force cache built in `bond_class2`, then consume it as a direct force-apply hot path in `angle_harmonic` while preserving the validated structural guard and direct fallback. [Correctness passed with no hard rejects, but the candidate regressed `weighted_median_bond_seconds` from 0.22529 to 0.26506 (+17.65% vs incumbent), so it should not replace the incumbent.] (0890ed81e853, weighted_median_bond_seconds=0.26506)
- [2026-04-12T18:19:08.313937Z] rejected: Replace the one-type bond-to-angle geometry cache with a prepared per-angle force cache built in `bond_class2`, then consume it as a direct force-apply hot path in `angle_harmonic` while preserving the validated structural guard and direct fallback. [Correctness passed, but moving full angle-force preparation into bond_class2 regressed weighted_median_bond_seconds by 17.65% versus incumbent 30584dbe2ab0.] (0890ed81e853, weighted_median_bond_seconds=0.26506)
- [2026-04-12T18:37:00Z] iter_0015: rejected fdcd68522a83 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.21063`, which was `35.44%` better than baseline and `6.51%` better than incumbent `30584dbe2ab0`, but it triggered a hard correctness failure in both benchmark cases, so the incumbent remains unchanged.
- [2026-04-12T18:37:00Z] correctness_failure: Compact the angle hot cache to bond deltas plus inverse bond lengths and consume them in the cached `angle_harmonic` hot path to remove cached-path divisions. [Hard correctness failure in both benchmark cases despite the nominal speedup: `test-32r-long` diverged at `etotal` `7.92579e-05`, `pe` `0.00213102`, `ke`/`temp` `0.00400269`, `press` `1.39153e+08`, and force abs/rms deltas `0.115462`/`0.0280042`, while `test-64r-short` also exceeded thresholds.] (fdcd68522a83, weighted_median_bond_seconds=0.21063)
- [2026-04-12T18:40:30.515340Z] correctness_failure: Compact the angle hot cache to bond deltas plus inverse bond lengths and consume them in the cached `angle_harmonic` hot path to remove cached-path divisions. [Hard correctness failure in both benchmark cases despite a nominal 6.51% speedup, so incumbent 30584dbe2ab0 remains.; case test-32r-long etotal relative_delta exceeds threshold (7.92579e-05 > 1e-08); case test-32r-long pe relative_delta exceeds threshold (0.00213102 > 1e-08); case test-32r-long ke relative_delta exceeds threshold (0.00400269 > 1e-08); case test-32r-long temp relative_delta exceeds threshold (0.00400269 > 1e-08); case test-32r-long press abs_delta exceeds threshold (1.39153e+08 > 1e-06); case test-32r-long force_components abs_delta exceeds threshold (0.115462 > 1e-08); case test-32r-long force_components rms_delta exceeds threshold (0.0280042 > 1e-08); case test-64r-short etotal relative_delta exceeds threshold (6.84866e-08 > 1e-08); case test-64r-short pe relative_delta exceeds threshold (3.14967e-08 > 1e-08); case test-64r-short ke relative_delta exceeds threshold (6.84812e-08 > 1e-08); case test-64r-short temp relative_delta exceeds threshold (6.63765e-08 > 1e-08); case test-64r-short press abs_delta exceeds threshold (2360 > 1e-06); case test-64r-short force_components abs_delta exceeds threshold (1.22e-05 > 1e-08); case test-64r-short force_components rms_delta exceeds threshold (1.13192e-06 > 1e-08)] (fdcd68522a83, weighted_median_bond_seconds=0.21063)
- [2026-04-12T18:58:11Z] iter_0016: rejected 1f058192fe33 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.23245`, which was `28.76%` better than baseline but `3.18%` slower than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, so the incumbent remains unchanged.
- [2026-04-12T18:58:11Z] rejected: Compact the angle hot cache to bond deltas plus exact bond lengths, recomputing cached-path `rsq` values in `angle_harmonic` after dropping cached `rsq` fields. [Correctness passed, but the candidate regressed `weighted_median_bond_seconds` from 0.22529 to 0.23245 (+3.18% vs incumbent), so it should not replace the incumbent.] (1f058192fe33418582791fc8083eb481c5321b50, weighted_median_bond_seconds=0.23245)
- [2026-04-12T18:58:11Z] rejected: Compact the angle hot cache to bond deltas plus exact bond lengths, recomputing cached-path `rsq` values in `angle_harmonic` after dropping cached `rsq` fields. [Correctness passed and fixed the iteration-15 correctness issue, but recomputing `rsq` in the consumer still made the primary metric 3.18% slower than incumbent `30584dbe2ab0`.] (1f058192fe33, weighted_median_bond_seconds=0.23245)
- [2026-04-12T19:02:54.047911Z] rejected: Compact the angle hot cache to bond deltas plus exact bond lengths, recomputing cached-path rsq values in angle_harmonic after dropping cached rsq fields. [Correctness recovered, but dropping cached rsq fields and recomputing them in angle_harmonic regressed weighted_median_bond_seconds by 3.18% versus the incumbent.] (1f058192fe33, weighted_median_bond_seconds=0.23245)
- [2026-04-12T19:25:25.730712Z] rejected: Precompute the one-type bond/angle pairing layout once per topology build and reuse it in bond_class2's hot angle-cache fill to remove repeated per-step topology validation while preserving the incumbent cache payload and angle_harmonic consumer math [Correctness passed, but weighted_median_bond_seconds improved only 0.92% versus incumbent 30584dbe2ab0, below the 2% promotion threshold.] (738ffb9e3a30, weighted_median_bond_seconds=0.22321)
- [2026-04-12T19:55:44.765755Z] rejected: Prepare and reuse a prevalidated one-type bond-angle orientation layout in `bond_class2` hot cache fill while keeping the incumbent angle cache payload and `angle_harmonic` math unchanged [Correctness passed, but `weighted_median_bond_seconds` improved only 0.83% versus incumbent `30584dbe2ab0`, below the 2% promotion threshold.] (bed99cfa6486, weighted_median_bond_seconds=0.22343)
- [2026-04-12T20:24:55.920504Z] rejected: Compact the cached angle hot entry to store the exact bond-length product `r12 = r1*r2` instead of separate `r1`/`r2`, preserving cached `del*` and `rsq` semantics while reusing that exact product in the cached `angle_harmonic` hot path. [Correctness passed, but weighted_median_bond_seconds improved only 1.79% versus incumbent 30584dbe2ab0, below the 2% promotion threshold.] (acd350270fd3, weighted_median_bond_seconds=0.22126)
- [2026-04-12T20:46:46.028779Z] rejected: Combine prevalidated bond-angle orientation layout reuse in `bond_class2` with exact `r12` hot-cache compaction consumed by `angle_harmonic`. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 1.54% versus the incumbent, below the 2% promotion threshold.] (c0a62a11cfaf, weighted_median_bond_seconds=0.22182)
- [2026-04-12T21:08:23.118526Z] correctness_failure: Store exact `r12 = r1*r2` in the hot angle cache and reuse a shared local `inv_r12` in the cached `angle_harmonic` hot path to remove the duplicate division by the same bond-length product. [Hard correctness failure in both benchmark cases from shared `inv_r12` reuse despite a nominal 2.95% speedup, so incumbent `30584dbe2ab0` remains.; case test-32r-long etotal relative_delta exceeds threshold (7.66767e-05 > 1e-08); case test-32r-long pe relative_delta exceeds threshold (0.00184228 > 1e-08); case test-32r-long ke relative_delta exceeds threshold (0.00346259 > 1e-08); case test-32r-long temp relative_delta exceeds threshold (0.00346259 > 1e-08); case test-32r-long press abs_delta exceeds threshold (1.29238e+08 > 1e-06); case test-32r-long force_components abs_delta exceeds threshold (0.100389 > 1e-08); case test-32r-long force_components rms_delta exceeds threshold (0.0278753 > 1e-08); case test-64r-short etotal relative_delta exceeds threshold (7.21825e-08 > 1e-08); case test-64r-short pe relative_delta exceeds threshold (6.86454e-08 > 1e-08); case test-64r-short ke relative_delta exceeds threshold (1.44472e-07 > 1e-08); case test-64r-short temp relative_delta exceeds threshold (1.46005e-07 > 1e-08); case test-64r-short press abs_delta exceeds threshold (1130 > 1e-06); case test-64r-short force_components abs_delta exceeds threshold (1e-05 > 1e-08); case test-64r-short force_components rms_delta exceeds threshold (1.15835e-06 > 1e-08)] (e4103f7dab04, weighted_median_bond_seconds=0.21864)
- [2026-04-12T21:31:56.364653Z] rejected: Compact the one-type hot angle cache to exact `r12 = r1*r2` and add prepared hot-angle index/layout metadata so `bond_class2` reuses stable topology preparation and cached `angle_harmonic` stops rereading `neighbor->anglelist` while preserving the incumbent arithmetic order. [Correctness passed, but the candidate’s 0.22213 weighted_median_bond_seconds is only a 1.40% improvement over incumbent 30584dbe2ab0, below the 2% promotion threshold.] (efcb4c8f585c, weighted_median_bond_seconds=0.22213)
- [2026-04-12T21:54:08.992772Z] iter_0023: rejected 567a79cfb769 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.22195`, which was `31.97%` better than baseline and `1.48%` better than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, but it did not clear the controller's `2%` minimum improvement threshold, so the incumbent remains unchanged.
- [2026-04-12T21:54:08.992772Z] rejected: Widen the cached `angle_harmonic` hot consumer to four-way exact unrolling while preserving the incumbent cache payload, cached-path arithmetic order, guard, and direct fallback. [Correctness passed, but the candidate improved `weighted_median_bond_seconds` by only `1.48%` versus incumbent `30584dbe2ab0`, below the `2%` promotion threshold.] (567a79cfb769, weighted_median_bond_seconds=0.22195)
- [2026-04-12T21:57:30.410463Z] rejected: Widen the cached `angle_harmonic` hot consumer to four-way exact unrolling while preserving the incumbent cache payload, cached-path arithmetic order, guard, and direct fallback. [Correctness passed, but the four-way cached `angle_harmonic` unroll improved `weighted_median_bond_seconds` by only 1.48% versus incumbent, below the 2% promotion threshold.] (567a79cfb769, weighted_median_bond_seconds=0.22195)
- [2026-04-12T22:15:36Z] iter_0024: rejected 2c69753dfc48 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.22158`, which was `32.09%` better than baseline and `1.65%` better than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, but it did not clear the controller's `2%` minimum improvement threshold, so the incumbent remains unchanged.
- [2026-04-12T22:15:36Z] rejected: Combine exact `r12` hot-angle cache compaction in `bond_class2` with a four-way cached `angle_harmonic` consumer while preserving the incumbent guard and cached-path division semantics. [Correctness passed, but the candidate improved `weighted_median_bond_seconds` by only `1.65%` versus incumbent `30584dbe2ab0`, below the `2%` promotion threshold.] (2c69753dfc48, weighted_median_bond_seconds=0.22158)
- [2026-04-12T22:17:11.208711Z] rejected: Combine exact `r12` hot-angle cache compaction in `bond_class2` with a four-way cached `angle_harmonic` consumer while preserving the incumbent guard and cached-path division semantics [Correctness passed, but 0.22158 weighted_median_bond_seconds is only a 1.65% improvement over incumbent 0.22529, below the 2% promotion threshold.] (2c69753dfc48, weighted_median_bond_seconds=0.22158)
- [2026-04-12T22:42:16.122578Z] accepted: Compact the hot angle cache to exact r12 payloads and widen the one-type cached bond_class2 producer to four-way exact unrolling while preserving the incumbent angle_harmonic arithmetic order and fallback guard [Correctness passed and weighted_median_bond_seconds improved from 0.22529 to 0.21875, a 2.90% gain over the incumbent that clears the 2% promotion threshold.] (bf07f28c4118, weighted_median_bond_seconds=0.21875)
- [2026-04-12T23:04:05.175316Z] rejected: Restore prepared hot-angle layout/index reuse in bond_class2 and a four-way cached angle_harmonic consumer while keeping the incumbent exact r12 cache payload and four-way bond producer [Correctness passed, but weighted_median_bond_seconds regressed from 0.21875 to 0.22115 versus incumbent bf07f28c4118, so the candidate should not replace the incumbent.] (a0800c8480fc, weighted_median_bond_seconds=0.22115)
## Long-Term Memory (Persistent)
### Benchmark contract
- Benchmark file: `.fermilink-optimize/benchmark.worker.yaml`
### Accepted experiments
- [2026-04-12T06:03:57.346190Z] baseline (e7c0ed95a333, weighted_median_bond_seconds=0.32627)
- [2026-04-12T06:35:23Z] iter_0001 accepted (911f06a50100, weighted_median_bond_seconds=0.28156, -13.70% vs baseline/incumbent)
- [2026-04-12T06:36:14.700426Z] Hoist `evflag`/`eflag`/`newton_bond` branches out of the serial `bond_class2` and `angle_harmonic` hot loops with OMP-style specialized `eval` helpers, and use flat `_noalias` typed views for coordinates, forces, and topology lists. [Correctness passed and the candidate cut weighted_median_bond_seconds from 0.32627 to 0.28156 (-13.70%), so it should replace the incumbent.] (911f06a50100, weighted_median_bond_seconds=0.28156)
- [2026-04-12T07:12:22.165171Z] iter_0003 accepted (4a79c588ea08, weighted_median_bond_seconds=0.27301, -3.04% vs prior incumbent, -16.32% vs baseline)
- [2026-04-12T07:12:22.165171Z] Add a `bond_class2` single-bond-type fast path that dispatches to a dedicated `eval_one_type` helper and hoists constant coefficients out of the inner loop. [Correctness passed with no guardrail errors, and the candidate cut weighted_median_bond_seconds from 0.28156 to 0.27301 (-3.04% vs incumbent, -16.32% vs baseline), so it should replace the incumbent.] (4a79c588ea08, weighted_median_bond_seconds=0.27301)
- [2026-04-12T07:13:31.035999Z] Add a `bond_class2` single-bond-type fast path that dispatches to a dedicated `eval_one_type` helper and hoists constant coefficients out of the inner loop. [Correctness passed and the single-bond-type `bond_class2` fast path improved weighted_median_bond_seconds from 0.28156 to 0.27301 (-3.04% vs incumbent).] (4a79c588ea08, weighted_median_bond_seconds=0.27301)
- [2026-04-12T16:25:54.575828Z] iter_0010 accepted (cf4ba8e7204d, weighted_median_bond_seconds=0.26621, -2.49% vs prior incumbent, -18.41% vs baseline)
- [2026-04-12T16:25:54.575828Z] Combine the correctness-safe `bond_class2` one-type two-way unrolled hot kernel with the correctness-safe `angle_harmonic` one-type dispatch and constant hoisting on top of incumbent `4a79c588ea08`. [Correctness passed with no guardrail errors, and the candidate cut weighted_median_bond_seconds from 0.27301 to 0.26621 (-2.49% vs incumbent, -18.41% vs baseline), so it should replace the incumbent.] (cf4ba8e7204d, weighted_median_bond_seconds=0.26621)
- [2026-04-12T16:29:29.938795Z] Combine the correctness-safe `bond_class2` one-type two-way unrolled hot kernel with the correctness-safe `angle_harmonic` one-type dispatch and constant hoisting on top of incumbent `4a79c588ea08`. [Correctness passed and weighted_median_bond_seconds improved from 0.27301 to 0.26621 (+2.49% vs incumbent), clearing the 2% promotion threshold.] (cf4ba8e7204d, weighted_median_bond_seconds=0.26621)
- [2026-04-12T16:51:21Z] iter_0011 accepted (672cbe901cae, weighted_median_bond_seconds=0.24606, -7.57% vs prior incumbent, -24.58% vs baseline)
- [2026-04-12T16:51:21Z] Add a dedicated `angle_harmonic` one-type no-`evflag`, Newton-on hot path with scalar force temporaries and two-way exact unrolling. [Correctness passed with no guardrail errors, and the candidate cut weighted_median_bond_seconds from 0.26621 to 0.24606 (-7.57% vs incumbent, -24.58% vs baseline), so it should replace the incumbent.] (672cbe901cae, weighted_median_bond_seconds=0.24606)
- [2026-04-12T16:53:15.447277Z] Add a dedicated `angle_harmonic` one-type no-`evflag`, Newton-on hot path with scalar force temporaries and two-way exact unrolling. [Correctness passed with no hard rejects, and the dedicated `angle_harmonic` hot path improved `weighted_median_bond_seconds` from 0.26621 to 0.24606 (+7.57% vs incumbent), clearing the 2% promotion threshold.] (672cbe901cae, weighted_median_bond_seconds=0.24606)
- [2026-04-12T17:28:33Z] iter_0012 accepted (0826ffd39444, weighted_median_bond_seconds=0.23436, -4.75% vs prior incumbent, -28.17% vs baseline)
- [2026-04-12T17:28:33Z] Cache one-type `bond_class2` bond geometry and reuse paired bond data in `angle_harmonic`'s dominant no-`evflag`, Newton-on hot path, with direct fallback when local ordering does not match. [Correctness passed with no hard rejects, and the cross-kernel geometry reuse improved `weighted_median_bond_seconds` from 0.24606 to 0.23436 (+4.75% vs incumbent), clearing the 2% promotion threshold.] (0826ffd39444, weighted_median_bond_seconds=0.23436)
- [2026-04-12T17:30:41.195348Z] Cache one-type bond_class2 bond geometry and reuse paired bond data in angle_harmonic's dominant no-evflag, Newton-on hot path, with direct fallback when local ordering does not match. [Correctness passed and weighted_median_bond_seconds improved from 0.24606 to 0.23436 (+4.75% vs incumbent), clearing the 2% promotion threshold.] (0826ffd39444, weighted_median_bond_seconds=0.23436)
- [2026-04-12T17:52:57Z] iter_0013 accepted (30584dbe2ab0, weighted_median_bond_seconds=0.22529, -3.87% vs prior incumbent, -30.95% vs baseline)
- [2026-04-12T17:52:57Z] Replace the one-type bond-to-angle reuse with an angle-oriented cache built in `bond_class2`, then consume it from a dedicated branch-free, two-way-unrolled `angle_harmonic` hot path with direct fallback to the incumbent local-geometry path when bond/angle pairing does not align. [Correctness passed with no hard rejects, and the angle-oriented cross-kernel reuse improved `weighted_median_bond_seconds` from 0.23436 to 0.22529 (+3.87% vs incumbent), clearing the 2% promotion threshold.] (30584dbe2ab0, weighted_median_bond_seconds=0.22529)
- [2026-04-12T17:55:14.956474Z] Replace the one-type bond-to-angle reuse with an angle-oriented cache built in `bond_class2`, then consume it from a dedicated branch-free, two-way-unrolled `angle_harmonic` hot path with direct fallback to the incumbent local-geometry path when bond/angle pairing does not align. [Correctness passed and weighted_median_bond_seconds improved 3.87% versus incumbent, clearing the 2% promotion threshold.] (30584dbe2ab0, weighted_median_bond_seconds=0.22529)
- [2026-04-12T22:42:16.122578Z] Compact the hot angle cache to exact r12 payloads and widen the one-type cached bond_class2 producer to four-way exact unrolling while preserving the incumbent angle_harmonic arithmetic order and fallback guard [Correctness passed and weighted_median_bond_seconds improved from 0.22529 to 0.21875, a 2.90% gain over the incumbent that clears the 2% promotion threshold.] (bf07f28c4118, weighted_median_bond_seconds=0.21875)
### Iteration reviews
#### Iteration 1: ACCEPTED
- hypothesis: Port the OMP-style `eval<EVFLAG,EFLAG,NEWTON_BOND>` specialization into the serial `bond_class2` and `angle_harmonic` kernels, and switch the hot loops to flat `_noalias` typed views, to remove inner-loop branching and pointer chasing without changing force math.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
  - `src/MOLECULE/angle_harmonic.h`
- benchmark outcome: Authoritative controller benchmark status was `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The primary metric improved from `weighted_median_bond_seconds=0.32627` to `0.28156` for a `13.70%` reduction, comfortably above the `2%` minimum improvement threshold. Both cases converged: `test-32r-long` reported `bond_seconds=0.28131` and `test-64r-short` reported `bond_seconds=0.019743`; peak RSS was `156.29 MB`.
- lesson learned: The serial bonded kernels still carried avoidable control-flow and indirection overhead that the OMP implementations had already addressed. Mirroring that specialization in the base styles produced a material speedup while preserving numerical behavior, so this family of low-risk control-flow hoists is validated for the bonded hot path.
- next hypothesis: Keep the specialized dispatch structure and target compute-side work inside the new eval bodies next, especially repeated coefficient loads and angle-geometry arithmetic, to see whether additional speedup remains after the branch/indirection cleanup.
- controller decision: Accept `911f06a501008c67741eb0ff406d6554eccb81d7` as the new incumbent, replacing `e7c0ed95a3337aeb7ea977f00d98928616ee97c7`.
#### Iteration 2: REJECTED
- hypothesis: Reuse per-type coefficients and reciprocals inside the specialized `bond_class2` and `angle_harmonic` eval loops, rewrite the class2 polynomial into a lower-overhead form, reuse already-computed force components, and only materialize `ev_tally` force arrays in `EVFLAG` instantiations to reduce arithmetic and temporary traffic without changing force math.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed both cases with overall benchmark status `ok`, but post-run correctness checking failed under field tolerances and triggered `hard_reject=true` with status `correctness_failure`. The candidate measured `weighted_median_bond_seconds=0.28546`, which is `12.51%` faster than baseline `0.32627` but `1.39%` slower than the incumbent `0.28156`, so it would not have met the `2%` improvement threshold even without the scientific failure. Case `test-32r-long` reported `bond_seconds=0.28545` but exceeded tolerances badly (`etotal relative_delta=9.3752e-05`, `pe=0.0022341`, `ke/temp=0.0041836`, `press abs_delta=1.38024e+08`, `force_components abs/rms=0.106059/0.0295306`). Case `test-64r-short` also failed correctness (`force_components abs/rms=1.24e-05/1.19468e-06`, `press abs_delta=2880`, plus smaller thermo mismatches). Peak RSS was `156.87 MB`.
- lesson learned: This bundled arithmetic/data-reuse refactor was not numerically transparent in practice. Algebraically similar rewrites inside these bonded kernels, especially the reciprocal-heavy `angle_harmonic` geometry path combined with other bookkeeping changes, can perturb forces enough to violate the benchmark’s strict tolerances, so they should not be treated as low-risk without isolated validation.
- next hypothesis: Keep the accepted iteration-1 specialization structure, but narrow the next experiment to one kernel and one micro-optimization at a time, starting with a change whose numerical equivalence is easier to audit, such as coefficient-load hoisting or force-component reuse in only one kernel. Avoid another mixed bond+angle arithmetic rewrite until the exact source of the force delta is localized.
- controller decision: Reject `25f839bdfc42d92e85ea7f477ea3496c7e82ac3f`; retain incumbent `911f06a501008c67741eb0ff406d6554eccb81d7`.
#### Iteration 3: ACCEPTED
- hypothesis: In the benchmark water topology, `bond_class2` sees exactly one bond type, so adding a general single-bond-type fast path should remove per-bond type reads and allow coefficient/derivative constants to be hoisted out of the inner loop while preserving the exact force and energy formulas.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.27301`, improving on the incumbent `0.28156` by `3.04%` and on baseline `0.32627` by `16.32%`, which clears the `2%` minimum improvement threshold. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.27261` and `test-64r-short` reported `bond_seconds=0.019165`; peak RSS was `156.28 MB`.
- lesson learned: The safe optimization space is narrower than the iteration-2 failure suggested. Topology-homogeneity dispatch plus exact-math coefficient hoisting inside `bond_class2` delivered another measurable win without perturbing correctness, so the benchmark benefits from eliminating metadata/coefficient traffic when the system is uniform even if broader algebraic rewrites are risky.
- next hypothesis: Extend the same style of exact-math, homogeneity-aware fast path to the next uniform bonded kernel, most plausibly `angle_harmonic`, or look for similarly auditable constant-hoisting/bookkeeping reductions that preserve the current formulas and keep the experiment scoped to one kernel at a time.
- controller decision: Accept `4a79c588ea0869e5216d32c826251941d28332ac` as the new incumbent, replacing `911f06a501008c67741eb0ff406d6554eccb81d7`.
#### Iteration 4: REJECTED
- hypothesis: The TIP4P benchmark topology also has exactly one angle type, so mirroring the accepted `bond_class2` approach in `angle_harmonic` with a single-angle-type dispatch plus hoisted `k[1]` and `theta0[1]` should trim per-angle metadata and coefficient loads without changing the force or energy formulas.
- changed files:
  - `src/MOLECULE/angle_harmonic.cpp`
  - `src/MOLECULE/angle_harmonic.h`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.27052`, which is `0.91%` better than incumbent `0.27301` and `17.09%` better than baseline `0.32627`, but it does not satisfy the controller objective's `2%` minimum relative improvement for promotion. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.27052` and `test-64r-short` reported `bond_seconds=0.018975`; peak RSS was `156.34 MB`.
- lesson learned: The single-type fast-path idea transfers cleanly to `angle_harmonic` from a correctness perspective, but in this benchmark that kernel is too small a share of the bonded hot path for coefficient-hoist-only cleanup to move the weighted median enough on its own. Safe bookkeeping reductions in `angle_harmonic` are real but low leverage compared with the accepted `bond_class2` work.
- next hypothesis: Keep the accepted single-type dispatch structure in mind, but aim the next experiment at a larger remaining cost center or a broader exact-math overhead reduction that still preserves formulas. Promising directions are another isolated `bond_class2` micro-optimization, or an `angle_harmonic` follow-up that removes more per-angle overhead than just type/coefficient traffic while staying numerically identical.
- controller decision: Reject `e5c1ddf090faf73367db2c2b0d16c1cf09459426`; retain incumbent `4a79c588ea0869e5216d32c826251941d28332ac` because the measured gain is below the `2%` promotion threshold.
#### Iteration 5: REJECTED
- hypothesis: Pair a numerically exact `bond_class2` micro-optimization that caches `fx`/`fy`/`fz` per bond with the previously validated `angle_harmonic` single-angle-type fast path, hoping the combined reduction in redundant multiply work and per-angle type/coefficient traffic would push the incumbent over the `2%` promotion bar without changing force math.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/MOLECULE/angle_harmonic.cpp`
  - `src/MOLECULE/angle_harmonic.h`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.27041`, which is `0.95%` better than incumbent `0.27301` and `17.12%` better than baseline `0.32627`, but it does not satisfy the controller objective's `2%` minimum relative improvement for promotion. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.27005` and `test-64r-short` reported `bond_seconds=0.018988`; peak RSS was `156.22 MB`.
- lesson learned: This bundled follow-up remained correctness-safe, but the incremental value beyond iteration 4 was effectively noise: restoring the `angle_harmonic` single-type path and caching per-bond force components only moved the weighted median from `0.27052` to `0.27041` (about `0.04%`). That suggests the remaining easy exact-math cleanup in these two bonded kernels is close to exhausted, and especially that `bond_class2` force-component caching is too small a lever to justify another bundled promotion attempt by itself.
- next hypothesis: Shift the next experiment back to a single higher-leverage kernel, preferably `bond_class2`, and look for a larger numerically exact reduction than simple force-component reuse or angle-type hoisting alone. If the `angle_harmonic` single-type fast path is kept in future candidates, treat it as a reusable baseline ingredient that must be paired with a materially stronger bond-side win.
- controller decision: Reject `224da0125669fbc7c5f70c6df1be14dcc6cc295c`; retain incumbent `4a79c588ea0869e5216d32c826251941d28332ac` because the measured gain is below the `2%` promotion threshold.
#### Iteration 6: REJECTED
- hypothesis: In the accepted single-bond-type `bond_class2` path, bondlist entries are emitted in consecutive same-`i1` runs, so accumulating each run should let the loop load `x[i1]` once and write `f[i1]` once per run while preserving the exact per-bond force, energy, and tally order.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.27144`, which is `0.58%` better than incumbent `0.27301` and `16.81%` better than baseline `0.32627`, but it does not satisfy the controller objective's `2%` minimum relative improvement for promotion. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.27144` and `test-64r-short` reported `bond_seconds=0.01903`; peak RSS was `157.75 MB`.
- lesson learned: Reusing the leading atom's coordinates and deferred force writeback across same-`i1` bond runs is correctness-safe and measurably helpful, but it still lands in the same sub-1% regime as the recent bookkeeping-oriented attempts. That suggests repeated `i1` loads/stores are no longer a dominant cost inside the one-type `bond_class2` fast path; the remaining headroom is more likely gated by the per-bond distance/polynomial work or by other benchmark kernels.
- next hypothesis: If `bond_class2` is revisited next, target a larger exact-math reduction than leading-atom reuse alone, such as removing a heavier per-bond overhead inside the accepted single-type structure or cutting EV/branch costs without changing arithmetic order. Otherwise, shift attention to a broader remaining hot path with more than sub-1% headroom.
- controller decision: Reject `e639b3949f97dddf2d44df07d2cf495f78123aa6`; retain incumbent `4a79c588ea0869e5216d32c826251941d28332ac` because correctness passed but the gain is only `0.58%`, below the `2%` promotion threshold.
#### Iteration 7: REJECTED
- hypothesis: Restore the previously validated `angle_harmonic` single-angle-type fast path and combine it with a more explicit same-`i1` two-bond block inside the single-type `bond_class2` kernel so the loop can reuse the leading atom state across adjacent bonds while preserving the exact per-bond math and tally order.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/MOLECULE/angle_harmonic.cpp`
  - `src/MOLECULE/angle_harmonic.h`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.27624`, which is `1.18%` slower than incumbent `0.27301` even though it remains `15.33%` better than baseline `0.32627`, so it fails the objective and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.27635` and `test-64r-short` reported `bond_seconds=0.019376`; peak RSS was `158.89 MB`.
- lesson learned: This combination shows that individually correctness-safe micro-optimizations do not compose additively here. The explicit same-`i1` two-bond block appears to add enough control flow, state management, or code-size pressure inside `bond_class2` to erase the modest benefit of restoring the `angle_harmonic` single-type path and actually regress the incumbent.
- next hypothesis: Do not pursue more explicit pair-blocking variants of same-`i1` reuse unless profiling shows a much larger payoff than the added loop complexity. Future candidates should either keep the `angle_harmonic` single-type path as optional scaffolding while targeting a lighter-weight, higher-leverage exact reduction in `bond_class2`, or move to another remaining hot path with clearer than sub-1% headroom.
- controller decision: Reject `47d88a34949485d0d2537b85da1c1bc763839085`; retain incumbent `4a79c588ea0869e5216d32c826251941d28332ac` because correctness passed but the candidate regressed the primary metric by `1.18%` versus the incumbent.
#### Iteration 8: REJECTED
- hypothesis: Restore the previously validated `angle_harmonic` single-angle-type fast path and tighten one-type bonded bookkeeping by materializing angle `ev_tally` arrays only in `EVFLAG` instantiations and computing `bond_class2` energy-only intermediates only when `EFLAG` is enabled, aiming to shave exact overhead without changing force math.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/MOLECULE/angle_harmonic.cpp`
  - `src/MOLECULE/angle_harmonic.h`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.27111`, which is `0.70%` better than incumbent `0.27301` and `16.91%` better than baseline `0.32627`, but it does not satisfy the controller objective's `2%` minimum relative improvement for promotion. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.27111` and `test-64r-short` reported `bond_seconds=0.018998`; peak RSS was `159.53 MB`.
- lesson learned: This exact bookkeeping cleanup is correctness-safe and does recover some performance, but it remains in the same sub-1% band as the other post-incumbent bonded micro-optimizations. Restoring the `angle_harmonic` single-type path plus gating `ev_tally` and energy-only temporaries more tightly is not enough to move the benchmark materially; the remaining bonded hot-path cost is likely dominated by heavier per-bond or per-angle math rather than this bookkeeping.
- next hypothesis: Keep the single-type `angle_harmonic` path as reusable scaffolding if it simplifies future experiments, but stop spending iterations on small `EVFLAG`/`EFLAG` bookkeeping trims alone. The next viable candidate should either remove a larger exact cost inside the accepted `bond_class2` single-type kernel or target another benchmark hot path with clearer than sub-1% headroom.
- controller decision: Reject `e3c2ae9ee96a1337381551c62385cb67dccf762b`; retain incumbent `4a79c588ea0869e5216d32c826251941d28332ac` because correctness passed but the gain is only `0.70%`, below the `2%` promotion threshold.
#### Iteration 9: REJECTED
- hypothesis: Add a dedicated two-way unrolled `bond_class2` one-type hot path for the dominant no-`evflag`, Newton-on case so the compiler has more independent exact work around the `sqrt` and divide latency while preserving the exact per-bond math and force-update order.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.26892`, which is `1.50%` better than incumbent `0.27301` and `17.58%` better than baseline `0.32627`, but it still fails the controller objective's `2%` minimum relative improvement for promotion. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.26892` and `test-64r-short` reported `bond_seconds=0.01887`; peak RSS was `159.61 MB`.
- lesson learned: Exact two-way unrolling in the incumbent `bond_class2` single-type path is correctness-safe and is the strongest post-incumbent bond-only improvement so far, but it still tops out below the promotion bar. That suggests there is some remaining instruction-level headroom in the dominant no-`evflag`, Newton-on path, yet loop-overhead and latency-hiding tweaks alone are unlikely to deliver the next accepted result.
- next hypothesis: Stop spending iterations on more elaborate unrolling or pair-blocking variants inside the accepted `bond_class2` one-type kernel unless they are paired with a clearly larger exact reduction. The next viable candidate should either remove a heavier exact per-bond cost than loop overhead alone or move to another benchmark hot path with more than roughly `1.5%` headroom.
- controller decision: Reject `6fbda0abc94b8116e90561046832a268d1d97728`; retain incumbent `4a79c588ea0869e5216d32c826251941d28332ac` because correctness passed but the gain is only `1.50%`, below the `2%` promotion threshold.
#### Iteration 10: ACCEPTED
- hypothesis: The strongest correctness-safe post-incumbent ideas were already isolated but individually sub-threshold: the `bond_class2` one-type two-way unrolled hot path from iteration 9 and the `angle_harmonic` one-type dispatch plus constant hoisting from iteration 4. Combining them on top of incumbent `4a79c588ea08` should stack exact reductions from two distinct bonded kernels while preserving force math and update order.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
  - `src/MOLECULE/angle_harmonic.h`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.26621`, which is `2.49%` better than incumbent `0.27301` and `18.41%` better than baseline `0.32627`, clearing the controller objective's `2%` minimum relative improvement for promotion. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.26621` and `test-64r-short` reported `bond_seconds=0.018738`; peak RSS was `159.09 MB`.
- lesson learned: Previously validated sub-threshold wins can compose into an accepted result when they target distinct bonded kernels and preserve exact math. The `angle_harmonic` single-type fast path was not promotion-worthy on its own, but it is useful supporting scaffolding when paired with a stronger `bond_class2` improvement; together they were additive enough to cross the benchmark's promotion bar.
- next hypothesis: Keep both one-type fast paths as part of the new incumbent and add only one fresh optimization lever at a time from here. Future candidates should prioritize heavier exact per-bond cost reductions inside `bond_class2` or another remaining hot path with clearer headroom, while treating `angle_harmonic` coefficient-hoist cleanup as already harvested baseline structure rather than a standalone target.
- controller decision: Accept `cf4ba8e7204d7484a30eb354b07f05005552dd43` as the new incumbent, replacing `4a79c588ea0869e5216d32c826251941d28332ac`.
#### Iteration 11: ACCEPTED
- hypothesis: The promoted iteration-10 incumbent still used the lighter one-type `angle_harmonic` specialization from iteration 4, so a more aggressive exact fast path for the dominant one-type, no-`evflag`, Newton-on angle case might unlock materially more headroom than coefficient hoisting alone. Scalar force temporaries plus two-way exact unrolling should reduce `angle_harmonic` loop overhead and latency without changing arithmetic outcomes.
- changed files:
  - `src/MOLECULE/angle_harmonic.cpp`
  - `src/MOLECULE/angle_harmonic.h`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.24606`, which is `7.57%` better than incumbent `0.26621` and `24.58%` better than baseline `0.32627`, clearing the controller objective's `2%` minimum relative improvement for promotion by a wide margin. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.24606` and `test-64r-short` reported `bond_seconds=0.017472`; peak RSS was `157.68 MB`.
- lesson learned: `angle_harmonic` still had substantially more exact optimization headroom than the earlier single-type dispatch and coefficient-hoist experiment implied. The decisive gain here came from specializing the dominant angle case more deeply, not from further `bond_class2` work, which means the earlier sub-1% angle-only result understated how much performance was still tied up in `angle_harmonic` control flow and force accumulation structure.
- next hypothesis: Keep the new `angle_harmonic` no-`evflag`, Newton-on hot path as part of the incumbent baseline. If `angle_harmonic` is revisited again, target another dominant exact specialization boundary or residual per-angle overhead on top of this path rather than re-running coefficient-hoist-only ideas; otherwise return to `bond_class2` only with a heavier exact cost reduction than the previously rejected unrolling, pair-blocking, and leading-atom reuse variants.
- controller decision: Accept `672cbe901cae5b8e7041fb6f4c3ab883fd8e6576` as the new incumbent, replacing `cf4ba8e7204d7484a30eb354b07f05005552dd43`.
#### Iteration 12: ACCEPTED
- hypothesis: The iteration-11 incumbent still recomputed the same O-H bond geometry inside `angle_harmonic` that `bond_class2` had already computed in the same timestep. If the dominant one-type `bond_class2` hot path caches bond endpoints plus `(delx, dely, delz, rsq, r)`, the dominant one-type, no-`evflag`, Newton-on `angle_harmonic` hot path should be able to reuse the paired bond data when local ordering matches each angle and fall back directly otherwise, eliminating duplicate geometry work without changing force math.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.23436`, which is `4.75%` better than incumbent `0.24606` and `28.17%` better than baseline `0.32627`, clearing the controller objective's `2%` minimum relative improvement threshold. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.23436` and `test-64r-short` reported `bond_seconds=0.016618`; peak RSS was `157.12 MB`.
- lesson learned: Exact cross-kernel data reuse is a stronger lever than the remaining isolated micro-optimizations inside either bonded kernel. Reusing `bond_class2` geometry inside `angle_harmonic` stayed correctness-safe because the implementation validated timestep freshness and local bond ordering, then fell back to the original per-angle geometry path whenever the cached layout did not match. That shows this water topology is regular enough for shared bonded-state reuse to pay off, but only when the optimization is guarded by explicit structural checks rather than assumed list alignment.
- next hypothesis: Treat the cached bond-geometry handoff as new incumbent scaffolding. If bonded work is revisited, look for another exact reuse opportunity that exploits the same regular topology while preserving a direct fallback for any ordering mismatch; otherwise search for heavier remaining costs on top of the new baseline instead of another round of single-kernel unrolling or bookkeeping cleanup.
- controller decision: Accept `0826ffd3944439fee85546aa3c9671ac4478beb4` as the new incumbent, replacing `672cbe901cae5b8e7041fb6f4c3ab883fd8e6576`.
#### Iteration 13: ACCEPTED
- hypothesis: The iteration-12 incumbent already proved that exact cross-kernel reuse pays off, but its shared state is still organized around bond production rather than `angle_harmonic` consumption. Replacing that with an angle-oriented cache built in `bond_class2` and a dedicated branch-free, two-way-unrolled dominant `angle_harmonic` consumer path should cut reuse overhead further while preserving a direct fallback to the incumbent local-geometry path whenever bond/angle pairing does not align.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22529`, which is `3.87%` better than incumbent `0.23436` and `30.95%` better than baseline `0.32627`, clearing the controller objective's `2%` minimum relative improvement threshold. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22529` and `test-64r-short` reported `bond_seconds=0.015985`; peak RSS was `157.60 MB`.
- lesson learned: The shape of the reuse handoff matters, not just the fact that geometry is reused. Replacing the earlier bond-oriented reuse with an angle-oriented cache and a branch-free, two-way-unrolled `angle_harmonic` consumer path delivered another multi-percent win, which indicates the remaining bonded headroom is increasingly tied to how cheaply `angle_harmonic` can validate and consume shared bond state rather than to additional isolated `bond_class2` math cleanup.
- next hypothesis: Keep the angle-oriented cache plus guarded fallback as the new incumbent baseline. If bonded work is revisited, prioritize another exact consumer-oriented reuse or cache-layout reduction on top of this path, such as trimming remaining pairing-validation overhead or hoisting more angle-side invariants, but do not weaken the explicit alignment checks or direct fallback to local geometry.
- controller decision: Accept `30584dbe2ab0c1af8b2a7a566a91b31999295371` as the new incumbent, replacing `0826ffd3944439fee85546aa3c9671ac4478beb4`.
#### Iteration 14: REJECTED
- hypothesis: The iteration-13 incumbent still leaves some `angle_harmonic` math on the consumer side after validating and loading the shared angle-oriented bond cache. If `bond_class2` prepares the final per-angle force vectors directly and `angle_harmonic` becomes a pure force-apply path under the same structural guard and fallback, the remaining consumer-side overhead might drop further without changing results.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.26506`, which is `17.65%` slower than incumbent `0.22529` even though it remains `18.76%` better than baseline `0.32627`, so it fails the objective decisively and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.26607` and `test-64r-short` reported `bond_seconds=0.018653`; peak RSS was `156.96 MB`.
- lesson learned: Moving the remaining angle-force preparation upstream into `bond_class2` is the wrong cache-shape trade for this benchmark. The iteration-13 geometry-oriented handoff was fast because it let `angle_harmonic` consume reused state cheaply; this variant instead pushes `acos`/`sqrt`/reciprocal-heavy angle math and final force-vector preparation into the bond kernel, inflating the measured `bond_seconds` hot path and overwhelming any savings from the simpler consumer.
- next hypothesis: Keep iteration 13's angle-oriented geometry cache and guarded direct fallback as the incumbent baseline, but do not move full per-angle force preparation into `bond_class2` again. If bonded work is revisited, look for lighter consumer-side reuse or validation/cache-access reductions that preserve the cheap geometry handoff, or switch to another hot path with clearer remaining headroom.
- controller decision: Reject `0890ed81e853f3eb86407fb21b716c8ca3b4c784`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate regressed the primary metric by `17.65%` versus the incumbent.
#### Iteration 15: REJECTED
- hypothesis: The iteration-13 incumbent still spends work loading full cached geometry and doing cached-path divisions in the dominant `angle_harmonic` consumer. If the shared angle-oriented cache is compacted to bond deltas plus inverse bond lengths, the cached path should be able to reconstruct what it needs with less cache traffic and no division, improving performance without changing results.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`; all runs completed, both cases converged, and the candidate measured `weighted_median_bond_seconds=0.21063`, which is `6.51%` better than incumbent `0.22529` and `35.44%` better than baseline `0.32627`. The per-case `bond_seconds` were `0.21063` for `test-32r-long` and `0.015063` for `test-64r-short`, with peak RSS `157.68 MB`. However, authoritative correctness failed under field tolerances in both cases and triggered a hard reject: `test-32r-long` exceeded thresholds for `etotal` (`7.92579e-05` relative), `pe` (`0.00213102` relative), `ke`/`temp` (`0.00400269` relative), `press` (`1.39153e+08` abs), and force abs/rms deltas (`0.115462` / `0.0280042`), while `test-64r-short` also broke the same fields at smaller magnitudes.
- lesson learned: This cache compaction is not numerically exact enough for the benchmark. The large long-case thermo and force deltas show that replacing the current geometry-oriented handoff with delta-plus-inverse-length reconstruction changes the effective angle-force math, even though the runtime drops sharply. This matches the earlier failure mode from iteration 2: reciprocal-based or otherwise re-derived bonded geometry may look algebraically equivalent, but it is not acceptable here unless it preserves the incumbent's exact arithmetic behavior to tolerance.
- next hypothesis: Keep the iteration-13 angle-oriented geometry cache and guarded fallback unchanged as the correctness-safe baseline. If cache-layout work is revisited, only pursue reductions that preserve the exact quantities and arithmetic already consumed by the hot `angle_harmonic` path, such as lighter packing or access-order changes around the existing cached `del*`, `rsq`, and `r` data, rather than reconstructing geometry from reciprocals; otherwise search for headroom in another hot path.
- controller decision: Reject `fdcd68522a8386e883490db921837f601bb3edeb`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because the candidate is a hard correctness failure despite the nominal `6.51%` primary-metric improvement.
#### Iteration 16: REJECTED
- hypothesis: After iteration 15 showed inverse-length reconstruction was not correctness-safe, a lighter cache compaction that keeps exact cached bond lengths but drops cached `rsq` fields might preserve the incumbent arithmetic while shrinking the angle-oriented cache from 10 doubles to 8 doubles. Recomputing cached-path `rsq` values from cached deltas inside `angle_harmonic` should recover correctness while still reducing cache traffic enough to beat the incumbent.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.23245`, which is `3.18%` slower than incumbent `0.22529` even though it remains `28.76%` better than baseline `0.32627`, so it fails the objective and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.23245` and `test-64r-short` reported `bond_seconds=0.016521`; peak RSS was `157.44 MB`.
- lesson learned: Restoring exact bond-length semantics is enough to recover correctness from iteration 15, but cache compaction by itself is not a free win. Dropping cached `rsq` fields shifts work back into the dominant `angle_harmonic` consumer, and that recomputation costs more than the smaller `BondClass2AngleHotEntry` footprint saves. For this incumbent, geometry values that the consumer uses directly should stay precomputed unless a larger access-pattern win accompanies the change.
- next hypothesis: Keep the iteration-13 cache layout, including cached `rsq` and `r`, as the performance baseline. If cache-layout work is revisited, target consumer-side access or validation overhead reductions that preserve the current exact geometry payload rather than deleting fields that the hot path immediately reconstructs; otherwise look for headroom in a different hot path.
- controller decision: Reject `1f058192fe33418582791fc8083eb481c5321b50`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate regressed the primary metric by `3.18%` versus the incumbent.
- [2026-04-12T19:22:24Z] iter_0017: rejected 738ffb9e3a30 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.22321`, which was `31.59%` better than baseline and `0.92%` better than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, but it did not clear the controller's `2%` minimum improvement threshold, so the incumbent remains unchanged.
- [2026-04-12T19:22:24Z] rejected: Precompute the one-type bond/angle pairing layout once per topology build and reuse it in bond_class2's hot angle-cache fill to remove repeated per-step topology validation while preserving the incumbent cache payload and angle_harmonic consumer math. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 0.92% versus incumbent 30584dbe2ab0, below the 2% acceptance threshold.] (738ffb9e3a30, weighted_median_bond_seconds=0.22321)
- [2026-04-12T19:54:13Z] iter_0018: rejected bed99cfa6486 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.22343`, which was `31.52%` better than baseline and `0.83%` better than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, but it still missed the controller's `2%` minimum improvement threshold so the incumbent remains unchanged.
- [2026-04-12T19:54:13.113681Z] rejected: Prepare and reuse a prevalidated one-type bond-angle orientation layout in `bond_class2` hot cache fill while keeping the incumbent angle cache payload and `angle_harmonic` math unchanged. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 0.83% versus incumbent 30584dbe2ab0, below the 2% promotion threshold.] (bed99cfa6486, weighted_median_bond_seconds=0.22343)
- [2026-04-12T20:22:55Z] iter_0019: rejected acd350270fd3 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.22126`, which was `32.19%` better than baseline and `1.79%` better than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, but it still missed the controller's `2%` minimum improvement threshold so the incumbent remains unchanged.
- [2026-04-12T20:22:55.806766Z] rejected: Compact the cached angle hot entry to store the exact bond-length product `r12 = r1*r2` instead of separate `r1`/`r2`, preserving cached `del*` and `rsq` semantics while reusing that exact product in the cached `angle_harmonic` hot path. [Correctness passed, but the candidate improved `weighted_median_bond_seconds` by only `1.79%` versus incumbent `30584dbe2ab0`, below the `2%` promotion threshold.] (acd350270fd3, weighted_median_bond_seconds=0.22126)
- [2026-04-12T20:44:02Z] iter_0020: rejected c0a62a11cfaf after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.22182`, which was `32.01%` better than baseline and `1.54%` better than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, but it still missed the controller's `2%` minimum improvement threshold so the incumbent remains unchanged.
- [2026-04-12T20:44:02Z] rejected: Combine prevalidated bond-angle orientation layout reuse in `bond_class2` with exact `r12` hot-cache compaction consumed by `angle_harmonic`. [Correctness passed, but the candidate improved `weighted_median_bond_seconds` by only `1.54%` versus incumbent `30584dbe2ab0`, below the `2%` promotion threshold.] (c0a62a11cfaf, weighted_median_bond_seconds=0.22182)
- [2026-04-12T21:06:26Z] iter_0021: correctness_failure e4103f7dab04 after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.21864`, which was `32.99%` better than baseline and `2.95%` better than incumbent `30584dbe2ab0`, but it triggered a hard correctness failure in both benchmark cases, so the incumbent remains unchanged.
- [2026-04-12T21:06:26Z] correctness_failure: Store exact `r12 = r1*r2` in the hot angle cache and reuse a shared local `inv_r12` in the cached `angle_harmonic` hot path to remove the duplicate division by the same bond-length product. [Hard correctness failure in both benchmark cases despite a nominal `2.95%` speedup, so incumbent `30584dbe2ab0` remains.; case test-32r-long etotal relative_delta exceeds threshold (7.66767e-05 > 1e-08); case test-32r-long pe relative_delta exceeds threshold (0.00184228 > 1e-08); case test-32r-long ke relative_delta exceeds threshold (0.00346259 > 1e-08); case test-32r-long temp relative_delta exceeds threshold (0.00346259 > 1e-08); case test-32r-long press abs_delta exceeds threshold (1.29238e+08 > 1e-06); case test-32r-long force_components abs_delta exceeds threshold (0.100389 > 1e-08); case test-32r-long force_components rms_delta exceeds threshold (0.0278753 > 1e-08); case test-64r-short etotal relative_delta exceeds threshold (7.21825e-08 > 1e-08); case test-64r-short pe relative_delta exceeds threshold (6.86454e-08 > 1e-08); case test-64r-short ke relative_delta exceeds threshold (1.44472e-07 > 1e-08); case test-64r-short temp relative_delta exceeds threshold (1.46005e-07 > 1e-08); case test-64r-short press abs_delta exceeds threshold (1130 > 1e-06); case test-64r-short force_components abs_delta exceeds threshold (1e-05 > 1e-08); case test-64r-short force_components rms_delta exceeds threshold (1.15835e-06 > 1e-08)] (e4103f7dab04, weighted_median_bond_seconds=0.21864)
- [2026-04-12T21:28:04Z] iter_0022: rejected efcb4c8f585c after authoritative benchmarking. The candidate recorded `weighted_median_bond_seconds=0.22213`, which was `31.92%` better than baseline and `1.40%` better than incumbent `30584dbe2ab0`, with correctness passing in both benchmark cases, but it did not clear the controller's `2%` minimum improvement threshold, so the incumbent remains unchanged.
- [2026-04-12T21:28:04.016355Z] rejected: Compact the one-type hot angle cache to exact `r12 = r1*r2` and add prepared hot-angle index/layout metadata so `bond_class2` reuses stable topology preparation and cached `angle_harmonic` stops rereading `neighbor->anglelist` while preserving the incumbent arithmetic order. [Correctness passed, but the candidate improved `weighted_median_bond_seconds` by only `1.40%` versus incumbent `30584dbe2ab0`, below the `2%` promotion threshold.] (efcb4c8f585c, weighted_median_bond_seconds=0.22213)
#### Iteration 17: REJECTED
- hypothesis: The iteration-13 incumbent still spends per-step work in `bond_class2` validating whether each one-type bond pair lines up with the corresponding angle-oriented cache slot, even though that bond/angle topology layout is stable between neighbor rebuilds. If the one-type pairing mode is prepared once per topology build and reused during hot cache fill, the incumbent cache payload and `angle_harmonic` consumer math should stay exact while trimming repeated validation overhead enough to improve the bonded hot path.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22321`, which is `0.92%` better than incumbent `0.22529` and `31.59%` better than baseline `0.32627`, but it misses the controller objective's `2%` minimum relative improvement threshold and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22292` and `test-64r-short` reported `bond_seconds=0.015842`; peak RSS was `157.41 MB`.
- lesson learned: Hoisting bond/angle layout validation out of the per-step hot cache fill is correctness-safe and measurably helpful, but it is too small a lever on top of the current incumbent. Once the angle-oriented cache already preserves consumer-friendly `del*`, `rsq`, and `r` payloads, the remaining validation work in `bond_class2` contributes less than a percent of total benchmark headroom by itself.
- next hypothesis: Keep the iteration-13 cache payload and guarded fallback as the baseline, and do not spend another iteration on validation-hoist-only changes. If this reuse path is revisited, combine the preserved exact geometry payload with a larger reduction in cache-fill or `angle_harmonic` consumption overhead, such as cutting remaining access-path branching or data-movement costs around the current entries; otherwise pivot to a different hot path with clearer headroom.
- controller decision: Reject `738ffb9e3a30a7e234fa33bad43a0daf708db7d1`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate improved the primary metric by only `0.92%`, below the `2%` promotion threshold.
#### Iteration 18: REJECTED
- hypothesis: Iteration 17 showed that topology-build-time validation hoisting was correctness-safe but too small on its own. Preparing a prevalidated one-byte bond/angle orientation layout once per stable topology and reusing it in `bond_class2` hot cache fill should preserve the incumbent angle-oriented cache payload and unchanged `angle_harmonic` math while removing the remaining per-step orientation checks from the producer path.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22343`, which is `0.83%` better than incumbent `0.22529` and `31.52%` better than baseline `0.32627`, but it still misses the controller objective's `2%` minimum relative improvement threshold and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22364` and `test-64r-short` reported `bond_seconds=0.015871`; peak RSS was `157.72 MB`.
- lesson learned: Prevalidating the orientation layout removes a bit more producer-side work than iteration 17's broader validation-hoist-only change, but the whole producer-side orientation/validation slice is still a sub-1% lever on top of the iteration-13 incumbent. Once the cache already preserves exact consumer-ready `del*`, `rsq`, and `r` values and `angle_harmonic` math is unchanged, shaving the last branchy orientation checks in `bond_class2` is not enough to move the benchmark materially.
- next hypothesis: Stop spending iterations on producer-side layout-preparation-only changes. If the angle-oriented cache is revisited, combine the preserved exact geometry payload with a larger reduction in `angle_harmonic` consumption overhead or cache-fill data movement; otherwise pivot to a different hot path with clearer remaining headroom instead of refining bond/angle orientation metadata in isolation.
- controller decision: Reject `bed99cfa64862e1fba21d6ed2e568e9932f147cc`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate improved the primary metric by only `0.83%`, below the `2%` promotion threshold.
#### Iteration 19: REJECTED
- hypothesis: Iterations 16-18 showed that deleting cached consumer inputs or only trimming producer-side validation work was either slower or sub-threshold. Replacing separate cached bond lengths `r1` and `r2` with their exact product `r12 = r1*r2` should preserve the incumbent cached `angle_harmonic` arithmetic, shrink the hot entry payload slightly, and remove repeated consumer-side products without risking the reciprocal-reconstruction correctness failure from iteration 15.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22126`, which is `1.79%` better than incumbent `0.22529` and `32.19%` better than baseline `0.32627`, but it still misses the controller objective's `2%` minimum relative improvement threshold and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22087` and `test-64r-short` reported `bond_seconds=0.015769`; peak RSS was `157.43 MB`.
- lesson learned: Exact-product cache compaction is correctness-safe and measurably better than the recent producer-side orientation-layout-only variants, but it is still only a near-threshold win. Preserving cached `del*` and `rsq` while collapsing `r1`/`r2` into the already-needed `r12` removes some payload and consumer work, yet the remaining headroom inside this exact cache layout looks small enough that field-substitution-only changes are unlikely to clear promotion by themselves.
- next hypothesis: If the angle-oriented cache is revisited again, keep exact consumer-ready geometry semantics intact and only pair them with a larger reduction in `angle_harmonic` load/use overhead or cache-fill data movement than a single-field substitution provides; otherwise pivot to another bonded hot-path lever rather than spending more standalone iterations on exact `r`-payload reshaping.
- controller decision: Reject `acd350270fd3efe03f2e257aa8fbfa6727d83b33`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate improved the primary metric by only `1.79%`, below the `2%` promotion threshold.
#### Iteration 20: REJECTED
- hypothesis: Iteration 19 showed that exact `r12` compaction was correctness-safe and close to the promotion bar, while iteration 18 showed that prevalidated producer-side orientation layout reuse was also correctness-safe but too small on its own. Combining those two validated ideas on top of the iteration-13 incumbent should preserve exact cached `del*` and `rsq` semantics, trim both producer-side layout work and consumer-side bond-length products, and potentially stack enough to clear the `2%` threshold.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22182`, which is `1.54%` better than incumbent `0.22529` and `32.01%` better than baseline `0.32627`, but it still misses the controller objective's `2%` minimum relative improvement threshold and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22182` and `test-64r-short` reported `bond_seconds=0.015828`; peak RSS was `157.29 MB`.
- lesson learned: These two correctness-safe micro-optimizations do not add constructively on the current cache design. The combined result is not only still sub-threshold, it is also slightly slower than iteration 19's `r12`-only variant (`0.22182` vs `0.22126`), which implies the extra producer-side layout machinery erases the small gain from eliminating separate cached `r1`/`r2` loads. On top of the incumbent angle-oriented cache, modest producer-side validation/layout savings and single-field exact payload reshaping are effectively exhausted as standalone or paired levers.
- next hypothesis: Stop spending more iterations on small combinations of producer-side orientation-layout reuse and exact `r`-payload reshaping within the current angle-oriented cache. Any follow-up on this reuse path should keep the exact cached geometry semantics and guarded fallback intact, but target a larger consumer-side load/use reduction in `angle_harmonic` or a more substantial cache-fill data-movement win; otherwise pivot to a different hot path with clearer remaining headroom.
- controller decision: Reject `c0a62a11cfaf197d44a5e48e9a4bf96eb4dc484d`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate improved the primary metric by only `1.54%`, below the `2%` promotion threshold.
#### Iteration 21: REJECTED
- hypothesis: Iteration 19 showed that exact `r12` cache compaction was correctness-safe but still slightly below the promotion bar. If the cached `angle_harmonic` path keeps storing exact `r12 = r1*r2` and simply reuses one shared local `inv_r12` instead of dividing by the same product twice, it should remove duplicate division work without changing the cache payload or the intended angle-force inputs.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`; all runs completed, both cases converged, and the candidate measured `weighted_median_bond_seconds=0.21864`, which is `2.95%` better than incumbent `0.22529` and `32.99%` better than baseline `0.32627`. The per-case `bond_seconds` were `0.2186` for `test-32r-long` and `0.015585` for `test-64r-short`, with peak RSS `158.12 MB`. However, authoritative correctness failed under field tolerances in both cases and triggered a hard reject: `test-32r-long` exceeded thresholds for `etotal` (`7.66767e-05` relative), `pe` (`0.00184228` relative), `ke`/`temp` (`0.00346259` relative), `press` (`1.29238e+08` abs), and force abs/rms deltas (`0.100389` / `0.0278753`), while `test-64r-short` also broke the same fields at smaller magnitudes.
- lesson learned: Exact cache payloads alone are not enough; the consumer's floating-point evaluation order also matters. Even though this variant kept exact cached `r12`, replacing the two original divisions with a shared reciprocal changed the arithmetic enough to recreate the same class of hard correctness failure seen in iteration 15's reciprocal-based reconstruction. For this benchmark, reciprocal reuse inside the cached `angle_harmonic` path is not numerically equivalent to the incumbent division structure.
- next hypothesis: Do not pursue shared-reciprocal reuse in the cached `angle_harmonic` path, even when the cache stores exact `r12`. If this angle-oriented cache is revisited, preserve the incumbent arithmetic order exactly and only look for non-arithmetic reductions around the same exact geometry semantics, such as load packing, access ordering, or larger data-movement wins; otherwise pivot to a different hot path with clearer remaining headroom.
- controller decision: Reject `e4103f7dab04a1b90d89db59e68b6349a44b910c`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because the candidate is a hard correctness failure despite the nominal `2.95%` primary-metric improvement.
#### Iteration 22: REJECTED
- hypothesis: Iteration 19 showed that exact `r12` cache compaction was correctness-safe and nearly fast enough, while iterations 17-18 showed that prepared bond/angle layout reuse was also correctness-safe but too small on its own. Combining exact `r12` payload compaction with prepared hot-angle index/layout metadata, and letting cached `angle_harmonic` consume cached indices instead of rereading `neighbor->anglelist`, should preserve the incumbent arithmetic order while trimming both cache payload and consumer-side topology traffic enough to clear the `2%` promotion bar.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22213`, which is `1.40%` better than incumbent `0.22529` and `31.92%` better than baseline `0.32627`, but it still misses the controller objective's `2%` minimum relative improvement threshold and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22213` and `test-64r-short` reported `bond_seconds=0.015969`; peak RSS was `157.71 MB`.
- lesson learned: This exact-arithmetic combination is correctness-safe, but the extra prepared hot-angle index/layout machinery still does not stack into a promotable win. The candidate is slightly slower than the simpler exact-`r12` variant from iteration 19 (`0.22213` vs `0.22126`), which indicates that avoiding cached-path `neighbor->anglelist` rereads and reusing prepared topology metadata saves less than the added producer-side metadata preparation and cache traffic costs on this workload. Within the current angle-oriented cache design, exact `r12` compaction remains the stronger lever and index/layout metadata is still only a marginal add-on.
- next hypothesis: Do not spend more standalone iterations on combining exact `r12` cache compaction with prepared hot-angle index/layout metadata or cached-index substitution for `neighbor->anglelist` rereads. If this reuse path is revisited, keep exact cached geometry semantics and arithmetic order intact but target a materially larger consumer-side data-movement reduction than index reuse alone provides; otherwise pivot to a different bonded hot path with clearer remaining headroom.
- controller decision: Reject `efcb4c8f585c2bd3979f0b70c97e2ef51614aa85`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate improved the primary metric by only `1.40%`, below the `2%` promotion threshold.
#### Iteration 23: REJECTED
- hypothesis: Iteration 22 showed that exact cache-payload and cached-index refinements were still sub-threshold, so the next narrow lever was instruction-level parallelism in the dominant cached consumer itself. Widening the cached `angle_harmonic` no-`evflag`, Newton-on hot path from two-way to four-way exact unrolling while preserving the incumbent cache payload, cached-path arithmetic order, guard, and direct fallback should keep correctness risk low and might reduce enough loop/control overhead to finally clear the `2%` promotion bar.
- changed files:
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22195`, which is `1.48%` better than incumbent `0.22529` and `31.97%` better than baseline `0.32627`, but it still misses the controller objective's `2%` minimum relative improvement threshold and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22193` and `test-64r-short` reported `bond_seconds=0.01584`; peak RSS was `157.82 MB`.
- lesson learned: Pure cached-consumer unroll widening is correctness-safe and modestly helpful, but the remaining loop-control and scheduling headroom in this path is too small to promote on its own. The four-way variant edges out the recent exact-`r12` plus cached-index metadata candidate from iteration 22 (`0.22195` vs `0.22213`), but it is still slower than the simpler exact-`r12` compaction from iteration 19 (`0.22126`). On top of the current angle-oriented cache, widening exact unrolling beyond the incumbent two-way consumer is another sub-threshold micro-optimization rather than a new promotable lever.
- next hypothesis: Do not spend more standalone iterations on pure cached `angle_harmonic` consumer unroll widening while preserving the same exact cache payload and arithmetic order. If this reuse path is revisited again, it likely needs a materially larger exact consumer-side data-movement reduction or a different cross-kernel lever to stack with the current baseline; otherwise pivot to another hot path with clearer remaining headroom.
- controller decision: Reject `567a79cfb769d4bd7418d1ab1648e409e0d6a33f`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate improved the primary metric by only `1.48%`, below the `2%` promotion threshold.
#### Iteration 24: REJECTED
- hypothesis: Iteration 23 showed that four-way exact cached-consumer unrolling was correctness-safe but sub-threshold, while iteration 19 showed that exact `r12` cache compaction was the best recent correctness-safe cache tweak. Combining those two exact changes while preserving the incumbent guard and cached-path division semantics should stack a bit more producer/consumer savings and might finally push the bonded hot path past the `2%` promotion bar without revisiting the correctness failure from shared reciprocal reuse.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22158`, which is `1.65%` better than incumbent `0.22529` and `32.09%` better than baseline `0.32627`, but it still misses the controller objective's `2%` minimum relative improvement threshold and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22158` and `test-64r-short` reported `bond_seconds=0.015782`; peak RSS was `157.48 MB`.
- lesson learned: The exact `r12` compaction and four-way cached-consumer widening do stack a little while preserving correctness, but not enough to matter for promotion. This combined candidate is slightly better than iteration 23's pure four-way consumer change (`0.22158` vs `0.22195`) and iteration 20's earlier producer-layout-plus-`r12` combination (`0.22158` vs `0.22182`), yet it still trails the simpler iteration 19 exact-`r12` compaction (`0.22126`). That pattern suggests the remaining headroom in this exact cache/unroll family is largely exhausted: once the incumbent guard, cached-path division order, and geometry semantics are preserved, stacking small producer/consumer micro-optimizations yields diminishing returns below the controller threshold.
- next hypothesis: Do not spend more standalone iterations on combining exact `r12` cache compaction with wider cached `angle_harmonic` unrolling while preserving the current guard and arithmetic semantics. If bonded-path work continues, it likely needs a materially larger exact data-movement reduction, a different correctness-safe cross-kernel reuse shape, or a pivot to another hot path with clearer remaining headroom rather than more stacking of this micro-optimization family.
- controller decision: Reject `2c69753dfc4867355dd37b71fb5260806b55efe5`; retain incumbent `30584dbe2ab0c1af8b2a7a566a91b31999295371` because correctness passed but the candidate improved the primary metric by only `1.65%`, below the `2%` promotion threshold.
#### Iteration 25: ACCEPTED
- hypothesis: Iteration 24 showed that exact `r12` payload compaction plus wider cached-consumer unrolling was still sub-threshold, while iteration 21 showed that changing cached-consumer arithmetic grouping around `r12` was not correctness-safe. Pairing the exact `r12` cache payload with a four-way exact unrolled one-type cached `bond_class2` producer instead, while preserving the incumbent `angle_harmonic` arithmetic order and fallback guard, should reduce producer-side loop and cache-fill overhead enough to clear promotion without revisiting the shared-reciprocal failure mode.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.21875`, which is `2.90%` better than incumbent `0.22529` and `32.95%` better than baseline `0.32627`, clearing the controller objective's `2%` minimum relative improvement threshold. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.21881` and `test-64r-short` reported `bond_seconds=0.01559`; peak RSS was `157.68 MB`.
- lesson learned: Exact `r12` compaction was not exhausted; it simply needed a larger exact producer-side companion than the recent consumer-side or metadata-only tweaks. The promotable stack came from shrinking the angle hot entry to the exact `r12` payload and cutting one-type cached `bond_class2` producer overhead with four-way exact unrolling, while leaving the cached `angle_harmonic` division/order semantics and fallback guard unchanged. That result reinforces two constraints for this benchmark: consumer arithmetic grouping must remain exactly stable, but producer-side cache-fill/data-movement reductions can still unlock multi-percent wins when paired with a correctness-safe compact payload.
- next hypothesis: Treat this accepted variant as the new baseline. If bonded-path work continues, preserve the exact cached `r12`, `del*`, and `rsq` semantics plus the incumbent `angle_harmonic` arithmetic order, structural validation, timestep freshness, and direct fallback. Favor larger exact producer-side cache-fill/data-movement reductions or other cross-kernel reuse changes that keep the current consumer math fixed, rather than more cached-consumer unrolling or any reciprocal-reuse rewrite.
- controller decision: Accept `bf07f28c411869f7ae2ab685092e6fe0372178f5` as the new incumbent, replacing `30584dbe2ab0c1af8b2a7a566a91b31999295371`.
#### Iteration 26: REJECTED
- hypothesis: With iteration 25 having already made exact `r12` payload compaction promotable via a four-way cached `bond_class2` producer, restoring prepared hot-angle layout/index reuse in `bond_class2` and reviving the earlier four-way cached `angle_harmonic` consumer might stack an additional exact data-movement win by removing cached-path `neighbor->anglelist` rereads while preserving the incumbent arithmetic order, structural guard, and fallback behavior.
- changed files:
  - `src/CLASS2/bond_class2.cpp`
  - `src/CLASS2/bond_class2.h`
  - `src/MOLECULE/angle_harmonic.cpp`
- benchmark outcome: Authoritative controller benchmarking completed with benchmark status `ok`, correctness passed under field tolerances, and there were no guardrail errors or hard-reject conditions. The candidate measured `weighted_median_bond_seconds=0.22115`, which is `1.10%` slower than incumbent `0.21875` even though it remains `32.22%` better than baseline `0.32627`, so it fails the objective and should not be promoted. Both cases converged cleanly: `test-32r-long` reported `bond_seconds=0.22115` and `test-64r-short` reported `bond_seconds=0.015794`; peak RSS was `157.98 MB`.
- lesson learned: The restored prepared hot-angle layout/index path does not stack with the accepted iteration-25 baseline. Once the exact `r12` cache payload and four-way cached producer are in place, reintroducing prepared hot-angle index reuse plus the four-way cached consumer is worse than leaving the simpler arithmetic-order-preserving incumbent consumer path alone. This reinforces the recent pattern from iterations 22-24: eliminating cached-path `neighbor->anglelist` rereads and widening the cached consumer are exhausted or counterproductive on top of the current producer-side baseline, even when correctness remains clean.
- next hypothesis: Keep iteration 25 as the incumbent and do not spend more iterations restoring prepared hot-angle layout/index reuse or four-way cached-consumer variants on top of the exact `r12` plus four-way producer baseline. If bonded-path work continues, look for a materially different exact producer-side cache-fill/data-movement reduction or another cross-kernel reuse change that leaves the incumbent `angle_harmonic` arithmetic order and fallback path untouched; otherwise pivot to a different hot path with clearer remaining headroom.
- controller decision: Reject `a0800c8480fc853c5cd9149642f9143186ff0855`; retain incumbent `bf07f28c411869f7ae2ab685092e6fe0372178f5` because correctness passed but the candidate regressed the primary metric by `1.10%` versus the incumbent.
### Rejected patterns
- [2026-04-12T06:53:52Z] Avoid bundling reciprocal-based angle-geometry rewrites, polynomial reshaping, and `ev_tally` bookkeeping changes across both kernels in one experiment; when correctness breaks, the source becomes hard to localize.
- [2026-04-12T06:53:52Z] Do not assume algebraic equivalence implies benchmark equivalence in this workload; low-level arithmetic reordering can materially change forces and long-run thermo observables.
- [2026-04-12T06:55:03.116086Z] Reuse per-type coefficients and reciprocals, evaluate the class2 polynomial in a lower-overhead form, reuse force components, and only materialize `ev_tally` force arrays in `bond_class2` and `angle_harmonic` specialized hot-loop instantiations. [Hard correctness failure in both benchmark cases, and the candidate was also 1.39% slower than incumbent on the primary metric.; case test-32r-long etotal relative_delta exceeds threshold (9.3752e-05 > 1e-08); case test-32r-long pe relative_delta exceeds threshold (0.0022341 > 1e-08); case test-32r-long ke relative_delta exceeds threshold (0.0041836 > 1e-08); case test-32r-long temp relative_delta exceeds threshold (0.0041836 > 1e-08); case test-32r-long press abs_delta exceeds threshold (1.38024e+08 > 1e-06); case test-32r-long force_components abs_delta exceeds threshold (0.106059 > 1e-08); case test-32r-long force_components rms_delta exceeds threshold (0.0295306 > 1e-08); case test-64r-short etotal relative_delta exceeds threshold (7.58474e-08 > 1e-08); case test-64r-short pe relative_delta exceeds threshold (4.16662e-08 > 1e-08); case test-64r-short ke relative_delta exceeds threshold (5.10999e-08 > 1e-08); case test-64r-short temp relative_delta exceeds threshold (4.97398e-08 > 1e-08); case test-64r-short press abs_delta exceeds threshold (2880 > 1e-06); case test-64r-short force_components abs_delta exceeds threshold (1.24e-05 > 1e-08); case test-64r-short force_components rms_delta exceeds threshold (1.19468e-06 > 1e-08)] (25f839bdfc42, weighted_median_bond_seconds=0.28546)
- [2026-04-12T07:31:41.978324Z] Add an `angle_harmonic` single-angle-type fast path that dispatches to `eval_one_type` and hoists `k[1]`/`theta0[1]` out of the inner loop. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 0.91% versus the incumbent, below the 2% acceptance threshold.] (e5c1ddf090fa, weighted_median_bond_seconds=0.27052)
- [2026-04-12T07:50:36Z] Bundling the correctness-safe `angle_harmonic` single-angle-type fast path with tiny `bond_class2` exact-math cleanup (`fx`/`fy`/`fz` caching) still does not reach the promotion bar; the extra gain over iteration 4 was only about `0.04%`.
- [2026-04-12T07:50:36Z] Cache per-bond force components in bond_class2 and restore the single-angle-type fast path in angle_harmonic. [Correctness passed with no guardrail errors, but the primary metric improved from 0.27301 to only 0.27041 (-0.95% vs incumbent), which is below the 2% acceptance threshold.] (224da0125669, weighted_median_bond_seconds=0.27041)
- [2026-04-12T07:52:08.695972Z] Cache per-bond force components in bond_class2 and restore the single-angle-type fast path in angle_harmonic. [Correctness passed, but weighted_median_bond_seconds improved only 0.95% versus the incumbent, below the 2% acceptance threshold.] (224da0125669, weighted_median_bond_seconds=0.27041)
- [2026-04-12T14:58:16Z] Consecutive same-`i1` run accumulation in the single-type `bond_class2` fast path is correctness-safe but still too small a lever for promotion; reducing leading-atom coordinate loads and collapsing `f[i1]` writeback to once per run improved the primary metric by only `0.58%` versus the incumbent.
- [2026-04-12T14:59:51.057432Z] Accumulate consecutive same-`i1` bonds in the single-type `bond_class2` fast path to reuse the leading atom's coordinates and force accumulator across each run. [Correctness passed, but same-`i1` bond-run accumulation improved weighted_median_bond_seconds by only 0.58% versus the incumbent, below the 2% acceptance threshold.] (e639b3949f97, weighted_median_bond_seconds=0.27144)
- [2026-04-12T15:18:42Z] Bundling the previously validated `angle_harmonic` single-type fast path with a more explicit same-`i1` two-bond block in the single-type `bond_class2` path regressed performance relative to the incumbent, so do not assume small correctness-safe wins will compose constructively.
- [2026-04-12T15:18:42Z] Restore the `angle_harmonic` single-angle-type fast path and add an explicit same-`i1` two-bond block in the single-type `bond_class2` kernel to reuse the leading atom state while preserving per-bond math and tally order. [Correctness passed, but the candidate regressed weighted_median_bond_seconds from 0.27301 to 0.27624 (+1.18% vs incumbent), so it should not replace the incumbent.] (47d88a34949485d0d2537b85da1c1bc763839085, weighted_median_bond_seconds=0.27624)
- [2026-04-12T15:21:05.980838Z] Restore the `angle_harmonic` single-angle-type fast path and add an explicit same-`i1` two-bond block in the single-type `bond_class2` kernel to reuse the leading atom state while preserving per-bond math and tally order. [Correctness passed, but iteration 7 regressed the primary metric to 0.27624, which is 1.18% slower than the incumbent 0.27301.] (47d88a349494, weighted_median_bond_seconds=0.27624)
- [2026-04-12T15:41:31Z] Restoring the correctness-safe `angle_harmonic` single-type path while tightening `EVFLAG`/`EFLAG`-guarded temporary work in the one-type bonded kernels is still only a sub-1% optimization lever; this bookkeeping cleanup does not materially shift the benchmark.
- [2026-04-12T15:41:31Z] Restore `angle_harmonic` single-angle-type dispatch and tighten one-type bonded hot paths by only materializing angle `ev_tally` arrays when `EVFLAG` is active and only computing `bond_class2` energy-only intermediates in `EFLAG` instantiations. [Correctness passed, but weighted_median_bond_seconds improved only 0.70% versus the incumbent, below the 2% acceptance threshold.] (e3c2ae9ee96a1337381551c62385cb67dccf762b, weighted_median_bond_seconds=0.27111)
- [2026-04-12T15:44:30.538581Z] Restore `angle_harmonic` single-angle-type dispatch and tighten one-type bonded hot paths by only materializing angle `ev_tally` arrays when `EVFLAG` is active and only computing `bond_class2` energy-only intermediates in `EFLAG` instantiations. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 0.70% versus incumbent 4a79c588ea08, below the 2% acceptance threshold.] (e3c2ae9ee96a, weighted_median_bond_seconds=0.27111)
- [2026-04-12T16:05:54Z] Exact two-way unrolling of the dominant one-type, no-`evflag`, Newton-on `bond_class2` path is correctness-safe and does outperform the incumbent, but it still only reaches `1.50%` improvement, so pure loop-overhead and latency-hiding tweaks appear capped below the promotion bar.
- [2026-04-12T16:05:54Z] Add a dedicated two-way unrolled `bond_class2` one-type hot path for the dominant no-`evflag`, Newton-on case while preserving exact per-bond math and update order. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 1.50% versus incumbent 4a79c588ea08, below the 2% acceptance threshold.] (6fbda0abc94b8116e90561046832a268d1d97728, weighted_median_bond_seconds=0.26892)
- [2026-04-12T16:07:51.032635Z] Add a dedicated two-way unrolled `bond_class2` one-type hot path for the dominant no-`evflag`, Newton-on case while preserving exact per-bond math and update order. [Correctness passed, but the two-way unrolled `bond_class2` hot path improved `weighted_median_bond_seconds` by only 1.50% versus the incumbent, below the 2% acceptance threshold.] (6fbda0abc94b, weighted_median_bond_seconds=0.26892)
- [2026-04-12T18:16:53Z] Do not replace the iteration-13 geometry-oriented handoff with a fully prepared per-angle force cache in `bond_class2`; shifting `angle_harmonic`'s trigonometric and force-vector preparation upstream regressed `weighted_median_bond_seconds` by `17.65%` versus the incumbent even though correctness stayed clean.
- [2026-04-12T18:16:53Z] Replace the one-type bond-to-angle geometry cache with a prepared per-angle force cache built in `bond_class2`, then consume it as a direct force-apply hot path in `angle_harmonic` while preserving the validated structural guard and direct fallback. [Correctness passed with no hard rejects, but the candidate regressed `weighted_median_bond_seconds` from 0.22529 to 0.26506 (+17.65% vs incumbent), so it should not replace the incumbent.] (0890ed81e853, weighted_median_bond_seconds=0.26506)
- [2026-04-12T18:19:08.313937Z] Replace the one-type bond-to-angle geometry cache with a prepared per-angle force cache built in `bond_class2`, then consume it as a direct force-apply hot path in `angle_harmonic` while preserving the validated structural guard and direct fallback. [Correctness passed, but moving full angle-force preparation into bond_class2 regressed weighted_median_bond_seconds by 17.65% versus incumbent 30584dbe2ab0.] (0890ed81e853, weighted_median_bond_seconds=0.26506)
- [2026-04-12T18:37:00Z] Do not compact the iteration-13 angle-oriented cache down to bond deltas plus inverse bond lengths if the `angle_harmonic` consumer must reconstruct geometry from reciprocal-derived values; this removed divisions and looked `6.51%` faster, but it broke exact thermo and force agreement badly enough for a hard reject.
- [2026-04-12T18:37:00Z] Compact the angle hot cache to bond deltas plus inverse bond lengths and consume them in the cached `angle_harmonic` hot path to remove cached-path divisions. [Hard correctness failure in both benchmark cases despite the nominal speedup; `test-32r-long` diverged at `etotal`, `pe`, `ke`, `temp`, `press`, and force-component tolerances, and `test-64r-short` also exceeded all corresponding thresholds.] (fdcd68522a83, weighted_median_bond_seconds=0.21063)
- [2026-04-12T18:40:30.515340Z] Compact the angle hot cache to bond deltas plus inverse bond lengths and consume them in the cached `angle_harmonic` hot path to remove cached-path divisions. [Hard correctness failure in both benchmark cases despite a nominal 6.51% speedup, so incumbent 30584dbe2ab0 remains.; case test-32r-long etotal relative_delta exceeds threshold (7.92579e-05 > 1e-08); case test-32r-long pe relative_delta exceeds threshold (0.00213102 > 1e-08); case test-32r-long ke relative_delta exceeds threshold (0.00400269 > 1e-08); case test-32r-long temp relative_delta exceeds threshold (0.00400269 > 1e-08); case test-32r-long press abs_delta exceeds threshold (1.39153e+08 > 1e-06); case test-32r-long force_components abs_delta exceeds threshold (0.115462 > 1e-08); case test-32r-long force_components rms_delta exceeds threshold (0.0280042 > 1e-08); case test-64r-short etotal relative_delta exceeds threshold (6.84866e-08 > 1e-08); case test-64r-short pe relative_delta exceeds threshold (3.14967e-08 > 1e-08); case test-64r-short ke relative_delta exceeds threshold (6.84812e-08 > 1e-08); case test-64r-short temp relative_delta exceeds threshold (6.63765e-08 > 1e-08); case test-64r-short press abs_delta exceeds threshold (2360 > 1e-06); case test-64r-short force_components abs_delta exceeds threshold (1.22e-05 > 1e-08); case test-64r-short force_components rms_delta exceeds threshold (1.13192e-06 > 1e-08)] (fdcd68522a83, weighted_median_bond_seconds=0.21063)
- [2026-04-12T18:58:11Z] Do not shrink the angle-oriented cache by dropping cached `rsq` fields alone; recomputing `rsq` from cached deltas preserves correctness when exact bond lengths remain stored, but it still regresses `weighted_median_bond_seconds` by `3.18%` versus the incumbent.
- [2026-04-12T18:58:11Z] Compact the angle hot cache to bond deltas plus exact bond lengths, recomputing cached-path `rsq` values in `angle_harmonic` after dropping cached `rsq` fields. [Correctness passed, but the candidate regressed `weighted_median_bond_seconds` from 0.22529 to 0.23245 (+3.18% vs incumbent), so it should not replace the incumbent.] (1f058192fe33418582791fc8083eb481c5321b50, weighted_median_bond_seconds=0.23245)
- [2026-04-12T19:02:54.047911Z] Compact the angle hot cache to bond deltas plus exact bond lengths, recomputing cached-path rsq values in angle_harmonic after dropping cached rsq fields. [Correctness recovered, but dropping cached rsq fields and recomputing them in angle_harmonic regressed weighted_median_bond_seconds by 3.18% versus the incumbent.] (1f058192fe33, weighted_median_bond_seconds=0.23245)
- [2026-04-12T19:22:24Z] Precomputing the one-type bond/angle pairing layout once per topology build is correctness-safe and does improve the bonded metric slightly, but validation-hoist-only work inside `bond_class2`'s angle-cache fill is only a `0.92%` lever on top of incumbent `30584dbe2ab0`, so it is below the promotion bar by itself.
- [2026-04-12T19:22:24Z] Precompute the one-type bond/angle pairing layout once per topology build and reuse it in bond_class2's hot angle-cache fill to remove repeated per-step topology validation while preserving the incumbent cache payload and angle_harmonic consumer math. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 0.92% versus incumbent 30584dbe2ab0, below the 2% acceptance threshold.] (738ffb9e3a30, weighted_median_bond_seconds=0.22321)
- [2026-04-12T19:25:25.730712Z] Precompute the one-type bond/angle pairing layout once per topology build and reuse it in bond_class2's hot angle-cache fill to remove repeated per-step topology validation while preserving the incumbent cache payload and angle_harmonic consumer math [Correctness passed, but weighted_median_bond_seconds improved only 0.92% versus incumbent 30584dbe2ab0, below the 2% promotion threshold.] (738ffb9e3a30, weighted_median_bond_seconds=0.22321)
- [2026-04-12T19:54:13Z] Do not spend more standalone iterations on producer-side bond/angle orientation metadata preparation inside `bond_class2`; even after replacing per-step orientation checks with a reused prevalidated layout, the gain stays at only `0.83%` versus incumbent `30584dbe2ab0`, below the promotion bar.
- [2026-04-12T19:55:44.765755Z] Prepare and reuse a prevalidated one-type bond-angle orientation layout in `bond_class2` hot cache fill while keeping the incumbent angle cache payload and `angle_harmonic` math unchanged [Correctness passed, but `weighted_median_bond_seconds` improved only 0.83% versus incumbent `30584dbe2ab0`, below the 2% promotion threshold.] (bed99cfa6486, weighted_median_bond_seconds=0.22343)
- [2026-04-12T20:22:55Z] Do not spend standalone iterations on exact `r`-payload reshaping inside the angle-oriented cache; replacing separate cached `r1`/`r2` with exact `r12` is correctness-safe and faster than the recent layout-hoist variants, but its `1.79%` gain is still below the promotion threshold.
- [2026-04-12T20:24:55.920504Z] Compact the cached angle hot entry to store the exact bond-length product `r12 = r1*r2` instead of separate `r1`/`r2`, preserving cached `del*` and `rsq` semantics while reusing that exact product in the cached `angle_harmonic` hot path. [Correctness passed, but weighted_median_bond_seconds improved only 1.79% versus incumbent 30584dbe2ab0, below the 2% promotion threshold.] (acd350270fd3, weighted_median_bond_seconds=0.22126)
- [2026-04-12T20:44:02Z] Do not expect prevalidated producer-side orientation-layout reuse to stack materially with exact `r12` cache compaction on top of the current angle-oriented cache; the combined candidate stayed below threshold and was slightly slower than the `r12`-only variant from iteration 19.
- [2026-04-12T20:46:46.028779Z] Combine prevalidated bond-angle orientation layout reuse in `bond_class2` with exact `r12` hot-cache compaction consumed by `angle_harmonic`. [Correctness passed, but the candidate improved weighted_median_bond_seconds by only 1.54% versus the incumbent, below the 2% promotion threshold.] (c0a62a11cfaf, weighted_median_bond_seconds=0.22182)
- [2026-04-12T21:06:26Z] Do not assume exact cached `r12` makes reciprocal reuse safe in the cached `angle_harmonic` path; replacing the original duplicated divisions with a shared `inv_r12` changed floating-point evaluation enough to trigger hard correctness failures in both benchmark cases.
- [2026-04-12T21:06:26Z] Store exact `r12 = r1*r2` in the hot angle cache and reuse a shared local `inv_r12` in the cached `angle_harmonic` hot path to remove the duplicate division by the same bond-length product. [Hard correctness failure in both benchmark cases despite a nominal 2.95% speedup; `test-32r-long` diverged at `etotal`, `pe`, `ke`, `temp`, `press`, and force-component tolerances, and `test-64r-short` also exceeded all corresponding thresholds.] (e4103f7dab04, weighted_median_bond_seconds=0.21864)
- [2026-04-12T21:08:23.118526Z] Store exact `r12 = r1*r2` in the hot angle cache and reuse a shared local `inv_r12` in the cached `angle_harmonic` hot path to remove the duplicate division by the same bond-length product. [Hard correctness failure in both benchmark cases from shared `inv_r12` reuse despite a nominal 2.95% speedup, so incumbent `30584dbe2ab0` remains.; case test-32r-long etotal relative_delta exceeds threshold (7.66767e-05 > 1e-08); case test-32r-long pe relative_delta exceeds threshold (0.00184228 > 1e-08); case test-32r-long ke relative_delta exceeds threshold (0.00346259 > 1e-08); case test-32r-long temp relative_delta exceeds threshold (0.00346259 > 1e-08); case test-32r-long press abs_delta exceeds threshold (1.29238e+08 > 1e-06); case test-32r-long force_components abs_delta exceeds threshold (0.100389 > 1e-08); case test-32r-long force_components rms_delta exceeds threshold (0.0278753 > 1e-08); case test-64r-short etotal relative_delta exceeds threshold (7.21825e-08 > 1e-08); case test-64r-short pe relative_delta exceeds threshold (6.86454e-08 > 1e-08); case test-64r-short ke relative_delta exceeds threshold (1.44472e-07 > 1e-08); case test-64r-short temp relative_delta exceeds threshold (1.46005e-07 > 1e-08); case test-64r-short press abs_delta exceeds threshold (1130 > 1e-06); case test-64r-short force_components abs_delta exceeds threshold (1e-05 > 1e-08); case test-64r-short force_components rms_delta exceeds threshold (1.15835e-06 > 1e-08)] (e4103f7dab04, weighted_median_bond_seconds=0.21864)
- [2026-04-12T21:28:04Z] Do not expect prepared hot-angle index/layout metadata and cached-index consumption to stack materially with exact `r12` cache compaction; eliminating cached-path `neighbor->anglelist` rereads preserved correctness but still improved the primary metric by only `1.40%` versus the incumbent and was slightly slower than the simpler `r12`-only iteration 19.
- [2026-04-12T21:28:04.016355Z] Compact the one-type hot angle cache to exact `r12 = r1*r2` and add prepared hot-angle index/layout metadata so `bond_class2` reuses stable topology preparation and cached `angle_harmonic` stops rereading `neighbor->anglelist` while preserving the incumbent arithmetic order. [Correctness passed, but the candidate improved `weighted_median_bond_seconds` by only `1.40%` versus incumbent `30584dbe2ab0`, below the `2%` promotion threshold.] (efcb4c8f585c, weighted_median_bond_seconds=0.22213)
- [2026-04-12T21:31:56.364653Z] Compact the one-type hot angle cache to exact `r12 = r1*r2` and add prepared hot-angle index/layout metadata so `bond_class2` reuses stable topology preparation and cached `angle_harmonic` stops rereading `neighbor->anglelist` while preserving the incumbent arithmetic order. [Correctness passed, but the candidate’s 0.22213 weighted_median_bond_seconds is only a 1.40% improvement over incumbent 30584dbe2ab0, below the 2% promotion threshold.] (efcb4c8f585c, weighted_median_bond_seconds=0.22213)
- [2026-04-12T21:54:08.992772Z] Do not expect pure four-way exact unrolling of the cached `angle_harmonic` consumer to clear promotion on top of the current angle-oriented cache baseline; it preserved correctness but improved the primary metric by only `1.48%` versus the incumbent and still trailed the simpler exact-`r12` iteration-19 result.
- [2026-04-12T21:54:08.992772Z] Widen the cached `angle_harmonic` hot consumer to four-way exact unrolling while preserving the incumbent cache payload, cached-path arithmetic order, guard, and direct fallback. [Correctness passed, but the candidate improved `weighted_median_bond_seconds` by only `1.48%` versus incumbent `30584dbe2ab0`, below the `2%` promotion threshold.] (567a79cfb769, weighted_median_bond_seconds=0.22195)
- [2026-04-12T21:57:30.410463Z] Widen the cached `angle_harmonic` hot consumer to four-way exact unrolling while preserving the incumbent cache payload, cached-path arithmetic order, guard, and direct fallback. [Correctness passed, but the four-way cached `angle_harmonic` unroll improved `weighted_median_bond_seconds` by only 1.48% versus incumbent, below the 2% promotion threshold.] (567a79cfb769, weighted_median_bond_seconds=0.22195)
- [2026-04-12T22:15:36Z] Do not expect exact `r12` cache compaction to stack materially with four-way exact cached-consumer unrolling on top of the current angle-oriented cache baseline; the combined candidate preserved correctness but still improved the primary metric by only `1.65%` versus the incumbent and remained slower than the simpler iteration-19 `r12`-only variant.
- [2026-04-12T22:15:36Z] Combine exact `r12` hot-angle cache compaction in `bond_class2` with a four-way cached `angle_harmonic` consumer while preserving the incumbent guard and cached-path division semantics. [Correctness passed, but the candidate’s `0.22158` weighted_median_bond_seconds is only a `1.65%` improvement over incumbent `30584dbe2ab0`, below the `2%` promotion threshold.] (2c69753dfc48, weighted_median_bond_seconds=0.22158)
- [2026-04-12T22:17:11.208711Z] Combine exact `r12` hot-angle cache compaction in `bond_class2` with a four-way cached `angle_harmonic` consumer while preserving the incumbent guard and cached-path division semantics [Correctness passed, but 0.22158 weighted_median_bond_seconds is only a 1.65% improvement over incumbent 0.22529, below the 2% promotion threshold.] (2c69753dfc48, weighted_median_bond_seconds=0.22158)
- [2026-04-12T23:04:05.175316Z] Restore prepared hot-angle layout/index reuse in bond_class2 and a four-way cached angle_harmonic consumer while keeping the incumbent exact r12 cache payload and four-way bond producer [Correctness passed, but weighted_median_bond_seconds regressed from 0.21875 to 0.22115 versus incumbent bf07f28c4118, so the candidate should not replace the incumbent.] (a0800c8480fc, weighted_median_bond_seconds=0.22115)
### Open hypotheses
- Treat the angle-oriented `bond_class2` cache with exact cached `r12`, the four-way exact one-type cached producer, and the arithmetic-order-preserving `angle_harmonic` consumer plus direct fallback as the new bonded hot-path baseline.
- Iteration 13 shows that shared-state layout should be optimized for the consuming kernel, not just the producing one; consumer-oriented cross-kernel reuse beat the earlier bond-oriented reuse even though both were correctness-safe.
- Iteration 14 shows that this should stop at a geometry-oriented or otherwise lightweight handoff; moving full per-angle force preparation into `bond_class2` bloats the bond timer and is not a viable follow-up on top of the current incumbent.
- Iteration 15 shows that cache-layout reductions must also preserve the incumbent consumer's exact arithmetic inputs; replacing stored geometry with reciprocal-derived reconstruction is not correctness-safe even when the structural guard and fallback stay in place.
- Iteration 16 shows that even correctness-safe cache compaction has to preserve the consumer's directly used hot-path inputs in precomputed form; dropping cached `rsq` and rebuilding it in `angle_harmonic` costs more than the smaller entry size saves.
- Iteration 17 shows that hoisting one-type bond/angle layout validation to topology-build time is correctness-safe but still sub-threshold on its own; future work on the current cache should pair preserved geometry semantics with a larger consumer- or access-path reduction than validation hoisting alone.
- Iteration 18 reinforces that producer-side orientation-layout preparation is also exhausted as a standalone lever; any further work on the angle-oriented cache should target larger consumer-side or data-movement reductions while preserving exact cached `del*`, `rsq`, and `r` semantics.
- Iteration 19 shows that exact `r12` payload compaction is correctness-safe and near-threshold on its own; iteration 25 then shows it becomes promotable when paired with a materially larger exact producer-side reduction, so future work on this path should keep exact `r12` semantics and look for stronger producer-side or cache-fill/data-movement companions rather than field-substitution-only edits.
- Iteration 20 shows that combining producer-side orientation-layout reuse with exact `r12` compaction still does not clear promotion and is slightly worse than the `r12`-only variant, so prepared topology metadata is not the producer-side companion that makes `r12` compaction pay off.
- Iteration 21 shows that even exact `r12` cache payloads are not enough if the cached `angle_harmonic` consumer changes arithmetic grouping; shared reciprocal reuse recreates the earlier hard-correctness failure mode, so future work on this path must preserve the incumbent division/order semantics exactly.
- Iteration 22 shows that even with preserved arithmetic order, replacing cached-path `neighbor->anglelist` rereads with prepared hot-angle index/layout metadata does not add enough on top of exact `r12` compaction and is slightly worse than the simpler iteration-19 variant; this micro-optimization family looks exhausted unless paired with a materially larger consumer-side data-movement reduction.
- Iteration 23 shows that even correctness-safe four-way exact unrolling of the cached `angle_harmonic` consumer is still sub-threshold and remains slower than the simpler exact-`r12` compaction from iteration 19, so pure cached-consumer instruction-scheduling tweaks beyond the incumbent two-way path are weaker than producer-side reductions that keep consumer math fixed.
- Iteration 24 shows that stacking exact `r12` compaction with four-way cached-consumer unrolling still reaches only `1.65%` improvement and remains slower than the simpler iteration-19 `r12`-only variant, so the winning stacking direction for this cache is producer-side unrolling/cache-fill reduction rather than wider cached-consumer unrolling.
- Iteration 25 shows that exact `r12` compaction plus four-way exact unrolling in the one-type cached `bond_class2` producer clears promotion while preserving the incumbent `angle_harmonic` arithmetic order and fallback guard, so the current follow-up space should prioritize exact producer-side or cache-fill/data-movement reductions on top of this baseline before revisiting consumer-side tweaks.
- Iteration 26 shows that restoring prepared hot-angle layout/index reuse and the earlier four-way cached `angle_harmonic` consumer is not a viable follow-up on top of iteration 25; eliminating cached-path `neighbor->anglelist` rereads and reintroducing that cached-consumer variant regresses the primary metric versus the new incumbent despite preserving correctness.
- Any future reuse scheme must preserve explicit structural validation, timestep freshness, and a direct fallback path. Do not assume bondlist/anglelist alignment or local ordering without a correctness-preserving escape hatch.
- Future `bond_class2`-only follow-ups should still assume that shaving only leading-atom traffic or small metadata/validation work is sub-threshold on its own relative to the new incumbent; the successful producer-side changes so far are the ones that materially cut exact hot-loop or cache-fill work.
- If bonded work is revisited again, prioritize only exact reuse or cache-layout reductions that keep the current cached geometry semantics intact, including precomputed `rsq` and exact `r12`, while lowering producer-side cache-fill/data-movement cost without changing `angle_harmonic` arithmetic order; otherwise consider a different benchmark hot path with clearer remaining headroom.
- Keep experiments narrow and benchmark-backed: iterations 10-13 show accepted wins now come from composing validated changes across kernels or exploiting deeper exact structure in the regular water topology while retaining correctness guards.
