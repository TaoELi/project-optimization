# FermiLink Optimize Memory

- package_id: lammps
- benchmark_id: lammps-tip4p-long-nve-pair-kspace
- benchmark_path: .fermilink-optimize/benchmark.worker.yaml
- optimize_branch: fermilink-optimize/lammps-tip4p
- started_at_utc: 2026-04-12T05:47:08.827512Z
- last_updated_utc: 2026-04-13T07:00:13.519469Z

## Short-Term Memory (Operational)
### Current objective
- Improve benchmark performance while preserving correctness.
### Latest accepted commit
- commit: 5840dac0d9f1
- primary_metric: weighted_median_pair_plus_kspace_seconds=70.984
- status: accepted
### Progress log

- [2026-04-12T05:55:29.326337Z] baseline: baseline (e7c0ed95a333, weighted_median_pair_plus_kspace_seconds=94.717)
- [2026-04-12T06:23:25Z] iteration 1 review: rejected candidate 0157b0571dbb (weighted_median_pair_plus_kspace_seconds=94.369, correctness OK, both cases converged) because the 0.37% improvement versus the incumbent missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T06:24:25.073489Z] rejected: Cache PPPM TIP4P virtual-site reconstruction and H-index lookup once per local oxygen per compute, reusing it across particle_map, make_rho, fieldforce, per-atom, and slab correction [Correctness passed, but 94.369 is only a 0.37% improvement over 94.717 and misses the benchmark contract's 2% minimum improvement threshold.] (0157b0571dbb, weighted_median_pair_plus_kspace_seconds=94.369)
- [2026-04-12T08:02:01Z] iteration 2 review: rejected candidate 2bbd33a175f (weighted_median_pair_plus_kspace_seconds=82.844, both cases converged, 12.54% faster than the incumbent) because it triggered a hard correctness failure with large force mismatches in both cases and broad thermo divergence in `test-32r-long`.
- [2026-04-12T08:02:01Z] rejected: Backport the serial OPT hot loop into base lj/cut/tip4p/long so the benchmarked pair kernel uses branch-specialized eval paths and local f[i] accumulation [Hard reject: correctness failure despite 12.54% speedup; `test-32r-long` failed etotal/pe/ke/temp/press tolerances and both cases failed `forces_xyz` tolerances.] (2bbd33a175f, weighted_median_pair_plus_kspace_seconds=82.844)
- [2026-04-12T08:02:55.766146Z] correctness_failure: Backport the serial OPT hot loop into base lj/cut/tip4p/long so the benchmarked pair kernel uses branch-specialized eval paths and local f[i] accumulation. [Hard reject: despite a 12.54% speedup, candidate 2bbd33a175f failed TIP4P correctness with large force mismatches in both cases and broad thermo divergence in test-32r-long, so the baseline incumbent remains.; case test-32r-long etotal_series relative_delta exceeds threshold (8.92069e-05 > 1e-06); case test-32r-long pe_series relative_delta exceeds threshold (0.00218177 > 1e-06); case test-32r-long ke_series relative_delta exceeds threshold (0.00408985 > 1e-06); case test-32r-long temp_series relative_delta exceeds threshold (0.00408985 > 1e-06); case test-32r-long press_series relative_delta exceeds threshold (0.200262 > 1e-05); case test-32r-long forces_xyz abs_delta exceeds threshold (0.107617 > 1e-08); case test-32r-long forces_xyz rms_delta exceeds threshold (0.0276885 > 1e-08); case test-64r-short forces_xyz abs_delta exceeds threshold (1.44e-05 > 1e-08); case test-64r-short forces_xyz rms_delta exceeds threshold (1.44068e-06 > 1e-08)] (2bbd33a175f2, weighted_median_pair_plus_kspace_seconds=82.844)
- [2026-04-12T08:33:03Z] iteration 3 review: accepted candidate 3a45586c8afa (weighted_median_pair_plus_kspace_seconds=81.885, both cases converged, correctness OK) because it reduced the incumbent metric by 13.55%, comfortably clearing the 2% minimum improvement threshold without guardrail errors.
- [2026-04-12T08:33:03Z] accepted: Specialize `pair_lj_cut_tip4p_long` with compile-time energy/virial/table dispatch, a shared TIP4P site-cache helper, and separate oxygen/non-oxygen `i` hot paths while preserving the original force writes and `ev_tally_tip4p()` semantics. [Correctness passed with no errors; `weighted_median_pair_plus_kspace_seconds` improved from 94.717 to 81.885, so this candidate becomes the new incumbent.] (3a45586c8afa, weighted_median_pair_plus_kspace_seconds=81.885)
- [2026-04-12T08:33:49.996752Z] accepted: Specialize `pair_lj_cut_tip4p_long` with compile-time energy/virial/table dispatch, a shared TIP4P site-cache helper, and separate oxygen/non-oxygen `i` hot paths while preserving the original force writes and `ev_tally_tip4p()` semantics. [Correctness passed with no guardrail errors, and the candidate improves the primary metric from 94.717 to 81.885, a 13.55% gain that clears the 2% acceptance bar.] (3a45586c8afa, weighted_median_pair_plus_kspace_seconds=81.885)
- [2026-04-12T08:59:11Z] iteration 4 review: rejected candidate 847eb8e5dd4b (weighted_median_pair_plus_kspace_seconds=81.985, both cases converged, correctness OK) because it regressed the incumbent by 0.12% and therefore missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T08:59:11Z] rejected: Cache local PPPM TIP4P M-site reconstruction and 1D rho stencils in `particle_map()` so `make_rho()`, fieldforce, per-atom, and slab correction reuse cached state instead of recomputing `find_M()`/`compute_rho1d()`. [Correctness passed with no guardrail errors, but `weighted_median_pair_plus_kspace_seconds` regressed from 81.885 to 81.985 versus the incumbent, so this candidate cannot replace it.] (847eb8e5dd4b, weighted_median_pair_plus_kspace_seconds=81.985)
- [2026-04-12T09:00:43.453614Z] rejected: Cache local PPPM TIP4P M-site reconstruction and 1D rho stencils in particle_map() so make_rho(), fieldforce, per-atom, and slab correction reuse the cached state instead of recomputing find_M()/compute_rho1d() [Correctness passed, but 81.985 is 0.12% slower than incumbent 81.885 and misses the 2% improvement bar.] (847eb8e5dd4b, weighted_median_pair_plus_kspace_seconds=81.985)
- [2026-04-12T09:24:24Z] iteration 5 review: rejected candidate 42d987d04a9c (weighted_median_pair_plus_kspace_seconds=81.298, correctness OK, both cases converged) because the 0.72% improvement versus incumbent 3a45586c8afa missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T09:24:24Z] rejected: Skip the extended TIP4P Coulomb precheck for exact non-oxygen/non-oxygen pairs in `pair_lj_cut_tip4p_long`, while keeping the incumbent oxygen/M-site path unchanged for any oxygen-containing interaction. [Correctness passed with no guardrail errors, and `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 81.298, but that 0.72% gain is below the 2% acceptance bar so the incumbent remains.] (42d987d04a9c, weighted_median_pair_plus_kspace_seconds=81.298)
- [2026-04-12T09:26:22.671744Z] rejected: Skip the extended TIP4P Coulomb precheck for exact non-oxygen/non-oxygen pairs in `pair_lj_cut_tip4p_long`, while keeping the incumbent oxygen/M-site path unchanged for any oxygen-containing interaction. [Correctness passed, but 81.298 is only a 0.72% improvement over incumbent 81.885 and misses the benchmark contract's 2% minimum improvement bar.] (42d987d04a9c, weighted_median_pair_plus_kspace_seconds=81.298)
- [2026-04-12T09:52:01Z] iteration 6 review: rejected candidate 89aa52c5901e (weighted_median_pair_plus_kspace_seconds=79.639, both cases converged, 2.74% faster than incumbent 3a45586c8afa) because it triggered a hard correctness failure with broad `test-32r-long` thermo drift and force mismatches plus `forces_xyz` mismatches in both benchmark cases.
- [2026-04-12T09:52:01Z] correctness_failure: Specialize `pair_lj_cut_tip4p_long` for the benchmark's global-only tally path, fast-path exact non-water/non-water Coulomb pairs, and locally accumulate non-water `f[i]` updates while preserving the incumbent TIP4P oxygen/M-site force distribution. [Hard reject: despite improving `weighted_median_pair_plus_kspace_seconds` from 81.885 to 79.639, `test-32r-long` failed `etotal_series`, `pe_series`, `ke_series`, `temp_series`, `press_series`, and `forces_xyz`, while both cases failed `forces_xyz`; incumbent `3a45586c8afa` remains.] (89aa52c5901e, weighted_median_pair_plus_kspace_seconds=79.639)
- [2026-04-12T09:53:32.781249Z] correctness_failure: Specialize `pair_lj_cut_tip4p_long` for the benchmark’s global-only tally path, fast-path exact non-water/non-water Coulomb pairs, and locally accumulate non-water `f[i]` updates while preserving the incumbent TIP4P oxygen/M-site force distribution [Hard reject: candidate 89aa52c5901e is 2.74% faster than incumbent 3a45586c8afa but fails benchmark correctness, with broad test-32r-long thermo divergence and forces_xyz mismatches in both cases.; case test-32r-long etotal_series relative_delta exceeds threshold (8.42997e-05 > 1e-06); case test-32r-long pe_series relative_delta exceeds threshold (0.00183969 > 1e-06); case test-32r-long ke_series relative_delta exceeds threshold (0.00345568 > 1e-06); case test-32r-long temp_series relative_delta exceeds threshold (0.00345568 > 1e-06); case test-32r-long press_series relative_delta exceeds threshold (0.222002 > 1e-05); case test-32r-long forces_xyz abs_delta exceeds threshold (0.105529 > 1e-08); case test-32r-long forces_xyz rms_delta exceeds threshold (0.0282935 > 1e-08); case test-64r-short forces_xyz abs_delta exceeds threshold (8.3e-06 > 1e-08); case test-64r-short forces_xyz rms_delta exceeds threshold (1.10787e-06 > 1e-08)] (89aa52c5901e, weighted_median_pair_plus_kspace_seconds=79.639)
- [2026-04-12T10:22:36Z] iteration 7 review: rejected candidate c37498ebee61 (weighted_median_pair_plus_kspace_seconds=81.445, correctness OK, both cases converged) because the 0.54% improvement versus incumbent 3a45586c8afa missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T10:22:36Z] rejected: Keep the exact H-H Coulomb cutoff fast path in `pair_lj_cut_tip4p_long`, and reuse the first oxygen `find_M()` result in `PPPMTIP4P::fieldforce_ik()` / `fieldforce_ad()` while hoisting constant alpha/slab factors out of the inner oxygen force-distribution branch. [Correctness passed with no guardrail errors, and `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 81.445, but that 0.54% gain is below the 2% acceptance bar so the incumbent remains.] (c37498ebee61, weighted_median_pair_plus_kspace_seconds=81.445)
- [2026-04-12T10:24:43.678950Z] rejected: Keep the exact H-H Coulomb cutoff fast path in `pair_lj_cut_tip4p_long`, and reuse the first oxygen `find_M()` result in `PPPMTIP4P::fieldforce_ik()` / `fieldforce_ad()` while hoisting constant alpha/slab factors out of the inner oxygen force-distribution branch [Correctness passed, but candidate c37498ebee61 improves the incumbent by only 0.54% (81.885 to 81.445), missing the 2% acceptance bar.] (c37498ebee61, weighted_median_pair_plus_kspace_seconds=81.445)
- [2026-04-12T10:42:25Z] iteration 8 review: rejected candidate 7d859a5521df (weighted_median_pair_plus_kspace_seconds=85.841, correctness OK, both cases converged) because it regressed the incumbent by 4.83% and therefore missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T10:42:25Z] rejected: Specialize `pair_lj_cut_tip4p_long` Coulomb handling by interaction class (`O-O`, `O-H`, `H-O`, and exact non-oxygen/non-oxygen) via a templated helper, preserving incumbent TIP4P force distribution and `ev_tally_tip4p()` semantics while retaining the safe exact non-water cutoff pruning. [Correctness passed with no guardrail errors, but `weighted_median_pair_plus_kspace_seconds` regressed from 81.885 to 85.841 versus the incumbent, so this candidate cannot replace it.] (7d859a5521df, weighted_median_pair_plus_kspace_seconds=85.841)
- [2026-04-12T10:44:10.582913Z] rejected: Specialize `pair_lj_cut_tip4p_long` Coulomb handling by interaction class (`O-O`, `O-H`, `H-O`, and exact non-oxygen/non-oxygen) via a templated helper, preserving incumbent TIP4P force distribution and `ev_tally_tip4p()` semantics while retaining the safe exact non-water cutoff pruning. [Correctness passed, but candidate 7d859a5521df regressed the incumbent primary metric from 81.885 to 85.841, missing the 2% improvement bar.] (7d859a5521df, weighted_median_pair_plus_kspace_seconds=85.841)
- [2026-04-12T11:05:20.024383403Z] iteration 9 review: rejected candidate 0356a6e5d35a (weighted_median_pair_plus_kspace_seconds=80.674, correctness OK, both cases converged) because the 1.48% improvement versus incumbent 3a45586c8afa still missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T11:05:20.024383403Z] rejected: Tighten `pair_lj_cut_tip4p_long` Coulomb prechecks only for exact `H-H` and actual-current-step `O(i)-H(j)` reach tests, while keeping all oxygen-`j` paths plus the incumbent TIP4P force/tally write order unchanged. [Correctness passed with no guardrail errors, and `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 80.674, but that 1.48% gain is below the 2% acceptance bar so the incumbent remains.] (0356a6e5d35a, weighted_median_pair_plus_kspace_seconds=80.674)
- [2026-04-12T11:09:23.683192Z] rejected: Tighten `pair_lj_cut_tip4p_long` Coulomb prechecks only for exact `H-H` and actual-current-step `O(i)-H(j)` reach tests, while keeping all oxygen-`j` paths plus the incumbent TIP4P force/tally write order unchanged. [Correctness passed, but 80.674 is only a 1.48% improvement over incumbent 81.885 and misses the 2% acceptance bar.] (0356a6e5d35a, weighted_median_pair_plus_kspace_seconds=80.674)
- [2026-04-12T11:24:59Z] iteration 10 review: rejected candidate 25819367f233 (weighted_median_pair_plus_kspace_seconds=80.914, correctness OK, both cases converged) because the 1.19% improvement versus incumbent 3a45586c8afa missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T11:24:59Z] rejected: Tighten exact oxygen-i/non-oxygen-j pair Coulomb prechecks in `pair_lj_cut_tip4p_long` and reuse the first PPPM TIP4P oxygen M-site mapping plus hoisted alpha/slab constants in `fieldforce_ik()` and `fieldforce_ad()`. [Correctness passed with no guardrail errors, and `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 80.914, but that 1.19% gain is below the 2% acceptance bar so the incumbent remains.] (25819367f233, weighted_median_pair_plus_kspace_seconds=80.914)
- [2026-04-12T11:27:49.530814Z] rejected: Tighten exact oxygen-i/non-oxygen-j pair Coulomb prechecks in `pair_lj_cut_tip4p_long` and reuse the first PPPM TIP4P oxygen M-site mapping plus hoisted alpha/slab constants in `fieldforce_ik()` and `fieldforce_ad()` [Correctness passed, but 80.914 is only a 1.19% improvement over incumbent 81.885, below the 2% acceptance bar.] (25819367f233, weighted_median_pair_plus_kspace_seconds=80.914)
- [2026-04-12T11:51:34Z] iteration 11 review: accepted candidate 2a0b902a8b5a (weighted_median_pair_plus_kspace_seconds=79.88, correctness OK, both cases converged) because it improved incumbent 3a45586c8afa by 2.45%, clearing the contract's 2% minimum relative improvement bar without guardrail errors.
- [2026-04-12T11:51:34Z] accepted: Tighten exact oxygen-to-nonoxygen Coulomb prechecks and add a dedicated water-H `i` fast path in `pair_lj_cut_tip4p_long` that skips impossible LJ work while preserving the incumbent TIP4P force/tally order. [Correctness passed with no guardrail errors, and `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 79.88, so this candidate becomes the new incumbent.] (2a0b902a8b5a, weighted_median_pair_plus_kspace_seconds=79.88)
- [2026-04-12T11:54:12.717864Z] accepted: Tighten exact oxygen-to-nonoxygen Coulomb prechecks and add a dedicated water-H `i` fast path in `pair_lj_cut_tip4p_long` that skips impossible LJ work while preserving the incumbent TIP4P force/tally order. [Correctness passed and 79.88 beats incumbent 81.885 by 2.45%, clearing the 2% acceptance bar.] (2a0b902a8b5a, weighted_median_pair_plus_kspace_seconds=79.88)
- [2026-04-12T12:14:55Z] iteration 12 review: rejected candidate 4f3910700624 (weighted_median_pair_plus_kspace_seconds=82.549, correctness OK, both cases converged) because it regressed incumbent 2a0b902a8b5a by 3.34% and missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T12:14:55Z] rejected: Prune impossible hydrogen-neighbor LJ work and reuse cached oxygen M-sites to tighten reverse nonoxygen-to-oxygen TIP4P Coulomb prechecks in `pair_lj_cut_tip4p_long`. [Correctness passed with no guardrail errors, but `weighted_median_pair_plus_kspace_seconds` regressed from 79.88 to 82.549 versus the incumbent, so this candidate cannot replace it.] (4f3910700624, weighted_median_pair_plus_kspace_seconds=82.549)
- [2026-04-12T12:16:29.265704Z] rejected: Prune impossible hydrogen-neighbor LJ work and reuse cached oxygen M-sites to tighten reverse nonoxygen-to-oxygen TIP4P Coulomb prechecks in pair_lj_cut_tip4p_long [Correctness passed, but candidate 4f3910700624 regressed weighted_median_pair_plus_kspace_seconds from 79.88 to 82.549 versus incumbent 2a0b902a8b5a.] (4f3910700624, weighted_median_pair_plus_kspace_seconds=82.549)
- [2026-04-12T12:39:39.653840Z] rejected: Skip dead water-H LJ work in `pair_lj_cut_tip4p_long` and cache PPPM TIP4P oxygen `find_M()` / H-index results across `particle_map()`, `make_rho()`, and `fieldforce_ik()`. [Correctness passed, but 78.33 improves incumbent 79.88 by only 1.94%, missing the benchmark contract’s 2% acceptance bar.] (c467f241b716, weighted_median_pair_plus_kspace_seconds=78.33)
- [2026-04-12T12:58:58.392601Z] rejected: Skip dead hydrogen-neighbor LJ work in `pair_lj_cut_tip4p_long` and replace the heavier PPPM full oxygen-site cache with lightweight per-step cached H indices plus on-demand M-site reconstruction in `particle_map()`, `make_rho()`, and the TIP4P fieldforce paths. [Correctness passed, but candidate b11c640621e48ee223768704bd744c8e8cb29952 improves the incumbent by only 1.68% (79.88 to 78.535), below the benchmark contract’s 2% acceptance bar.] (b11c640621e4, weighted_median_pair_plus_kspace_seconds=78.535)
- [2026-04-12T13:20:29.771114Z] rejected: Add an IK-only PPPM TIP4P oxygen cache that stores per-step H indices, lower-left grid deltas, and rho1d stencil weights in `particle_map()` and reuses them in `make_rho()` and `fieldforce_ik()` while leaving pair, AD, per-atom, and slab paths unchanged. [Correct and slightly faster (79.58 vs 79.88), but the 0.38% gain misses the benchmark contract’s 2% acceptance bar.] (48e378f90ae8, weighted_median_pair_plus_kspace_seconds=79.58)
- [2026-04-12T13:42:30.493425Z] accepted: Restore safe hydrogen-neighbor LJ pruning in `pair_lj_cut_tip4p_long` and reuse cached TIP4P oxygen M/H state across PPPM `particle_map()`/`make_rho()`/`fieldforce_ik()` with `memset` density zeroing and linear IK brick indexing [Correctness passed and 78.12 beats incumbent 79.88 by 2.20%, clearing the 2% acceptance bar.] (ab55ea3e8508, weighted_median_pair_plus_kspace_seconds=78.12)
- [2026-04-12T14:09:33Z] iteration 17 review: rejected candidate `ad7811dc14f3` (weighted_median_pair_plus_kspace_seconds=77.512, correctness OK, both cases converged) because the 0.78% improvement versus incumbent `ab55ea3e8508` missed the contract's 2% minimum relative improvement bar, even though it set the best correctness-preserving raw metric so far.
- [2026-04-12T14:11:05.492168Z] rejected: Generation-stamped TIP4P pair/PPPM caches plus a common non-special pair fast path and cross-step PPPM oxygen-H mapping reuse between reneighbor steps [Correctness passed, but 77.512 improves incumbent 78.12 by only 0.78%, below the benchmark contract’s 2% acceptance bar.] (ad7811dc14f3, weighted_median_pair_plus_kspace_seconds=77.512)
- [2026-04-12T14:29:49.534368Z] rejected: Pair-only TIP4P optimization: replace the per-step `hneigh[][2]` reset with generation-stamped cached M-site bookkeeping and add a common non-special-neighbor fast path in `pair_lj_cut_tip4p_long`, leaving PPPM unchanged. [Correctness passed, but 77.755 improves incumbent 78.12 by only 0.47%, below the 2% acceptance bar.] (b87604e6465e, weighted_median_pair_plus_kspace_seconds=77.755)
- [2026-04-12T14:53:28.582548Z] correctness_failure: Buffer hydrogen-i Coulomb force accumulation in pair_lj_cut_tip4p_long and flush once after the neighbor loop while preserving the incumbent oxygen and TIP4P redistribution write order [Hard reject: candidate 1478ab81846a is only 0.07% faster than incumbent ab55ea3e8508 but fails authoritative field-tolerance correctness in both cases.; case test-32r-long etotal_series relative_delta exceeds threshold (8.42997e-05 > 1e-06); case test-32r-long pe_series relative_delta exceeds threshold (0.00183969 > 1e-06); case test-32r-long ke_series relative_delta exceeds threshold (0.00345568 > 1e-06); case test-32r-long temp_series relative_delta exceeds threshold (0.00345568 > 1e-06); case test-32r-long press_series relative_delta exceeds threshold (0.222002 > 1e-05); case test-32r-long forces_xyz abs_delta exceeds threshold (0.105529 > 1e-08); case test-32r-long forces_xyz rms_delta exceeds threshold (0.0282935 > 1e-08); case test-64r-short forces_xyz abs_delta exceeds threshold (8.3e-06 > 1e-08); case test-64r-short forces_xyz rms_delta exceeds threshold (1.10787e-06 > 1e-08)] (1478ab81846a, weighted_median_pair_plus_kspace_seconds=78.069)
- [2026-04-12T23:20:50Z] rejected: Cache all-charge PPPM/TIP4P IK rho stencils and base brick indices in make_rho() and reuse them in fieldforce_ik() while retaining the incumbent oxygen M/H cache [Correctness passed, but candidate 57da41d1ad04 improves the incumbent by only 0.10% (78.12 to 78.045), below the benchmark contract's 2% acceptance bar.] (57da41d1ad04, weighted_median_pair_plus_kspace_seconds=78.045)
- [2026-04-12T23:23:20.739271Z] rejected: Cache all-charge PPPM/TIP4P IK rho stencils and base brick indices in make_rho() and reuse them in fieldforce_ik() while retaining the incumbent oxygen M/H cache [Correctness passed, but 78.045 is only a 0.10% improvement over incumbent 78.12 and misses the benchmark contract’s 2% acceptance bar.] (57da41d1ad04, weighted_median_pair_plus_kspace_seconds=78.045)
- [2026-04-12T23:45:56.880851Z] accepted: Use contiguous alias-friendly x/f/part2grid views in pair_lj_cut_tip4p_long and TIP4P PPPM particle_map/make_rho/fieldforce_ik without changing force, tally, or cache semantics [Correctness passed and candidate 2599e1f9f4c8 reduces weighted_median_pair_plus_kspace_seconds from 78.12 to 76.248, a 2.40% improvement over incumbent ab55ea3e8508 that clears the 2% acceptance bar.] (2599e1f9f4c8, weighted_median_pair_plus_kspace_seconds=76.248)
- [2026-04-13T00:06:02.800028Z] rejected: Reuse the pair style's current-step TIP4P O/H/M cache inside PPPM with a timestep guard and orthogonal-box fallback to the incumbent PPPM find_M cache [Correctness passed, but 76.144 is only a 0.14% improvement over incumbent 2599e1f9f4c8’s 76.248, well below the contract’s 2% acceptance bar.] (22ab6a7a53b5, weighted_median_pair_plus_kspace_seconds=76.144)
- [2026-04-13T00:35:02.234641Z] rejected: Merge the correctness-clean pair-only generation-stamped TIP4P site cache and common non-special-neighbor fast path into the current alias-view `pair_lj_cut_tip4p_long` incumbent [Correctness passed, but 75.536 improves incumbent 76.248 by only 0.93%, below the contract’s 2% acceptance bar.] (bd8ee0840992, weighted_median_pair_plus_kspace_seconds=75.536)
- [2026-04-13T00:57:24.707435Z] rejected: Specialize TIP4P PPPM order-5 make_rho()/fieldforce_ik() stencil kernels with a generic fallback [Correctness passed, but the order-5 PPPM stencil specialization regressed the incumbent by 4.90%, far short of the 2% improvement bar.] (a4391bb48f95, weighted_median_pair_plus_kspace_seconds=79.984)
- [2026-04-13T01:19:47.412439Z] rejected: Pure-water `lj/cut/tip4p/long` pair fast path plus generation-stamped TIP4P M-site reuse and common `sb == 0` handling in `pair_lj_cut_tip4p_long` [Correctness passed, but 75.36 improves incumbent 76.248 by only 1.16%, below the 2% acceptance bar.] (757c16f42b32, weighted_median_pair_plus_kspace_seconds=75.36)
- [2026-04-13T01:52:05.965463Z] accepted: Contiguous generation-stamped TIP4P pair/PPPM caches plus a pure-water lj/cut/tip4p/long pair fast path that keeps exact oxygen-site cutoffs and incumbent force/tally ordering [Correctness passed and the candidate cuts weighted_median_pair_plus_kspace_seconds from 76.248 to 72.919, a 4.37% improvement over incumbent 2599e1f9f4c8 that clears the 2% acceptance bar.] (4cbc0fa85603, weighted_median_pair_plus_kspace_seconds=72.919)
- [2026-04-13T02:16:05.916755Z] accepted: Inline TIP4P pair/PPPM cache-hit fast paths while keeping cache-miss reconstruction in dedicated out-of-line slow helpers [Correctness passed and 70.984 beats incumbent 72.919 by 2.65%, clearing the 2% acceptance bar.] (5840dac0d9f1, weighted_median_pair_plus_kspace_seconds=70.984)
- [2026-04-13T02:37:41.519068Z] rejected: Streamline PPPMTIP4P by removing the unconditional particle_map() allreduce and specializing the oxygen/non-oxygen plus z-force hot paths in particle_map(), make_rho(), and fieldforce_ik() without changing atom traversal or TIP4P force distribution [Correctness passed, but candidate aaa3d5fa36f1 regressed the incumbent primary metric from 70.984 to 71.085 and missed the 2% improvement bar.] (aaa3d5fa36f1, weighted_median_pair_plus_kspace_seconds=71.085)
- [2026-04-13T03:02:29.800765Z] rejected: Use typed alias-friendly cached TIP4P M-site views in pair_lj_cut_tip4p_long and pppm_tip4p while keeping the incumbent cache-miss reconstruction and exact force/tally ordering unchanged [Correctness passed, but candidate b9d56dcc532a improves the incumbent by only 0.65% (70.984 to 70.525), below the 2% acceptance bar.] (b9d56dcc532a, weighted_median_pair_plus_kspace_seconds=70.525)
- [2026-04-13T03:25:44.822843Z] correctness_failure: Cache per-step TIP4P O->M deltas/reach in pair_lj_cut_tip4p_long and reuse them to form exact oxygen-path Coulomb separations plus tighter cached-oxygen prechecks without changing force redistribution or tally order [Hard correctness failure in both benchmark cases, and the candidate also regressed the incumbent by 1.03%.; case test-32r-long etotal_series relative_delta exceeds threshold (8.74878e-05 > 1e-06); case test-32r-long pe_series relative_delta exceeds threshold (0.00235329 > 1e-06); case test-32r-long ke_series relative_delta exceeds threshold (0.00440903 > 1e-06); case test-32r-long temp_series relative_delta exceeds threshold (0.00440903 > 1e-06); case test-32r-long press_series relative_delta exceeds threshold (0.253437 > 1e-05); case test-32r-long forces_xyz abs_delta exceeds threshold (0.118184 > 1e-08); case test-32r-long forces_xyz rms_delta exceeds threshold (0.0291073 > 1e-08); case test-64r-short etotal_series relative_delta exceeds threshold (1.18848e-06 > 1e-06); case test-64r-short pe_series relative_delta exceeds threshold (3.56251e-05 > 1e-06); case test-64r-short ke_series relative_delta exceeds threshold (6.64653e-05 > 1e-06); case test-64r-short temp_series relative_delta exceeds threshold (6.64676e-05 > 1e-06); case test-64r-short press_series relative_delta exceeds threshold (0.00278554 > 1e-05); case test-64r-short forces_xyz abs_delta exceeds threshold (0.0186149 > 1e-08); case test-64r-short forces_xyz rms_delta exceeds threshold (0.000930271 > 1e-08)] (b028e7ec4bbc, weighted_median_pair_plus_kspace_seconds=71.716)
- [2026-04-13T03:48:21.383149Z] rejected: Precompute map-resolvable TIP4P oxygen M-sites before the pair loop and read the prepared cache directly, with the incumbent lazy path retained for unresolved ghost oxygens [Correctness passed, but 71.282 regressed incumbent 70.984 by 0.42% and missed the 2% acceptance bar.] (9eb54352a2e0, weighted_median_pair_plus_kspace_seconds=71.282)
- [2026-04-13T04:10:44.934114Z] rejected: Defer cached TIP4P hydrogen-index loads until exact oxygen interactions pass the final Coulomb cutoff in pair_lj_cut_tip4p_long, and use an xM-only cached M-site accessor in pppm_tip4p::particle_map() [Correctness passed, but 70.124 improves incumbent 70.984 by only 1.21%, below the 2% acceptance bar.] (8bfdf92c40a5, weighted_median_pair_plus_kspace_seconds=70.124)
- [2026-04-13T04:37:28.799233Z] rejected: Defer TIP4P pair hydrogen-index loads until exact oxygen-cutoff hits, add a common nonspecial Coulomb fast path in `pair_lj_cut_tip4p_long`, and use an xM-only PPPM cache accessor in `particle_map()` [Correctness passed, but candidate ff67ca3c0752 regressed the incumbent primary metric from 70.984 to 72.294, so it misses the 2% improvement bar.] (ff67ca3c0752, weighted_median_pair_plus_kspace_seconds=72.294)
- [2026-04-13T04:59:53.175610Z] rejected: Split TIP4P pair/PPPM caches into compact M-site coordinate arrays plus separate H-index/stamp state, deferring pair oxygen-neighbor H-index loads until exact Coulomb-cutoff hits [Correctness passed, but 70.877 improves incumbent 70.984 by only 0.15%, far below the 2% acceptance threshold.] (625e91025a03, weighted_median_pair_plus_kspace_seconds=70.877)
- [2026-04-13T05:23:30.434777Z] rejected: Defer pair-side TIP4P hydrogen-index and Coulomb-special loads until exact oxygen-path survivors, plus an xM-only PPPM particle_map cache accessor [Correctness passed, but candidate 44b554c8a168 improves the incumbent by only 0.67% (70.984 to 70.511), below the 2% acceptance bar, so 5840dac0d9f1 remains incumbent.] (44b554c8a168, weighted_median_pair_plus_kspace_seconds=70.511)
- [2026-04-13T05:50:34.677960Z] rejected: Use the lazy TIP4P pair/PPPM cache-access shape with an xM-only PPPM particle_map accessor, and hoist exact-survivor pair charge and coordinate math without changing force, tally, or write order [Correctness passed, but candidate d6aa774723cb improves the incumbent by only 0.35% (70.984 to 70.737), well below the 2% acceptance bar.] (d6aa774723cb, weighted_median_pair_plus_kspace_seconds=70.737)
- [2026-04-13T06:14:38.236230Z] rejected: Compose typed alias-friendly TIP4P cached M-site coordinates with lazy pair-side hydrogen-index use and an xM-only PPPM particle_map accessor, preserving incumbent cutoff and TIP4P force/tally ordering [Correctness passed, but 70.108 improves incumbent 70.984 by only 1.23%, below the contract’s 2% acceptance bar.] (e04c1aedddd3, weighted_median_pair_plus_kspace_seconds=70.108)
- [2026-04-13T06:37:31.798714Z] rejected: Lazy TIP4P xM-only cache access plus pair hydrogen-index deferral and PPPM stencil-base reuse [Correctness passed, but 71.763 regressed incumbent 70.984 by 1.10%, so this candidate cannot replace the incumbent.] (095bee6d96dc, weighted_median_pair_plus_kspace_seconds=71.763)
- [2026-04-13T07:00:13.519469Z] rejected: Split TIP4P pair/PPPM cache hits into xM-only versus hydrogen-index reads, and memoize the pair-side oxygen reach scalar alongside cached xM updates while preserving exact surviving M-site geometry [Correctness passed, but candidate 7c4d21219695 regressed the incumbent primary metric from 70.984 to 71.327, so it cannot replace 5840dac0d9f1.] (7c4d21219695, weighted_median_pair_plus_kspace_seconds=71.327)
## Long-Term Memory (Persistent)
### Benchmark contract
- Benchmark file: `.fermilink-optimize/benchmark.worker.yaml`
- Acceptance bar: preserve correctness and improve `weighted_median_pair_plus_kspace_seconds` by at least 2% versus the incumbent.
### Accepted experiments
- [2026-04-12T05:55:29.326337Z] baseline (e7c0ed95a333, weighted_median_pair_plus_kspace_seconds=94.717)
- [2026-04-12T08:33:03Z] candidate `3a45586c8afa` accepted as the new incumbent.
  Hypothesis: specializing `pair_lj_cut_tip4p_long` around compile-time energy/virial/table dispatch, reusing shared TIP4P site-cache state, and splitting oxygen vs non-oxygen `i` hot paths would remove branch and bookkeeping overhead while keeping the original force writes and `ev_tally_tip4p()` semantics intact.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 94.717 to 81.885, a 13.55% reduction versus the prior incumbent. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 84.337 s for `test-32r-long` and 5.5991 s for `test-64r-short`.
  Lesson learned: large gains are available in the base TIP4P long pair kernel, but only when the optimization preserves the original force accumulation and virial/energy tally semantics exactly. Unlike the rejected wholesale OPT-loop backport, targeted compile-time dispatch and hot-path separation delivered similar-scale speedup without scientific regressions.
  Next hypothesis: stack this accepted pair-kernel specialization with other semantics-preserving wins that attack different costs, starting with the previously safe PPPM-side TIP4P caching from iteration 1 or similarly localized hoists that compose cleanly with the new pair hot paths.
- [2026-04-12T08:33:49.996752Z] Specialize `pair_lj_cut_tip4p_long` with compile-time energy/virial/table dispatch, a shared TIP4P site-cache helper, and separate oxygen/non-oxygen `i` hot paths while preserving the original force writes and `ev_tally_tip4p()` semantics. [Correctness passed with no guardrail errors, and the candidate improves the primary metric from 94.717 to 81.885, a 13.55% gain that clears the 2% acceptance bar.] (3a45586c8afa, weighted_median_pair_plus_kspace_seconds=81.885)
- [2026-04-12T11:51:34Z] candidate `2a0b902a8b5a` accepted as the new incumbent.
  Hypothesis: tightening exact oxygen-to-nonoxygen Coulomb prechecks and adding a dedicated water-H `i` fast path in `pair_lj_cut_tip4p_long` would turn the previously sub-threshold pair-side pruning ideas into an acceptance-grade speedup by skipping impossible Coulomb/LJ work while preserving the incumbent TIP4P force and `ev_tally_tip4p()` ordering exactly.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 79.88, a 2.45% reduction versus the prior incumbent. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 80.039 s for `test-32r-long` and 5.4225 s for `test-64r-short`.
  Lesson learned: semantics-preserving pair-side pruning can still clear the bar after iteration 3, but only when it reaches oxygen-containing interactions and impossible LJ work inside the incumbent control flow without adding broad template fan-out or disturbing force/tally order. The win here came from a focused water-H `i` fast path plus tighter exact oxygen/non-oxygen reach tests, not from changing accumulation behavior.
  Next hypothesis: build on incumbent `2a0b902a8b5a` with another narrow, semantics-preserving reduction in remaining oxygen/M-site pair work or deeper PPPM inner-loop math/indexing, while avoiding broad cached state, altered `f[i]` write ordering, `ev_tally` changes, or code-size-heavy interaction-class specialization.
- [2026-04-12T11:51:34Z] Tighten exact oxygen-to-nonoxygen Coulomb prechecks and add a dedicated water-H `i` fast path in `pair_lj_cut_tip4p_long` that skips impossible LJ work while preserving the incumbent TIP4P force/tally order. [Correctness passed with no guardrail errors, and the candidate improves the primary metric from 81.885 to 79.88, a 2.45% gain that clears the 2% acceptance bar.] (2a0b902a8b5a, weighted_median_pair_plus_kspace_seconds=79.88)
- [2026-04-12T11:54:12.717864Z] Tighten exact oxygen-to-nonoxygen Coulomb prechecks and add a dedicated water-H `i` fast path in `pair_lj_cut_tip4p_long` that skips impossible LJ work while preserving the incumbent TIP4P force/tally order. [Correctness passed and 79.88 beats incumbent 81.885 by 2.45%, clearing the 2% acceptance bar.] (2a0b902a8b5a, weighted_median_pair_plus_kspace_seconds=79.88)
- [2026-04-12T13:42:30.493425Z] Restore safe hydrogen-neighbor LJ pruning in `pair_lj_cut_tip4p_long` and reuse cached TIP4P oxygen M/H state across PPPM `particle_map()`/`make_rho()`/`fieldforce_ik()` with `memset` density zeroing and linear IK brick indexing [Correctness passed and 78.12 beats incumbent 79.88 by 2.20%, clearing the 2% acceptance bar.] (ab55ea3e8508, weighted_median_pair_plus_kspace_seconds=78.12)
- [2026-04-12T23:45:56.880851Z] Use contiguous alias-friendly x/f/part2grid views in pair_lj_cut_tip4p_long and TIP4P PPPM particle_map/make_rho/fieldforce_ik without changing force, tally, or cache semantics [Correctness passed and candidate 2599e1f9f4c8 reduces weighted_median_pair_plus_kspace_seconds from 78.12 to 76.248, a 2.40% improvement over incumbent ab55ea3e8508 that clears the 2% acceptance bar.] (2599e1f9f4c8, weighted_median_pair_plus_kspace_seconds=76.248)
- [2026-04-13T01:52:05.965463Z] Contiguous generation-stamped TIP4P pair/PPPM caches plus a pure-water lj/cut/tip4p/long pair fast path that keeps exact oxygen-site cutoffs and incumbent force/tally ordering [Correctness passed and the candidate cuts weighted_median_pair_plus_kspace_seconds from 76.248 to 72.919, a 4.37% improvement over incumbent 2599e1f9f4c8 that clears the 2% acceptance bar.] (4cbc0fa85603, weighted_median_pair_plus_kspace_seconds=72.919)
- [2026-04-13T02:16:05.916755Z] Inline TIP4P pair/PPPM cache-hit fast paths while keeping cache-miss reconstruction in dedicated out-of-line slow helpers [Correctness passed and 70.984 beats incumbent 72.919 by 2.65%, clearing the 2% acceptance bar.] (5840dac0d9f1, weighted_median_pair_plus_kspace_seconds=70.984)
### Rejected experiments
- [2026-04-12T06:23:25Z] candidate `0157b0571dbb` rejected as non-incumbent despite passing correctness.
  Hypothesis: caching PPPM TIP4P virtual-site reconstruction and bonded H-index lookup once per local oxygen per compute would remove repeated `find_M()` and H lookup overhead across `particle_map`, `make_rho`, `fieldforce_ad`, `fieldforce_ik`, `fieldforce_peratom`, and slab correction.
  Changed files: `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 94.717 to 94.369, both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported.
  Lesson learned: the cached PPPM bookkeeping is scientifically safe and measurably faster, but the win is too small to clear the 2% acceptance threshold; reducing repeated PPPM-side M-site reconstruction alone does not move the dominant pair+kspace cost enough on this workload.
- [2026-04-12T08:02:01Z] candidate `2bbd33a175f` rejected for correctness failure.
  Hypothesis: backporting the serial OPT hot loop into the base `lj/cut/tip4p/long` implementation, with branch-specialized `eval<>` paths and local `f[i]` accumulation, would cut pair-side overhead enough to produce an acceptance-grade speedup.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 94.717 to 82.844, a 12.54% reduction versus the incumbent, and both benchmark cases converged. However, correctness failed hard: `test-32r-long` exceeded tolerances for `etotal_series`, `pe_series`, `ke_series`, `temp_series`, `press_series`, and `forces_xyz` (abs delta 0.107617, RMS delta 0.0276885), while `test-64r-short` also failed `forces_xyz` (abs delta 1.44e-05, RMS delta 1.44068e-06). With `hard_reject=true`, this candidate cannot become the incumbent.
  Lesson learned: the base TIP4P long pair kernel is highly sensitive to exact force distribution and energy/virial tally semantics; a wholesale transplant of the serial OPT loop is not correctness-preserving here, and the resulting differences are far larger than roundoff noise.
  Next hypothesis: keep the original pair kernel’s physical bookkeeping intact and pursue smaller, semantics-preserving pair-side optimizations, such as hoisting invariant branch selection or reusing per-oxygen `newsite`/hydrogen-neighbor state within the existing control flow instead of replacing the full interaction loop.
- [2026-04-12T08:59:11Z] candidate `847eb8e5dd4b` rejected as non-incumbent despite passing correctness.
  Hypothesis: caching local TIP4P M-site reconstruction and 1D rho stencils during `particle_map()` would let `make_rho()`, fieldforce, per-atom, and slab-correction paths reuse PPPM state and remove repeated `find_M()`/`compute_rho1d()` work.
  Changed files: `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` measured 81.985 versus the incumbent's 81.885, so the candidate was 0.12% slower and far short of the 2% acceptance bar. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 81.732 s for `test-32r-long` and 5.5536 s for `test-64r-short`.
  Lesson learned: after the accepted pair-kernel specialization, broad PPPM-side cache-state expansion does not automatically compose into a net gain; the saved recomputation was at best neutral here and may have been offset by added cache maintenance and memory traffic in `particle_map()`.
  Next hypothesis: profile the incumbent again and target the now-dominant remaining costs with narrower, semantics-preserving changes, such as pair-side hot-path cleanup that keeps the accepted structure intact or more focused PPPM math/communication reductions that avoid large per-atom cached state.
- [2026-04-12T09:24:24Z] candidate `42d987d04a9c` rejected as non-incumbent despite passing correctness.
  Hypothesis: skipping the extended TIP4P Coulomb `cut_coulsqplus` precheck for exact non-oxygen/non-oxygen pairs in `pair_lj_cut_tip4p_long` would avoid unnecessary water-specific work on interactions that never need M-site handling, while leaving every oxygen-containing interaction on the incumbent path.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 81.298, a 0.72% reduction versus the incumbent, but that still misses the 2% acceptance threshold. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 81.298 s for `test-32r-long` and 5.5280 s for `test-64r-short`.
  Lesson learned: tightening the exact non-water Coulomb fast path is scientifically safe and measurably beneficial, but it only trims a secondary cost center after iteration 3; the dominant workload still sits in oxygen/M-site interactions and PPPM, so isolated non-oxygen precheck pruning is not enough for another acceptance-grade gain.
  Next hypothesis: keep this idea only as a supporting optimization and focus the next candidate on higher-leverage hot paths inside oxygen-containing pair interactions or a newly profiled PPPM hotspot that composes with incumbent `3a45586c8afa`.
- [2026-04-12T09:52:01Z] candidate `89aa52c5901e` rejected for correctness failure.
  Hypothesis: specializing `pair_lj_cut_tip4p_long` for the benchmark's global-only tally path, combining that with the exact non-water/non-water Coulomb fast path, and locally accumulating non-water `f[i]` updates would compound safe incumbent ideas into an acceptance-grade speedup while preserving the incumbent TIP4P oxygen/M-site force distribution.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 79.639, a 2.74% reduction versus the incumbent, and both benchmark cases converged. However, correctness failed hard: `test-32r-long` exceeded tolerances for `etotal_series`, `pe_series`, `ke_series`, `temp_series`, `press_series`, and `forces_xyz` (abs delta 0.105529, RMS delta 0.0282935), while `test-64r-short` also failed `forces_xyz` (abs delta 8.3e-06, RMS delta 1.10787e-06). With `hard_reject=true`, this candidate cannot become the incumbent.
  Lesson learned: the exact non-water Coulomb fast path from iteration 5 appears safe in isolation, but composing it with benchmark-specific global-only tally specialization and deferred non-water `f[i]` accumulation reintroduces the same class of TIP4P force/tally semantic breakage seen in iteration 2. The added speedup likely came from changing accumulation and tally behavior rather than from a scientifically neutral pruning of work.
  Next hypothesis: keep exact non-water fast paths isolated, but avoid any optimization that changes force write ordering, local `f[i]` accumulation, or `ev_tally` semantics. Target the next iteration at narrower semantics-preserving hoists inside the incumbent control flow, especially invariant dispatch/bookkeeping around oxygen/M-site work or a newly profiled PPPM hotspot.
- [2026-04-12T10:22:36Z] candidate `c37498ebee61` rejected as non-incumbent despite passing correctness.
  Hypothesis: keeping the exact H-H Coulomb cutoff fast path from iteration 5 while reusing the first oxygen `find_M()` result inside `PPPMTIP4P::fieldforce_ik()` / `fieldforce_ad()` and hoisting constant `alpha`/slab factors out of the oxygen force-distribution branch would combine two semantics-preserving micro-optimizations into a measurable pair+kspace win.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pppm_tip4p.cpp`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 81.445, a 0.54% reduction versus the incumbent, but that still misses the 2% acceptance threshold. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 81.445 s for `test-32r-long` and 5.4984 s for `test-64r-short`.
  Lesson learned: this pair plus PPPM cleanup composes safely with the incumbent and confirms that reusing existing `find_M()` results and hoisting invariant force-distribution factors do not perturb TIP4P correctness. However, the gain remains marginal, so the remaining dominant cost is not the repeated `fieldforce_*()` M-site reconstruction or the exact H-H cutoff precheck by itself.
  Next hypothesis: keep these semantics-preserving micro-hoists as supporting ideas, but aim the next experiment at a larger oxygen/M-site or PPPM hot loop where repeated math or indexing can be removed without introducing new cache state or changing force/tally/write-order semantics.
- [2026-04-12T10:42:25Z] candidate `7d859a5521df` rejected as non-incumbent despite passing correctness.
  Hypothesis: specializing `pair_lj_cut_tip4p_long` Coulomb handling by interaction class (`O-O`, `O-H`, `H-O`, and exact non-oxygen/non-oxygen) would remove repeated `jtype == typeO` branching inside the incumbent pair hot loop while preserving the accepted TIP4P force distribution, `ev_tally_tip4p()` semantics, and the safe exact non-water cutoff pruning.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` measured 85.841 versus the incumbent's 81.885, so the candidate was 4.83% slower and far short of the 2% acceptance bar. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 85.841 s for `test-32r-long` and 5.8109 s for `test-64r-short`.
  Lesson learned: further pair-side interaction-class templating did not compose into a net win on top of the accepted incumbent. The extra helper split and code duplication likely increased instruction footprint or constrained compiler optimization more than they reduced the remaining branch cost, so this workload is no longer bottlenecked by that `jtype` test alone.
  Next hypothesis: avoid more code-size-heavy pair-kernel branch specialization and instead target a higher-leverage oxygen/M-site or PPPM hotspot where redundant math or indexing can be removed with minimal control-flow duplication and no changes to force/tally/write-order semantics.
- [2026-04-12T11:05:20.024383403Z] candidate `0356a6e5d35a` rejected as non-incumbent despite passing correctness.
  Hypothesis: tightening `pair_lj_cut_tip4p_long` Coulomb prechecks only for exact `H-H` pairs and actual-current-step `O(i)-H(j)` reach tests would trim unnecessary TIP4P cutoff work inside the incumbent pair hot loop while keeping all oxygen-`j` handling plus the accepted force/tally write order unchanged.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 80.674, a 1.48% reduction versus the incumbent, but that still misses the 2% acceptance threshold. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 80.643 s for `test-32r-long` and 5.4737 s for `test-64r-short`.
  Lesson learned: this narrower precheck tightening is scientifically safe and materially better than the iteration 8 interaction-class helper, so the exact `H-H` and current-step `O(i)-H(j)` reach tests do carry some avoidable overhead. But the remaining benefit is still sub-threshold, which means pair-side precheck pruning is now a supporting optimization rather than a standalone route to a new incumbent.
  Next hypothesis: keep this safe pruning idea only as a component of a broader candidate and shift the next experiment toward a higher-leverage oxygen/M-site or PPPM reduction that removes repeated math/indexing without changing force write ordering, `ev_tally` semantics, or reintroducing heavy template/code-size growth.
- [2026-04-12T11:24:59Z] candidate `25819367f233` rejected as non-incumbent despite passing correctness.
  Hypothesis: combining a tighter exact oxygen-i/non-oxygen-j Coulomb precheck in `pair_lj_cut_tip4p_long` with the safe PPPM `fieldforce_ik()` / `fieldforce_ad()` reuse of the already identified TIP4P oxygen mapping and hoisted alpha/slab constants would stack two semantics-preserving micro-optimizations into an acceptance-grade pair+kspace reduction.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 81.885 to 80.914, a 1.19% reduction versus the incumbent, but that still misses the 2% acceptance threshold. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 80.681 s for `test-32r-long` and 5.5793 s for `test-64r-short`.
  Lesson learned: stacking two previously safe micro-optimizations remained scientifically safe, but the gains did not add constructively enough to reach promotion. Relative to iteration 9's stronger 1.48% result, the extra PPPM `fieldforce_*()` cleanup did not offset the cost mix as hoped, which suggests the remaining dominant opportunity is not in small PPPM force-distribution hoists and may lie more in oxygen/M-site pair work than in current `fieldforce` bookkeeping.
  Next hypothesis: keep exact oxygen/non-oxygen precheck tightening as a supporting ingredient, but target the next candidate at a higher-leverage semantics-preserving oxygen/M-site hot path or a deeper PPPM inner-loop math/indexing reduction that removes repeated work without broad cached state, altered force write ordering, or `ev_tally` changes.
- [2026-04-12T12:14:55Z] candidate `4f3910700624` rejected as non-incumbent despite passing correctness.
  Hypothesis: pruning impossible hydrogen-neighbor LJ work and reusing already-cached oxygen M-sites to tighten reverse nonoxygen-to-oxygen Coulomb prechecks in `pair_lj_cut_tip4p_long` would extend iteration 11's safe pair-side pruning into the remaining nonoxygen-`i` / oxygen-`j` path without changing TIP4P force distribution or `ev_tally_tip4p()` ordering.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` measured 82.549 versus the incumbent's 79.88, so the candidate was 3.34% slower and far short of the 2% acceptance bar. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 82.537 s for `test-32r-long` and 5.6357 s for `test-64r-short`.
  Lesson learned: this reverse nonoxygen-to-oxygen screening tweak does not compose with the incumbent. Reusing cached oxygen M-sites for the reverse precheck and deferring `special_lj` loads appears to add more branch/math overhead or instruction-pressure than the saved work removes, so further pair-loop micro-optimizations in this exact reverse-screening direction are unlikely to pay off.
  Next hypothesis: stop pursuing reverse nonoxygen-to-oxygen precheck tightening and cached-oxygen-site screening tweaks in the pair loop. Target the next candidate at a higher-leverage oxygen/M-site pair or PPPM inner-loop reduction that removes repeated math, indexing, or communication work without broad cached state, added code-size pressure, or any force/tally/write-order changes.
- [2026-04-12T13:38:12Z] iteration 16 review: accepted candidate ab55ea3e8508 (weighted_median_pair_plus_kspace_seconds=78.12, correctness OK, both cases converged) because it improved the incumbent by 2.20%, clearing the contract's 2% minimum relative improvement bar.
- [2026-04-12T13:38:12Z] candidate `ab55ea3e8508` accepted as new incumbent.
  Hypothesis: restoring the safe hydrogen-neighbor LJ pruning from the earlier near-miss and pairing it with full TIP4P oxygen M/H caching across PPPM `particle_map()` / `make_rho()` / `fieldforce_ik()`, plus lower-overhead density clearing and IK brick indexing, would recover iteration 13's strong gain and shave enough surrounding PPPM overhead to finally clear the 2% bar.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 79.88 to 78.12, a 2.20% reduction versus the incumbent and a 17.53% reduction versus baseline, with both benchmark cases converged, `correctness_ok=true`, and no guardrail errors. Case-level pair-plus-kspace timings were 78.12 s for `test-32r-long` and 5.3094 s for `test-64r-short`; peak RSS was 157.84 MiB. This clears the acceptance threshold, so `ab55ea3e8508` becomes the new incumbent.
  Lesson learned: the fuller PPPM oxygen cache shape from iteration 13 was the right high-leverage direction, but it needed cheaper surrounding PPPM loops to pay off. In contrast with the weaker H-index-only and IK-only variants from iterations 14 and 15, keeping cached `xM`/H state across PPPM phases and pairing it with `memset` density zeroing plus linear IK brick access is enough to turn the earlier near-miss into an acceptance-grade win.
  Next hypothesis: build on incumbent `ab55ea3e8508` and look for another semantics-preserving reduction that complements this accepted full-cache design, especially additional PPPM oxygen-path math/indexing cleanups or pair-side oxygen-path pruning that reuses already-cached state without changing force/tally/write-order semantics or adding another large cache footprint.
- [2026-04-12T13:38:12Z] accepted: Restore safe hydrogen-neighbor LJ pruning in `pair_lj_cut_tip4p_long` and reuse cached TIP4P oxygen M/H state across PPPM `particle_map()` / `make_rho()` / `fieldforce_ik()` with `memset` density zeroing and linear IK brick indexing. [Correctness passed, and `weighted_median_pair_plus_kspace_seconds` improved from 79.88 to 78.12, a 2.20% gain that clears the 2% acceptance bar.] (ab55ea3e8508, weighted_median_pair_plus_kspace_seconds=78.12)
- [2026-04-12T14:09:33Z] candidate `ad7811dc14f3` rejected as non-incumbent despite passing correctness.
  Hypothesis: generation-stamping the incumbent TIP4P pair/PPPM caches, adding a common non-special pair fast path, and reusing PPPM oxygen-H mapping state across non-reneighbor steps would trim cache-clear and bookkeeping overhead on top of incumbent `ab55ea3e8508` without changing TIP4P force distribution or `ev_tally` semantics.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 78.12 to 77.512, a 0.78% reduction versus the incumbent and an 18.16% reduction versus baseline. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 77.512 s for `test-32r-long` and 5.2507 s for `test-64r-short`; peak RSS was 157.42 MiB. This is the best correctness-preserving raw metric seen so far, but it still misses the 2% acceptance threshold, so `ab55ea3e8508` remains the incumbent.
  Lesson learned: generation-stamped validity and cross-step oxygen-H mapping reuse are scientifically safe and directionally helpful, but they only deliver an incremental gain on top of the accepted full-cache design from iteration 16. That implies the remaining bottleneck is no longer dominated by per-step cache clearing or oxygen-H remapping between reneighbor events, and extra cache-lifecycle machinery has entered diminishing returns.
  Next hypothesis: treat generation-stamped cache reuse and the common non-special pair fast path as safe supporting ingredients, but only bundle them with a higher-leverage reduction in oxygen-containing pair math or PPPM oxygen-path indexing/interpolation. Avoid spending another standalone iteration on cache-generation or cross-step reuse mechanics alone.
- [2026-04-12T23:20:50Z] iteration 20 review: rejected candidate `57da41d1ad04` (weighted_median_pair_plus_kspace_seconds=78.045, correctness OK, both cases converged) because the 0.10% improvement versus incumbent `ab55ea3e8508` missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T23:20:50Z] candidate `57da41d1ad04fb3ce605b0b697f4de981bf3f6af` rejected as non-incumbent despite passing correctness.
  Hypothesis: caching all-charge PPPM/TIP4P IK `rho1d` stencils and base brick indices in `make_rho()` and reusing them in `fieldforce_ik()`, on top of the incumbent oxygen M/H cache, would remove enough repeated interpolation/indexing work from the PPPM oxygen path to deliver another acceptance-grade reduction without touching pair semantics.
  Changed files: `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 78.12 to 78.045, a 0.10% reduction versus incumbent `ab55ea3e8508` and a 17.60% reduction versus baseline. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 78.045 s for `test-32r-long` and 5.2858 s for `test-64r-short`; peak RSS was 157.36 MiB. This is directionally positive but far below the 2% acceptance threshold, so `ab55ea3e8508` remains the incumbent.
  Lesson learned: deeper PPPM-side reuse of all-charge IK stencils and brick bases is scientifically safe, but on top of the accepted full oxygen M/H cache it only buys a near-noise improvement. That implies the saved `make_rho()` / `fieldforce_ik()` recomputation is no longer a dominant cost center, and the extra cache fill/load traffic largely offsets the arithmetic and indexing removed.
  Next hypothesis: do not spend another standalone iteration on broader per-charge stencil/base-index caching in PPPM. The next candidate should target a higher-leverage semantics-preserving reduction that composes with incumbent `ab55ea3e8508`, most likely eliminating remaining oxygen-path math/indexing or pair-side oxygen work without adding more cache-heavy state or changing force/tally/write-order semantics.
- [2026-04-12T23:44:04Z] iteration 21 review: accepted candidate `2599e1f9f4c8` (weighted_median_pair_plus_kspace_seconds=76.248, correctness OK, both cases converged) because it improved the incumbent `ab55ea3e8508` by 2.40%, clearing the contract's 2% minimum relative improvement bar without guardrail errors.
- [2026-04-12T23:44:04Z] candidate `2599e1f9f4c8bb574886ac80f8e67052265a0ad4` accepted as the new incumbent.
  Hypothesis: replacing hot-loop `double **` and `part2grid[][3]` pointer chasing with contiguous alias-friendly `x`/`f`/`part2grid` views in `pair_lj_cut_tip4p_long` and TIP4P PPPM `particle_map()` / `make_rho()` / `fieldforce_ik()`, while preserving all existing force, tally, cache, and write-order semantics, would remove enough access overhead to produce an acceptance-grade speedup on top of incumbent `ab55ea3e8508`.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pppm_tip4p.cpp`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 78.12 to 76.248, a 2.40% reduction versus incumbent `ab55ea3e8508` and a 19.50% reduction versus baseline. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 76.248 s for `test-32r-long` and 5.182 s for `test-64r-short`; the long case split was 55.487 s pair and 20.761 s kspace, and peak RSS was 158.17 MiB. This clears the acceptance threshold, so `2599e1f9f4c8` becomes the new incumbent.
  Lesson learned: there is still acceptance-grade headroom in low-risk hot-loop access cleanup even after the larger semantic and cache wins were harvested. Iteration 20 showed that adding more PPPM cache reuse on top of `ab55ea3e8508` was near-noise, but iteration 21 demonstrates that reducing pointer indirection and repeated indexed loads across the already-correct pair and PPPM kernels can compound with the incumbent cache design and unlock a materially larger gain without touching scientifically sensitive force-update or tally ordering.
  Next hypothesis: build on incumbent `2599e1f9f4c8` and continue pursuing semantics-preserving hot-loop access cleanup rather than new cache-heavy structures or deferred writes. The next candidate should target remaining benchmark-relevant TIP4P paths that still use indirect `x`/`f`/grid access or repeated local indexing, especially adjacent PPPM oxygen paths not covered here and any remaining oxygen-heavy pair arithmetic that can be hoisted or linearized without changing force, virial, cache, or `ev_tally` semantics.
- [2026-04-13T00:05:07Z] iteration 22 review: rejected candidate `22ab6a7a53b5` (weighted_median_pair_plus_kspace_seconds=76.144, correctness OK, both cases converged) because the 0.14% improvement versus incumbent `2599e1f9f4c8` missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T00:05:07Z] candidate `22ab6a7a53b5fd99fad9764b927d00d6df26e1f3` rejected as non-incumbent despite passing correctness.
  Hypothesis: reusing the pair style's current-step TIP4P O/H/M cache inside PPPM, with a timestep-validity guard and orthogonal-box fallback to the incumbent PPPM `find_M()` cache, would remove enough duplicate M-site reconstruction and hydrogen lookup work across pair and kspace to deliver another acceptance-grade gain on top of incumbent `2599e1f9f4c8`.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 76.248 to 76.144, a 0.14% reduction versus incumbent `2599e1f9f4c8` and a 19.61% reduction versus baseline. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 76.13 s for `test-32r-long` and 5.2137 s for `test-64r-short`; the long case split was 55.528 s pair and 20.602 s kspace, and peak RSS was 157.29 MiB. This is directionally positive but far below the 2% acceptance threshold, so `2599e1f9f4c8` remains the incumbent.
  Lesson learned: same-step pair-to-PPPM TIP4P cache sharing is scientifically safe with strict timestep and orthogonal-box guards, but it is now too low leverage to matter materially. The saved cross-kernel `find_M()`/H-lookup work produces only a near-noise net gain after the accepted full PPPM oxygen cache and iteration 21's access cleanup, and the weaker short-case result suggests the extra `extract()`/guard/shared-cache coordination largely trades arithmetic for control-flow and memory overhead instead of removing a dominant cost.
  Next hypothesis: do not spend another standalone iteration on exporting pair-owned TIP4P cache state into PPPM. Build on incumbent `2599e1f9f4c8` with higher-leverage semantics-preserving hot-loop cleanup inside each kernel separately, especially remaining indirect PPPM oxygen/grid access or oxygen-heavy pair arithmetic that can be hoisted or linearized without adding new cross-kernel coupling, cache-heavy state, or any force/tally/write-order risk.
- [2026-04-13T00:32:22.997554Z] iteration 23 review: rejected candidate `bd8ee0840992` (weighted_median_pair_plus_kspace_seconds=75.536, correctness OK, both cases converged) because the 0.93% improvement versus incumbent `2599e1f9f4c8` missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T00:32:22.997554Z] candidate `bd8ee0840992b9c3536b9e568fa737071e9ec415` rejected as non-incumbent despite passing correctness.
  Hypothesis: merging the correctness-clean pair-only generation-stamped TIP4P site cache and common non-special-neighbor fast path into the current alias-view `pair_lj_cut_tip4p_long` incumbent would stack safe pair-side bookkeeping and common-case branch reductions on top of iteration 21's access cleanup strongly enough to clear the 2% acceptance bar without touching PPPM code or force/tally semantics.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 76.248 to 75.536, a 0.93% reduction versus incumbent `2599e1f9f4c8` and a 20.25% reduction versus baseline. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 75.559 s for `test-32r-long` and 5.1293 s for `test-64r-short`; the long case split was 55.164 s pair and 20.395 s kspace, and peak RSS was 157.03 MiB. This is the strongest post-incumbent improvement so far, but it still misses the 2% acceptance threshold, so `2599e1f9f4c8` remains the incumbent.
  Lesson learned: the generation-stamped pair cache and common non-special-neighbor fast path do compose cleanly with the alias-friendly incumbent and remain scientifically safe, but the pair-only payoff is still sub-1% at this stage. Since PPPM code was unchanged here, the useful conclusion is that there is only limited remaining standalone headroom in pair-side bookkeeping and common-case cleanup on top of incumbent `2599e1f9f4c8`.
  Next hypothesis: treat this pair-only generation-stamped/common-fast-path merge as a supporting ingredient rather than a standalone incumbent candidate. The next experiment should bundle it with a higher-leverage, still semantics-preserving in-kernel reduction, most likely remaining PPPM oxygen/grid access/indexing cleanup or pair-side arithmetic hoisting that removes materially more work without introducing cross-kernel cache sharing, deferred force updates, or any change to TIP4P write/tally ordering.
- [2026-04-13T03:22:25Z] iteration 30 review: rejected candidate `b028e7ec4bbc` (weighted_median_pair_plus_kspace_seconds=71.716, benchmark runs converged) because the benchmark contract reported `hard_reject=true` for field-tolerance correctness failures in both cases, and the candidate also regressed incumbent `5840dac0d9f1` by 1.03%.
- [2026-04-13T03:22:25Z] candidate `b028e7ec4bbcf0afb4c2ac9ca0b4e05bad56fec5` rejected for correctness failure.
  Hypothesis: caching per-step TIP4P O->M deltas/reach in `pair_lj_cut_tip4p_long` and reusing them to form exact oxygen-path Coulomb separations plus tighter cached-oxygen prechecks would remove enough repeated oxygen-path geometry work to produce an acceptance-grade pair win without changing force redistribution or tally order.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` regressed from incumbent `5840dac0d9f1`'s 70.984 to 71.716, a 1.03% slowdown versus the incumbent, though it remains 24.28% better than baseline `e7c0ed95a333`. Both benchmark cases converged and run status was `ok`; case-level pair-plus-kspace timings were 71.684 s for `test-32r-long` and 4.9556 s for `test-64r-short`, with peak RSS 159.36 MiB. However, authoritative field-tolerance correctness failed hard in both cases: `test-32r-long` exceeded `etotal_series`, `pe_series`, `ke_series`, `temp_series`, `press_series`, and `forces_xyz` (abs delta 0.118184, RMS delta 0.0291073), while `test-64r-short` also exceeded `etotal_series`, `pe_series`, `ke_series`, `temp_series`, `press_series`, and `forces_xyz` (abs delta 0.0186149, RMS delta 0.000930271). With `hard_reject=true`, this candidate cannot replace the incumbent, so `5840dac0d9f1` remains in place.
  Lesson learned: even when TIP4P force redistribution and tally order stay unchanged, reusing cached O->M deltas/reach to reform oxygen-path Coulomb separations or tighten cached-oxygen prechecks is not scientifically safe in the current pair kernel. The broad force and thermo divergence across both cases shows that the oxygen-path geometry/cutoff math is extremely sensitive to how M-site separations and reach bounds are reconstructed; pair-side algebraic rewrites in that path can silently change physics even when they appear semantically equivalent.
  Next hypothesis: do not pursue further pair-side rewrites that derive oxygen-path M-site separations or tighter reach tests from cached O->M deltas unless equivalence can be proven bit-for-bit against the incumbent formulas. Build on incumbent `5840dac0d9f1` with a composed but lower-risk hot-path simplification that leaves the exact oxygen-path geometry/cutoff logic untouched, focusing instead on surrounding cache-hit overhead, repeated non-geometric indexing/load cleanup, or other pair+PPPM reductions that preserve existing force/tally/write-order and exact cutoff semantics.
- [2026-04-13T03:46:43Z] iteration 31 review: rejected candidate `9eb54352a2e0` (weighted_median_pair_plus_kspace_seconds=71.282, correctness OK, both cases converged) because it regressed incumbent `5840dac0d9f1` by 0.42% and therefore missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T03:46:43Z] candidate `9eb54352a2e07109348506cd31c4ac2fcaa397c0` rejected as non-incumbent despite passing correctness.
  Hypothesis: precomputing map-resolvable TIP4P oxygen M-sites before the pair loop and reading the prepared cache directly, while retaining the incumbent lazy path for unresolved ghost oxygens, would remove enough in-loop TIP4P site reconstruction and cache-hit overhead to produce another acceptance-grade pair-side gain on top of incumbent `5840dac0d9f1` without changing oxygen-path geometry, cutoff formulas, or force/tally ordering.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` regressed from incumbent `5840dac0d9f1`'s 70.984 to 71.282, a 0.42% slowdown versus the incumbent, though it remains 24.74% better than baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, and `guardrail_errors=[]`, with peak RSS 157.92 MiB. Case-level pair-plus-kspace timings were 71.212 s for `test-32r-long` and 4.8895 s for `test-64r-short`; in the long case, pair time regressed to 51.387 s while kspace time was 19.825 s. This does not clear the acceptance threshold, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: eager pair-side preparation of map-resolvable oxygen M-sites is scientifically safe, but on top of the incumbent's inlined cache-hit fast paths it over-prepares too many sites and front-loads extra `atom->map()`, `closest_image()`, and `compute_newsite()` work that the demand-driven lazy path often avoids or amortizes better. The long-case pair regression indicates that the remaining bottleneck is not the cost of reading cached `xM` for already-hit oxygens; a whole-loop oxygen prepass adds more memory traffic and bookkeeping than it removes, especially when unresolved ghost oxygens still need the incumbent fallback path.
  Next hypothesis: avoid another standalone eager pre-loop TIP4P site-preparation pass over all map-resolvable oxygen atoms. Build on incumbent `5840dac0d9f1` with a more selective composed reduction that keeps the lazy-on-demand site-preparation semantics, only hoists work proven to be reused within actual interacting paths, and focuses on remaining pair+PPPM cache-hit indexing/load overhead rather than broad pair-only cache refreshes.
### Open hypotheses
- The next candidate should build on incumbent `5840dac0d9f1`. Iterations 30 and 31 show that pair-side changes which either reform oxygen-path M-site separations/reach tests or eagerly precompute oxygen sites across the whole pair domain are both poor directions: the former is scientifically unsafe, while the latter is correctness-clean but negative leverage because it adds front-loaded bookkeeping and cache traffic. Iterations 28 and 29 also show standalone PPPM micro-specialization and representation-only cache-view refactors are sub-1% levers. Favor a composed pair+PPPM hot-path simplification that keeps the incumbent exact oxygen-path geometry/cutoff formulas and lazy-on-demand site-preparation semantics intact, and removes surrounding cache-hit indexing/control-flow overhead only along actual interacting paths, rather than adding more PPPM branch splitting, allreduce elision, cross-kernel cache sharing, typed-view cleanup alone, eager all-oxygen prepasses, or oxygen-path algebra rewrites. Continue to avoid deferred `f[i]` updates, altered TIP4P force/tally/write ordering, new cache-heavy state, and large hot-path code growth.

### Rejected patterns
- [2026-04-13T03:46:43Z] Precompute map-resolvable TIP4P oxygen M-sites before the pair loop and read the prepared cache directly, with lazy fallback for unresolved ghost oxygens [Correctness passed, but candidate `9eb54352a2e0` regressed incumbent `5840dac0d9f1` by 0.42%; avoid broad eager pair-side oxygen-site preparation because the front-loaded `map`/`closest_image`/`compute_newsite` work and cache traffic outweigh the savings from bypassing the incumbent's demand-driven cache-hit path.] (9eb54352a2e0, weighted_median_pair_plus_kspace_seconds=71.282)
- [2026-04-13T03:22:25Z] Cache per-step TIP4P O->M deltas/reach in `pair_lj_cut_tip4p_long` and reuse them to form exact oxygen-path Coulomb separations plus tighter cached-oxygen prechecks [Hard reject: candidate `b028e7ec4bbc` regressed the incumbent and failed field tolerances in both cases; do not rewrite oxygen-path geometry/reach math from cached deltas unless bit-for-bit equivalence to the incumbent formulas is proven.] (b028e7ec4bbc, weighted_median_pair_plus_kspace_seconds=71.716)
- [2026-04-12T06:24:25.073489Z] Cache PPPM TIP4P virtual-site reconstruction and H-index lookup once per local oxygen per compute, reusing it across particle_map, make_rho, fieldforce, per-atom, and slab correction [Correctness passed, but 94.369 is only a 0.37% improvement over 94.717 and misses the benchmark contract's 2% minimum improvement threshold.] (0157b0571dbb, weighted_median_pair_plus_kspace_seconds=94.369)
- [2026-04-12T08:02:01Z] Backport the serial OPT hot loop into base `lj/cut/tip4p/long` with branch-specialized `eval<>` paths and local `f[i]` accumulation [12.54% faster, but hard-rejected for large force and thermo deltas; do not reuse the OPT loop wholesale without proving identical TIP4P force/virial semantics.] (2bbd33a175f, weighted_median_pair_plus_kspace_seconds=82.844)
- [2026-04-12T08:02:55.766146Z] Backport the serial OPT hot loop into base lj/cut/tip4p/long so the benchmarked pair kernel uses branch-specialized eval paths and local f[i] accumulation. [Hard reject: despite a 12.54% speedup, candidate 2bbd33a175f failed TIP4P correctness with large force mismatches in both cases and broad thermo divergence in test-32r-long, so the baseline incumbent remains.; case test-32r-long etotal_series relative_delta exceeds threshold (8.92069e-05 > 1e-06); case test-32r-long pe_series relative_delta exceeds threshold (0.00218177 > 1e-06); case test-32r-long ke_series relative_delta exceeds threshold (0.00408985 > 1e-06); case test-32r-long temp_series relative_delta exceeds threshold (0.00408985 > 1e-06); case test-32r-long press_series relative_delta exceeds threshold (0.200262 > 1e-05); case test-32r-long forces_xyz abs_delta exceeds threshold (0.107617 > 1e-08); case test-32r-long forces_xyz rms_delta exceeds threshold (0.0276885 > 1e-08); case test-64r-short forces_xyz abs_delta exceeds threshold (1.44e-05 > 1e-08); case test-64r-short forces_xyz rms_delta exceeds threshold (1.44068e-06 > 1e-08)] (2bbd33a175f2, weighted_median_pair_plus_kspace_seconds=82.844)
- [2026-04-12T08:59:11Z] Cache local PPPM TIP4P M-site reconstruction and 1D rho stencils in `particle_map()` for reuse across `make_rho()`, fieldforce, per-atom, and slab correction [Correctness passed, but the change regressed the incumbent slightly (81.985 vs 81.885); avoid broad PPPM cache-state expansion unless profiling shows the saved recomputation clearly outweighs the extra bookkeeping.] (847eb8e5dd4b, weighted_median_pair_plus_kspace_seconds=81.985)
- [2026-04-12T09:00:43.453614Z] Cache local PPPM TIP4P M-site reconstruction and 1D rho stencils in particle_map() so make_rho(), fieldforce, per-atom, and slab correction reuse the cached state instead of recomputing find_M()/compute_rho1d() [Correctness passed, but 81.985 is 0.12% slower than incumbent 81.885 and misses the 2% improvement bar.] (847eb8e5dd4b, weighted_median_pair_plus_kspace_seconds=81.985)
- [2026-04-12T09:24:24Z] Skip the extended TIP4P Coulomb precheck for exact non-oxygen/non-oxygen pairs in `pair_lj_cut_tip4p_long` [Correctness passed and the candidate was 0.72% faster than the incumbent (81.298 vs 81.885), but the gain is too small to clear the 2% bar; do not spend another iteration on non-water precheck pruning alone unless it is bundled with a larger oxygen/M-site or PPPM reduction.] (42d987d04a9c, weighted_median_pair_plus_kspace_seconds=81.298)
- [2026-04-12T09:26:22.671744Z] Skip the extended TIP4P Coulomb precheck for exact non-oxygen/non-oxygen pairs in `pair_lj_cut_tip4p_long`, while keeping the incumbent oxygen/M-site path unchanged for any oxygen-containing interaction. [Correctness passed, but 81.298 is only a 0.72% improvement over incumbent 81.885 and misses the benchmark contract's 2% minimum improvement bar.] (42d987d04a9c, weighted_median_pair_plus_kspace_seconds=81.298)
- [2026-04-12T09:52:01Z] Specialize `pair_lj_cut_tip4p_long` for the benchmark's global-only tally path and defer non-water `f[i]` updates locally while also fast-pathing exact non-water/non-water Coulomb pairs [2.74% faster, but hard-rejected for large `test-32r-long` thermo/force deltas and `forces_xyz` mismatches in both cases; avoid tally or accumulation rewrites unless identical TIP4P force/virial semantics are proven.] (89aa52c5901e, weighted_median_pair_plus_kspace_seconds=79.639)
- [2026-04-12T09:53:32.781249Z] Specialize `pair_lj_cut_tip4p_long` for the benchmark’s global-only tally path, fast-path exact non-water/non-water Coulomb pairs, and locally accumulate non-water `f[i]` updates while preserving the incumbent TIP4P oxygen/M-site force distribution [Hard reject: candidate 89aa52c5901e is 2.74% faster than incumbent 3a45586c8afa but fails benchmark correctness, with broad test-32r-long thermo divergence and forces_xyz mismatches in both cases.; case test-32r-long etotal_series relative_delta exceeds threshold (8.42997e-05 > 1e-06); case test-32r-long pe_series relative_delta exceeds threshold (0.00183969 > 1e-06); case test-32r-long ke_series relative_delta exceeds threshold (0.00345568 > 1e-06); case test-32r-long temp_series relative_delta exceeds threshold (0.00345568 > 1e-06); case test-32r-long press_series relative_delta exceeds threshold (0.222002 > 1e-05); case test-32r-long forces_xyz abs_delta exceeds threshold (0.105529 > 1e-08); case test-32r-long forces_xyz rms_delta exceeds threshold (0.0282935 > 1e-08); case test-64r-short forces_xyz abs_delta exceeds threshold (8.3e-06 > 1e-08); case test-64r-short forces_xyz rms_delta exceeds threshold (1.10787e-06 > 1e-08)] (89aa52c5901e, weighted_median_pair_plus_kspace_seconds=79.639)
- [2026-04-12T10:22:36Z] Keep the exact H-H Coulomb cutoff fast path in `pair_lj_cut_tip4p_long` and reuse the first oxygen `find_M()` result plus constant `alpha`/slab factors in `PPPMTIP4P::fieldforce_ik()` / `fieldforce_ad()` [Correctness passed and the candidate was 0.54% faster than the incumbent (81.445 vs 81.885), but the gain is still too small to clear the 2% bar; treat this as a safe supporting optimization, not a standalone iteration target.] (c37498ebee61, weighted_median_pair_plus_kspace_seconds=81.445)
- [2026-04-12T10:24:43.678950Z] Keep the exact H-H Coulomb cutoff fast path in `pair_lj_cut_tip4p_long`, and reuse the first oxygen `find_M()` result in `PPPMTIP4P::fieldforce_ik()` / `fieldforce_ad()` while hoisting constant alpha/slab factors out of the inner oxygen force-distribution branch [Correctness passed, but candidate c37498ebee61 improves the incumbent by only 0.54% (81.885 to 81.445), missing the 2% acceptance bar.] (c37498ebee61, weighted_median_pair_plus_kspace_seconds=81.445)
- [2026-04-12T10:42:25Z] Specialize `pair_lj_cut_tip4p_long` Coulomb handling by interaction class (`O-O`, `O-H`, `H-O`, and exact non-oxygen/non-oxygen`) via a templated helper [Correctness passed, but the candidate regressed the incumbent by 4.83% (85.841 vs 81.885); avoid further code-size-heavy pair-kernel branch specialization unless profiling shows a clear branch bottleneck that outweighs instruction-footprint costs.] (7d859a5521df, weighted_median_pair_plus_kspace_seconds=85.841)
- [2026-04-12T10:44:10.582913Z] Specialize `pair_lj_cut_tip4p_long` Coulomb handling by interaction class (`O-O`, `O-H`, `H-O`, and exact non-oxygen/non-oxygen) via a templated helper, preserving incumbent TIP4P force distribution and `ev_tally_tip4p()` semantics while retaining the safe exact non-water cutoff pruning. [Correctness passed, but candidate 7d859a5521df regressed the incumbent primary metric from 81.885 to 85.841, missing the 2% improvement bar.] (7d859a5521df, weighted_median_pair_plus_kspace_seconds=85.841)
- [2026-04-12T11:05:20.024383403Z] Tighten `pair_lj_cut_tip4p_long` Coulomb prechecks only for exact `H-H` and actual-current-step `O(i)-H(j)` reach tests, while keeping all oxygen-`j` paths plus the incumbent TIP4P force/tally write order unchanged. [Correctness passed, and the candidate improved the incumbent from 81.885 to 80.674, but that 1.48% gain still misses the 2% bar; treat this exact `H-H`/`O(i)-H(j)` precheck tightening as a safe supporting tweak rather than a standalone iteration target.] (0356a6e5d35a, weighted_median_pair_plus_kspace_seconds=80.674)
- [2026-04-12T11:09:23.683192Z] Tighten `pair_lj_cut_tip4p_long` Coulomb prechecks only for exact `H-H` and actual-current-step `O(i)-H(j)` reach tests, while keeping all oxygen-`j` paths plus the incumbent TIP4P force/tally write order unchanged. [Correctness passed, but 80.674 is only a 1.48% improvement over incumbent 81.885 and misses the 2% acceptance bar.] (0356a6e5d35a, weighted_median_pair_plus_kspace_seconds=80.674)
- [2026-04-12T11:24:59Z] Tighten exact oxygen-i/non-oxygen-j pair Coulomb prechecks in `pair_lj_cut_tip4p_long` and reuse the first PPPM TIP4P oxygen M-site mapping plus hoisted alpha/slab constants in `fieldforce_ik()` / `fieldforce_ad()` [Correctness passed and the candidate was 1.19% faster than the incumbent (80.914 vs 81.885), but the gain still misses the 2% bar; combining safe pair-precheck pruning with small PPPM `fieldforce` hoists is not enough on its own, so future wins need a higher-leverage oxygen/M-site or deeper PPPM reduction.] (25819367f233, weighted_median_pair_plus_kspace_seconds=80.914)
- [2026-04-12T11:27:49.530814Z] Tighten exact oxygen-i/non-oxygen-j pair Coulomb prechecks in `pair_lj_cut_tip4p_long` and reuse the first PPPM TIP4P oxygen M-site mapping plus hoisted alpha/slab constants in `fieldforce_ik()` and `fieldforce_ad()` [Correctness passed, but 80.914 is only a 1.19% improvement over incumbent 81.885, below the 2% acceptance bar.] (25819367f233, weighted_median_pair_plus_kspace_seconds=80.914)
- [2026-04-12T12:14:55Z] Prune impossible hydrogen-neighbor LJ work and reuse cached oxygen M-sites to tighten reverse nonoxygen-to-oxygen TIP4P Coulomb prechecks in `pair_lj_cut_tip4p_long` [Correctness passed, but the candidate regressed the incumbent by 3.34% (82.549 vs 79.88); avoid more reverse nonoxygen-to-oxygen precheck tightening or cached-oxygen-site screening tweaks in the pair loop unless profiling shows a materially larger win than the added branch/math overhead.] (4f3910700624, weighted_median_pair_plus_kspace_seconds=82.549)
- [2026-04-12T12:16:29.265704Z] Prune impossible hydrogen-neighbor LJ work and reuse cached oxygen M-sites to tighten reverse nonoxygen-to-oxygen TIP4P Coulomb prechecks in pair_lj_cut_tip4p_long [Correctness passed, but candidate 4f3910700624 regressed weighted_median_pair_plus_kspace_seconds from 79.88 to 82.549 versus incumbent 2a0b902a8b5a.] (4f3910700624, weighted_median_pair_plus_kspace_seconds=82.549)
- [2026-04-12T12:38:09Z] candidate `c467f241b716` rejected as non-incumbent despite passing correctness.
  Hypothesis: skipping dead water-H Lennard-Jones work inside `pair_lj_cut_tip4p_long` while caching TIP4P oxygen `find_M()` / hydrogen-index results across PPPM `particle_map()`, `make_rho()`, and `fieldforce_ik()` would safely stack pair-side pruning with lower-overhead PPPM oxygen bookkeeping into an acceptance-grade pair+kspace reduction.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 79.88 to 78.33, a 1.94% reduction versus the incumbent, with both benchmark cases converged, `correctness_ok=true`, and no guardrail errors. Case-level pair-plus-kspace timings were 78.33 s for `test-32r-long` and 5.3352 s for `test-64r-short`; this is the best correctness-preserving result since the incumbent, but it still misses the 2% acceptance bar by 0.0476 s on the weighted median, so `2a0b902a8b5a` remains the incumbent.
  Lesson learned: this confirms that dead water-H LJ pruning and cross-phase reuse of oxygen `find_M()` / H-index results are scientifically safe and do compose into a real gain. However, the near-miss despite substantial PPPM cache plumbing means the remaining benchmark cost is not dominated by those lookups alone, and broader cache state can add enough memory traffic/bookkeeping overhead to blunt the benefit.
  Next hypothesis: stay on the PPPM side, but aim for a lower-overhead oxygen-path reduction than this full cache plumbing: reuse already-available oxygen mapping/stencil state more directly inside the IK hot path, or remove repeated per-oxygen indexing/math after `particle_map()` without introducing more large cache arrays. Treat the dead water-H LJ skip as a supporting optimization rather than the main lever.
- [2026-04-12T12:39:39.653840Z] Skip dead water-H LJ work in `pair_lj_cut_tip4p_long` and cache PPPM TIP4P oxygen `find_M()` / H-index results across `particle_map()`, `make_rho()`, and `fieldforce_ik()`. [Correctness passed, but 78.33 improves incumbent 79.88 by only 1.94%, missing the benchmark contract’s 2% acceptance bar.] (c467f241b716, weighted_median_pair_plus_kspace_seconds=78.33)
- [2026-04-12T12:57:18Z] candidate `b11c640621e48ee223768704bd744c8e8cb29952` rejected as non-incumbent despite passing correctness.
  Hypothesis: keeping the safe dead hydrogen-neighbor LJ pruning in `pair_lj_cut_tip4p_long` while replacing the heavier PPPM full oxygen-site cache with a lighter per-step hydrogen-index cache plus on-demand M-site reconstruction would preserve most of iteration 13's gain, cut PPPM cache bookkeeping/memory traffic, and finally clear the 2% acceptance bar.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 79.88 to 78.535, a 1.68% reduction versus the incumbent, with both benchmark cases converged, `correctness_ok=true`, and no guardrail errors. Case-level pair-plus-kspace timings were 78.34 s for `test-32r-long` and 5.35 s for `test-64r-short`. This remains below the 2% acceptance threshold and is also slightly slower than iteration 13's fuller PPPM cache candidate (78.33), so `2a0b902a8b5a` stays incumbent.
  Lesson learned: the lighter PPPM bookkeeping strategy is scientifically safe and still materially faster than the incumbent, but recomputing the M site on demand gives back more performance than the reduced cache state saves. That means the current PPPM oxygen-path cost is sensitive to repeated reconstruction work, and an H-index-only cache is not enough to beat the contract by itself even when combined with the safe dead hydrogen-neighbor LJ skip.
  Next hypothesis: treat the dead hydrogen-neighbor LJ pruning as a supporting optimization only, and target the next PPPM experiment at reusing already-available oxygen mapping, stencil, or M-site state deeper in `make_rho()` / `fieldforce_*()` without either the full `xM`-style cache footprint or repeated on-demand reconstruction. If that lighter-weight reuse cannot outperform iteration 13, shift attention to a different high-leverage pair or PPPM hotspot instead of iterating further on this cache-shape tradeoff.
- [2026-04-12T12:57:18Z] Skip dead hydrogen-neighbor LJ work in `pair_lj_cut_tip4p_long` and replace the heavier PPPM full oxygen-site cache with lightweight per-step cached H indices plus on-demand M-site reconstruction in `particle_map()`, `make_rho()`, and the TIP4P fieldforce paths. [Correctness passed, but 78.535 improves incumbent 79.88 by only 1.68%, missing the benchmark contract's 2% acceptance bar.] (b11c640621e48ee223768704bd744c8e8cb29952, weighted_median_pair_plus_kspace_seconds=78.535)
- [2026-04-12T12:58:58.392601Z] Skip dead hydrogen-neighbor LJ work in `pair_lj_cut_tip4p_long` and replace the heavier PPPM full oxygen-site cache with lightweight per-step cached H indices plus on-demand M-site reconstruction in `particle_map()`, `make_rho()`, and the TIP4P fieldforce paths. [Correctness passed, but candidate b11c640621e48ee223768704bd744c8e8cb29952 improves the incumbent by only 1.68% (79.88 to 78.535), below the benchmark contract’s 2% acceptance bar.] (b11c640621e4, weighted_median_pair_plus_kspace_seconds=78.535)
- [2026-04-12T13:18:34Z] candidate `48e378f90ae888cfac572d0819ff75a25bdf5ffb` rejected as non-incumbent despite passing correctness.
  Hypothesis: an IK-only PPPM TIP4P oxygen cache that stores per-step hydrogen indices, lower-left grid deltas, and `rho1d` stencil weights in `particle_map()` and reuses them in `make_rho()` / `fieldforce_ik()` would remove enough repeated oxygen-path reconstruction and stencil work to deliver an acceptance-grade kspace reduction without touching pair, AD, per-atom, or slab code paths.
  Changed files: `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 79.88 to 79.58, a 0.38% reduction versus the incumbent, with both benchmark cases converged, `correctness_ok=true`, and no guardrail errors. Case-level pair-plus-kspace timings were 79.567 s for `test-32r-long` and 5.4047 s for `test-64r-short`; peak RSS was 158.59 MiB. This is directionally positive but far below the 2% acceptance threshold, so incumbent `2a0b902a8b5a` remains in place.
  Lesson learned: PPPM-side reuse of hydrogen indices, grid deltas, and precomputed IK stencils is scientifically safe, but this cache shape is too low leverage on its own. Compared with the stronger pair+PPPM near-miss candidates from iterations 13 and 14, the tiny standalone gain here indicates that the benchmark is not dominated by the recomputation removed by this IK-only cache, and extra cache bookkeeping or memory traffic limits the payoff.
  Next hypothesis: if PPPM work is revisited, keep it focused on lighter-weight reuse of already-available oxygen mapping or stencil state deeper in `make_rho()` / `fieldforce_ik()` without adding large per-atom cache arrays; otherwise shift the next experiment to a different higher-leverage pair or kspace hotspot, because PPPM-only oxygen caching is not a strong enough primary lever to clear the contract.
- [2026-04-12T13:18:34Z] Add an IK-only PPPM TIP4P oxygen cache that stores per-step H indices, lower-left grid deltas, and rho1d stencil weights in `particle_map()` and reuses them in `make_rho()` and `fieldforce_ik()` while leaving pair, AD, per-atom, and slab paths unchanged. [Correctness passed, but candidate 48e378f90ae888cfac572d0819ff75a25bdf5ffb improves the incumbent by only 0.38% (79.88 to 79.58), below the benchmark contract’s 2% acceptance bar.] (48e378f90ae888cfac572d0819ff75a25bdf5ffb, weighted_median_pair_plus_kspace_seconds=79.58)
- [2026-04-12T13:20:29.771114Z] Add an IK-only PPPM TIP4P oxygen cache that stores per-step H indices, lower-left grid deltas, and rho1d stencil weights in `particle_map()` and reuses them in `make_rho()` and `fieldforce_ik()` while leaving pair, AD, per-atom, and slab paths unchanged. [Correct and slightly faster (79.58 vs 79.88), but the 0.38% gain misses the benchmark contract’s 2% acceptance bar.] (48e378f90ae8, weighted_median_pair_plus_kspace_seconds=79.58)
- [2026-04-12T14:11:05.492168Z] Generation-stamped TIP4P pair/PPPM caches plus a common non-special pair fast path and cross-step PPPM oxygen-H mapping reuse between reneighbor steps [Correctness passed, but 77.512 improves incumbent 78.12 by only 0.78%, below the benchmark contract’s 2% acceptance bar.] (ad7811dc14f3, weighted_median_pair_plus_kspace_seconds=77.512)
- [2026-04-12T14:28:31Z] iteration 18 review: rejected candidate b87604e6465e (weighted_median_pair_plus_kspace_seconds=77.755, correctness OK, both cases converged) because the 0.47% improvement versus incumbent `ab55ea3e8508` missed the contract's 2% minimum relative improvement bar.
- [2026-04-12T14:28:31Z] candidate `b87604e6465ee77e5ab59afb7de46267a5c6485b` rejected as non-incumbent despite passing correctness.
  Hypothesis: replacing the per-step `hneigh[][2]` reset with generation-stamped cached M-site bookkeeping and adding a common non-special-neighbor fast path in `pair_lj_cut_tip4p_long` would trim pair-side bookkeeping and dominant `sb == 0` inner-loop overhead enough to produce another acceptance-grade gain while leaving PPPM unchanged.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from 78.12 to 77.755, a 0.47% reduction versus the incumbent and a 17.91% reduction versus baseline, but that still misses the 2% acceptance threshold. Both benchmark cases converged, `correctness_ok=true`, and no guardrail errors were reported. Case-level pair-plus-kspace timings were 77.755 s for `test-32r-long` and 5.2969 s for `test-64r-short`; peak RSS was 158.60 MiB. This is directionally positive, but `ab55ea3e8508` remains the incumbent.
  Lesson learned: this confirms the pair-only generation-stamped TIP4P site cache and common non-special fast path are scientifically safe and do reduce overhead, but only at sub-1% scale on top of the accepted iteration 16 design. Relative to iteration 17's slightly stronger 77.512 result, the pair-side portion of that broader experiment accounts for only part of the gain, so additional wins are unlikely to come from more `hneigh`-lifetime or `special_*`-load micro-optimizations alone.
  Next hypothesis: stop spending full iterations on pair-only bookkeeping micro-tweaks in `pair_lj_cut_tip4p_long`. The next candidate should target a higher-leverage reduction that composes with incumbent `ab55ea3e8508`, most likely deeper PPPM oxygen-path math/indexing cleanup or another semantics-preserving hot-path reduction that removes more work than the current cache-lifetime and common-case branch cleanups.
- [2026-04-12T14:29:49.534368Z] Pair-only TIP4P optimization: replace the per-step `hneigh[][2]` reset with generation-stamped cached M-site bookkeeping and add a common non-special-neighbor fast path in `pair_lj_cut_tip4p_long`, leaving PPPM unchanged. [Correctness passed, but 77.755 improves incumbent 78.12 by only 0.47%, below the 2% acceptance bar.] (b87604e6465e, weighted_median_pair_plus_kspace_seconds=77.755)
- [2026-04-12T14:52:19Z] iteration 19 review: rejected candidate `1478ab81846a` (weighted_median_pair_plus_kspace_seconds=78.069, benchmark runs converged) because the benchmark contract reported `hard_reject=true` for field-tolerance correctness failures despite the slight runtime win.
- [2026-04-12T14:52:19Z] candidate `1478ab81846a3ca770c796c9d2ec5279593fa96e` rejected for correctness failure.
  Hypothesis: buffering hydrogen-`i` Coulomb `f[i]` accumulation in `pair_lj_cut_tip4p_long` and flushing it once after the neighbor loop would reduce repeated force-array stores on the non-water `i` path while preserving the incumbent oxygen and TIP4P redistribution write order.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved slightly from 78.12 to 78.069, a 0.07% reduction versus incumbent `ab55ea3e8508` and a 17.58% reduction versus baseline. Both benchmark cases converged and run status was `ok`; case-level pair-plus-kspace timings were 77.92 s for `test-32r-long` and 5.2806 s for `test-64r-short`, with peak RSS 158.78 MiB. However, authoritative field-tolerance correctness failed hard: `test-32r-long` exceeded `etotal_series`, `pe_series`, `ke_series`, `temp_series`, `press_series`, and `forces_xyz` (abs delta 0.105529, RMS delta 0.0282935), while `test-64r-short` also failed `forces_xyz` (abs delta 8.3e-06, RMS delta 1.10787e-06). With `hard_reject=true`, this candidate cannot become the incumbent, so `ab55ea3e8508` remains in place.
  Lesson learned: even with the incumbent oxygen and TIP4P redistribution writes left untouched, deferring hydrogen-side direct Coulomb `f[i]` updates to the end of the neighbor loop is not semantics-preserving. This reproduces the same hard-failure signature seen in iteration 6, so the pair kernel is sensitive not only to oxygen/TIP4P write order but also to immediate per-interaction force-update and tally sequencing on hydrogen/non-water `i` paths.
  Next hypothesis: avoid any further local buffering or flush-later scheme for `f[i]` in `pair_lj_cut_tip4p_long`, including hydrogen-only variants. Future candidates should keep immediate in-loop force updates and instead target higher-leverage semantics-preserving work such as deeper PPPM oxygen-path math/indexing cleanup or pair-side pruning that removes work without altering force-accumulation timing, virial/tally behavior, or write ordering.
- [2026-04-12T14:52:19Z] Buffer hydrogen-`i` Coulomb force accumulation in `pair_lj_cut_tip4p_long` and flush once after the neighbor loop while preserving the incumbent oxygen and TIP4P redistribution write order. [Hard reject: despite a 0.07% speedup (78.069 vs 78.12), the candidate failed field-tolerance correctness with broad `test-32r-long` thermo divergence and `forces_xyz` mismatches in both cases; avoid deferred `f[i]` accumulation, including hydrogen-only buffering.] (1478ab81846a, weighted_median_pair_plus_kspace_seconds=78.069)
- [2026-04-12T14:53:28.582548Z] Buffer hydrogen-i Coulomb force accumulation in pair_lj_cut_tip4p_long and flush once after the neighbor loop while preserving the incumbent oxygen and TIP4P redistribution write order [Hard reject: candidate 1478ab81846a is only 0.07% faster than incumbent ab55ea3e8508 but fails authoritative field-tolerance correctness in both cases.; case test-32r-long etotal_series relative_delta exceeds threshold (8.42997e-05 > 1e-06); case test-32r-long pe_series relative_delta exceeds threshold (0.00183969 > 1e-06); case test-32r-long ke_series relative_delta exceeds threshold (0.00345568 > 1e-06); case test-32r-long temp_series relative_delta exceeds threshold (0.00345568 > 1e-06); case test-32r-long press_series relative_delta exceeds threshold (0.222002 > 1e-05); case test-32r-long forces_xyz abs_delta exceeds threshold (0.105529 > 1e-08); case test-32r-long forces_xyz rms_delta exceeds threshold (0.0282935 > 1e-08); case test-64r-short forces_xyz abs_delta exceeds threshold (8.3e-06 > 1e-08); case test-64r-short forces_xyz rms_delta exceeds threshold (1.10787e-06 > 1e-08)] (1478ab81846a, weighted_median_pair_plus_kspace_seconds=78.069)
- [2026-04-12T23:23:20.739271Z] Cache all-charge PPPM/TIP4P IK rho stencils and base brick indices in make_rho() and reuse them in fieldforce_ik() while retaining the incumbent oxygen M/H cache [Correctness passed, but 78.045 is only a 0.10% improvement over incumbent 78.12 and misses the benchmark contract’s 2% acceptance bar.] (57da41d1ad04, weighted_median_pair_plus_kspace_seconds=78.045)
- [2026-04-13T00:06:02.800028Z] Reuse the pair style's current-step TIP4P O/H/M cache inside PPPM with a timestep guard and orthogonal-box fallback to the incumbent PPPM find_M cache [Correctness passed, but 76.144 is only a 0.14% improvement over incumbent 2599e1f9f4c8’s 76.248, well below the contract’s 2% acceptance bar.] (22ab6a7a53b5, weighted_median_pair_plus_kspace_seconds=76.144)
- [2026-04-13T00:32:22.997554Z] Merge the correctness-clean pair-only generation-stamped TIP4P site cache and common non-special-neighbor fast path into the current alias-view `pair_lj_cut_tip4p_long` incumbent [Correctness passed, but 75.536 improves incumbent 2599e1f9f4c8 by only 0.93%, below the 2% acceptance bar; keep this pair-only cache/common-case cleanup as a safe supporting optimization to bundle with a higher-leverage in-kernel reduction rather than spending another standalone iteration on it.] (bd8ee0840992, weighted_median_pair_plus_kspace_seconds=75.536)
- [2026-04-13T00:35:02.234641Z] Merge the correctness-clean pair-only generation-stamped TIP4P site cache and common non-special-neighbor fast path into the current alias-view `pair_lj_cut_tip4p_long` incumbent [Correctness passed, but 75.536 improves incumbent 76.248 by only 0.93%, below the contract’s 2% acceptance bar.] (bd8ee0840992, weighted_median_pair_plus_kspace_seconds=75.536)
- [2026-04-13T00:55:36Z] iteration 24 review: rejected candidate `a4391bb48f95` (weighted_median_pair_plus_kspace_seconds=79.984, correctness OK, both cases converged) because the order-5 PPPM stencil specialization regressed incumbent `2599e1f9f4c8` by 4.90% and therefore misses the contract’s required 2% improvement bar by a wide margin.
- [2026-04-13T00:55:36Z] candidate `a4391bb48f95b34359bd5614cbaa5613ba7945df` rejected as non-incumbent despite passing correctness.
  Hypothesis: specializing the TIP4P PPPM order-5 `make_rho()` and `fieldforce_ik()` stencil kernels, while retaining a generic fallback for other orders, would remove enough loop overhead in the dominant order-5 IK path to compound with incumbent alias-friendly views and produce another acceptance-grade PPPM win.
  Changed files: `src/KSPACE/pppm_tip4p.cpp`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` regressed from incumbent `2599e1f9f4c8`’s 76.248 to 79.984, making the candidate 4.90% slower than the incumbent even though it remains 15.55% better than baseline `e7c0ed95a333`’s 94.717. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 158.45 MiB. Case-level pair-plus-kspace timings were 79.833 s for `test-32r-long` and 5.3853 s for `test-64r-short`, so this candidate does not become the new incumbent and `2599e1f9f4c8` remains in place.
  Lesson learned: direct order-5 stencil specialization in `pppm_tip4p.cpp` is scientifically safe but not performance-positive on top of the current alias-view incumbent. The extra dedicated kernels and fallback structure likely increase instruction footprint and/or interfere with the compiler’s optimization of the incumbent generic order loop enough to outweigh the reduced loop-control work. For this benchmark, PPPM wins are not coming from cloning the order-5 rho/interpolation stencil bodies in isolation.
  Next hypothesis: avoid further standalone experiments that only unroll or duplicate the order-5 PPPM stencil loops behind a generic fallback. The next candidate should target a lower-overhead reduction that keeps one hot implementation path, such as reducing shared memory traffic or index arithmetic across `particle_map()` / `make_rho()` / `fieldforce_ik()`, or bundling the previously safe sub-1% pair-side cleanup with a more leveraged PPPM simplification that removes work without expanding hot-path code size.
- [2026-04-13T00:57:24.707435Z] Specialize TIP4P PPPM order-5 make_rho()/fieldforce_ik() stencil kernels with a generic fallback [Correctness passed, but the order-5 PPPM stencil specialization regressed the incumbent by 4.90%, far short of the 2% improvement bar.] (a4391bb48f95, weighted_median_pair_plus_kspace_seconds=79.984)
- [2026-04-13T01:18:28Z] iteration 25 review: rejected candidate `757c16f42b32` (weighted_median_pair_plus_kspace_seconds=75.36, correctness OK, both cases converged) because the 1.16% improvement versus incumbent `2599e1f9f4c8` missed the contract's 2% minimum relative improvement bar despite setting the best correctness-clean raw metric so far.
- [2026-04-13T01:18:28Z] candidate `757c16f42b32166e7b84537b48cbee7daa86c978` rejected as non-incumbent despite passing correctness.
  Hypothesis: adding a pure-water `lj/cut/tip4p/long` pair fast path, reusing generation-stamped TIP4P M-sites, and collapsing common `sb == 0` handling in `pair_lj_cut_tip4p_long` would cut dominant pure-water pair overhead on top of incumbent `2599e1f9f4c8` enough to clear the acceptance bar without touching PPPM semantics.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `2599e1f9f4c8`'s 76.248 to 75.36, a 1.16% reduction versus the incumbent and a 20.44% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 157.52 MiB. Case-level pair-plus-kspace timings were 75.36 s for `test-32r-long` and 5.1162 s for `test-64r-short`. This is the best correctness-preserving raw metric seen so far, but it still misses the contract's 2% improvement threshold, so `2599e1f9f4c8` remains the incumbent.
  Lesson learned: pair-side pure-water specialization plus safe generation-stamped M-site reuse is scientifically clean and more effective than the earlier standalone pair-only cache/common-case cleanup from iteration 23, but it still tops out around a 1% gain on top of the alias-view incumbent. That suggests pair-loop bookkeeping and common `sb == 0` handling are no longer dominant bottlenecks, so more standalone pair micro-specialization is unlikely to produce an acceptance-grade jump by itself.
  Next hypothesis: treat the pure-water fast path and generation-stamped M-site reuse as safe supporting ingredients to bundle with a higher-leverage change, most likely a PPPM-side reduction or a broader oxygen-path math/indexing simplification that removes more total work without changing force/tally/write-order semantics or inflating hot-path code size too aggressively.
- [2026-04-13T01:18:28Z] Pure-water `lj/cut/tip4p/long` pair fast path plus generation-stamped TIP4P M-site reuse and common `sb == 0` handling in `pair_lj_cut_tip4p_long` [Correctness passed, but 75.36 improves incumbent 76.248 by only 1.16%, below the 2% acceptance bar.] (757c16f42b32, weighted_median_pair_plus_kspace_seconds=75.36)
- [2026-04-13T01:19:47.412439Z] Pure-water `lj/cut/tip4p/long` pair fast path plus generation-stamped TIP4P M-site reuse and common `sb == 0` handling in `pair_lj_cut_tip4p_long` [Correctness passed, but 75.36 improves incumbent 76.248 by only 1.16%, below the 2% acceptance bar.] (757c16f42b32, weighted_median_pair_plus_kspace_seconds=75.36)
- [2026-04-13T01:51:09Z] iteration 26 review: accepted candidate `4cbc0fa85603` (weighted_median_pair_plus_kspace_seconds=72.919, correctness OK, both cases converged) because it improved incumbent `2599e1f9f4c8` by 4.37%, clearing the contract's 2% minimum relative improvement bar without guardrail errors.
- [2026-04-13T01:51:09Z] candidate `4cbc0fa856036c692930cd76775c671201e183cc` accepted as the new incumbent.
  Hypothesis: combining contiguous generation-stamped TIP4P pair/PPPM caches with the pure-water `lj/cut/tip4p/long` pair fast path, while keeping exact oxygen-site cutoff tests and the incumbent force/tally/write ordering intact, would let the previously safe sub-2% ingredients compound into an acceptance-grade win on top of incumbent `2599e1f9f4c8`.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `2599e1f9f4c8`'s 76.248 to 72.919, a 4.37% reduction versus the incumbent and a 23.01% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, and `guardrail_errors=[]`, with peak RSS 158.30 MiB. Case-level pair-plus-kspace timings were 73.125 s for `test-32r-long` and 4.9984 s for `test-64r-short`; in the long case, pair time dropped to 52.843 s and kspace time to 20.282 s. This comfortably clears the acceptance threshold, so `4cbc0fa85603` becomes the new incumbent.
  Lesson learned: the pair-only pure-water fast path and generation-stamped TIP4P cache work were not exhausted as isolated ideas; they became acceptance-grade once paired with coordinated contiguous cache/layout cleanup across both pair and PPPM. The gain is broad rather than narrowly pair-only, with long-case pair and kspace both improving versus iteration 21, which indicates the current bottleneck still rewards composed in-kernel access/bookkeeping reductions as long as exact cutoff logic and immediate force/tally sequencing stay unchanged.
  Next hypothesis: build on incumbent `4cbc0fa85603` with another semantics-preserving composed reduction rather than a standalone micro-tweak. The next candidate should target remaining oxygen-heavy pair or PPPM hot paths that still pay repeated index/math overhead inside the current pure-water and TIP4P cache design, while explicitly avoiding deferred force accumulation, altered tally/write ordering, or large code-size-heavy PPPM stencil duplication.
- [2026-04-13T02:13:44Z] iteration 27 review: accepted candidate `5840dac0d9f1` (weighted_median_pair_plus_kspace_seconds=70.984, correctness OK, both cases converged) because it improved incumbent `4cbc0fa85603` by 2.65%, clearing the contract's 2% minimum relative improvement bar without guardrail errors.
- [2026-04-13T02:13:44Z] candidate `5840dac0d9f14f3d7eb7d862f91d66ae209efef3` accepted as the new incumbent.
  Hypothesis: inlining the generation-stamped TIP4P cache-hit fast paths in `pair_lj_cut_tip4p_long` and `pppm_tip4p`, while pushing cache-miss reconstruction into dedicated out-of-line slow helpers, would shrink hot-path control flow enough to produce another acceptance-grade win on top of incumbent `4cbc0fa85603` without changing any scientifically sensitive force, cutoff, or tally behavior.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `4cbc0fa85603`'s 72.919 to 70.984, a 2.65% reduction versus the incumbent and a 25.06% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, and `guardrail_errors=[]`, with peak RSS 158.63 MiB. Case-level pair-plus-kspace timings were 71.026 s for `test-32r-long` and 4.8557 s for `test-64r-short`; in the long case, pair time dropped to 50.855 s and kspace time to 20.171 s. This clears the acceptance threshold, so `5840dac0d9f1` becomes the new incumbent.
  Lesson learned: after iteration 26 established that contiguous generation-stamped caches plus the pure-water fast path were valuable, there was still measurable overhead in the cache-hit path itself. Keeping miss reconstruction available but out of line is scientifically safe and materially faster, which implies the remaining headroom is now coming from reducing hot-path call/branch footprint and repeated cache-hit bookkeeping rather than inventing broader cache structures. The gain is strongest on the pair side but still positive in PPPM, so the shared theme remains hot-path simplification inside the existing cache design.
  Next hypothesis: build on incumbent `5840dac0d9f1` by targeting the remaining cache-hit overhead and repeated oxygen-path math/indexing inside the current generation-stamped pair/PPPM design, especially places where rare miss handling or helper layering still pollutes the hot path. Keep pursuing semantics-preserving in-kernel reductions, and continue to avoid deferred force accumulation, altered cutoff or tally ordering, new cross-kernel cache coupling, or code-size-heavy PPPM stencil cloning.
- [2026-04-13T02:36:04Z] iteration 28 review: rejected candidate `aaa3d5fa36f1` (weighted_median_pair_plus_kspace_seconds=71.085, correctness OK, both cases converged) because it regressed incumbent `5840dac0d9f1` by 0.14% and therefore missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T02:36:04Z] candidate `aaa3d5fa36f1e36cf221153043c697991cbef070` rejected as non-incumbent despite passing correctness.
  Hypothesis: removing the unconditional `particle_map()` allreduce and specializing the oxygen/non-oxygen plus z-force hot paths in `particle_map()`, `make_rho()`, and `fieldforce_ik()` would cut remaining PPPMTIP4P cache-hit/control-flow overhead enough to produce another acceptance-grade win on top of incumbent `5840dac0d9f1` without changing atom traversal or TIP4P force distribution.
  Changed files: `src/KSPACE/pppm_tip4p.cpp`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` regressed from incumbent `5840dac0d9f1`'s 70.984 to 71.085, making the candidate 0.14% slower than the incumbent even though it remains 24.95% better than baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 158.70 MiB. Case-level pair-plus-kspace timings were 70.919 s for `test-32r-long` and 4.8443 s for `test-64r-short`; in the long case, pair time was 50.898 s and kspace time was 20.021 s. This does not clear the acceptance threshold, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: on top of the incumbent's inlined cache-hit fast paths, standalone PPPMTIP4P micro-optimizations that remove a small communication step and add more oxygen/non-oxygen or z-force specialization are too low leverage and can lose to the added control-flow or code-shape cost. The kspace-side savings visible in the reported long-case split were not enough to improve the authoritative aggregate primary metric, which suggests the remaining PPPM bottleneck is no longer the unconditional `particle_map()` allreduce or the unspecialized inner-path structure by itself.
  Next hypothesis: avoid further standalone PPPMTIP4P branch-specialization or allreduce-elision experiments on top of incumbent `5840dac0d9f1`. The next candidate should target a larger composed reduction that keeps one lean cache-hit path and removes repeated oxygen-path indexing/math end-to-end inside the existing generation-stamped design; if PPPM-only ideas continue to stay sub-noise, shift back to a coordinated pair+PPPM hot-path simplification rather than another isolated PPPM micro-tweak.
- [2026-04-13T02:36:04Z] Streamline PPPMTIP4P by removing the unconditional `particle_map()` allreduce and specializing the oxygen/non-oxygen plus z-force hot paths in `particle_map()`, `make_rho()`, and `fieldforce_ik()` without changing atom traversal or TIP4P force distribution [Correctness passed, but 71.085 regressed incumbent 70.984 by 0.14%, so the candidate misses the 2% acceptance bar.] (aaa3d5fa36f1, weighted_median_pair_plus_kspace_seconds=71.085)
- [2026-04-13T02:37:41.519068Z] Streamline PPPMTIP4P by removing the unconditional particle_map() allreduce and specializing the oxygen/non-oxygen plus z-force hot paths in particle_map(), make_rho(), and fieldforce_ik() without changing atom traversal or TIP4P force distribution [Correctness passed, but candidate aaa3d5fa36f1 regressed the incumbent primary metric from 70.984 to 71.085 and missed the 2% improvement bar.] (aaa3d5fa36f1, weighted_median_pair_plus_kspace_seconds=71.085)
- [2026-04-13T02:59:23Z] iteration 29 review: rejected candidate `b9d56dcc532a` (weighted_median_pair_plus_kspace_seconds=70.525, correctness OK, both cases converged) because the 0.65% improvement versus incumbent `5840dac0d9f1` missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T02:59:23Z] candidate `b9d56dcc532ad104e00d2d322ed2be5a8d108eb8` rejected as non-incumbent despite passing correctness.
  Hypothesis: using typed alias-friendly cached TIP4P M-site views in `pair_lj_cut_tip4p_long` and `pppm_tip4p`, while keeping the incumbent cache-miss reconstruction and exact force/tally ordering unchanged, would trim remaining cache-hit access overhead enough to produce another acceptance-grade win on top of incumbent `5840dac0d9f1`.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `5840dac0d9f1`'s 70.984 to 70.525, a 0.65% reduction versus the incumbent and a 25.54% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 157.83 MiB. Case-level pair-plus-kspace timings were 70.525 s for `test-32r-long` and 4.8407 s for `test-64r-short`; in the long case, pair time dropped to 50.44 s and kspace time to 20.085 s. This is directionally positive, but it does not clear the contract's 2% acceptance threshold, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: typed alias-friendly cache-hit views are scientifically safe and do shave a small amount of overhead from the current pair and PPPM hot paths, but representation-only access cleanup is now too low leverage by itself. The modest pair and kspace improvements suggest the incumbent already captured most of the pointer-chasing and control-flow win, so the remaining bottleneck is not dominated by raw cached M-site coordinate access alone.
  Next hypothesis: avoid another standalone iteration that only repackages cached TIP4P state behind typed views or helper accessors. The next candidate should bundle the proven-safe alias-friendly access style with a larger composed reduction that removes repeated oxygen-path math/indexing across the current pair+PPPM cache-hit path, while continuing to avoid new cross-kernel cache sharing, deferred force updates, altered TIP4P force/tally ordering, or branch-splitting-heavy PPPM restructures.
- [2026-04-13T02:59:23Z] Use typed alias-friendly cached TIP4P M-site views in `pair_lj_cut_tip4p_long` and `pppm_tip4p` while keeping the incumbent cache-miss reconstruction and exact force/tally ordering unchanged [Correctness passed, but 70.525 improves incumbent 70.984 by only 0.65%, below the 2% acceptance bar.] (b9d56dcc532a, weighted_median_pair_plus_kspace_seconds=70.525)
- [2026-04-13T03:02:29.800765Z] Use typed alias-friendly cached TIP4P M-site views in pair_lj_cut_tip4p_long and pppm_tip4p while keeping the incumbent cache-miss reconstruction and exact force/tally ordering unchanged [Correctness passed, but candidate b9d56dcc532a improves the incumbent by only 0.65% (70.984 to 70.525), below the 2% acceptance bar.] (b9d56dcc532a, weighted_median_pair_plus_kspace_seconds=70.525)
- [2026-04-13T03:25:44.822843Z] Cache per-step TIP4P O->M deltas/reach in pair_lj_cut_tip4p_long and reuse them to form exact oxygen-path Coulomb separations plus tighter cached-oxygen prechecks without changing force redistribution or tally order [Hard correctness failure in both benchmark cases, and the candidate also regressed the incumbent by 1.03%.; case test-32r-long etotal_series relative_delta exceeds threshold (8.74878e-05 > 1e-06); case test-32r-long pe_series relative_delta exceeds threshold (0.00235329 > 1e-06); case test-32r-long ke_series relative_delta exceeds threshold (0.00440903 > 1e-06); case test-32r-long temp_series relative_delta exceeds threshold (0.00440903 > 1e-06); case test-32r-long press_series relative_delta exceeds threshold (0.253437 > 1e-05); case test-32r-long forces_xyz abs_delta exceeds threshold (0.118184 > 1e-08); case test-32r-long forces_xyz rms_delta exceeds threshold (0.0291073 > 1e-08); case test-64r-short etotal_series relative_delta exceeds threshold (1.18848e-06 > 1e-06); case test-64r-short pe_series relative_delta exceeds threshold (3.56251e-05 > 1e-06); case test-64r-short ke_series relative_delta exceeds threshold (6.64653e-05 > 1e-06); case test-64r-short temp_series relative_delta exceeds threshold (6.64676e-05 > 1e-06); case test-64r-short press_series relative_delta exceeds threshold (0.00278554 > 1e-05); case test-64r-short forces_xyz abs_delta exceeds threshold (0.0186149 > 1e-08); case test-64r-short forces_xyz rms_delta exceeds threshold (0.000930271 > 1e-08)] (b028e7ec4bbc, weighted_median_pair_plus_kspace_seconds=71.716)
- [2026-04-13T03:48:21.383149Z] Precompute map-resolvable TIP4P oxygen M-sites before the pair loop and read the prepared cache directly, with the incumbent lazy path retained for unresolved ghost oxygens [Correctness passed, but 71.282 regressed incumbent 70.984 by 0.42% and missed the 2% acceptance bar.] (9eb54352a2e0, weighted_median_pair_plus_kspace_seconds=71.282)
- [2026-04-13T04:09:39Z] iteration 32 review: rejected candidate `8bfdf92c40a5` (weighted_median_pair_plus_kspace_seconds=70.124, correctness OK, both cases converged) because the 1.21% improvement versus incumbent `5840dac0d9f1` missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T04:09:39Z] candidate `8bfdf92c40a56b2072446a249af828d777dd0fef` rejected as non-incumbent despite passing correctness.
  Hypothesis: if the incumbent's remaining overhead still includes unnecessary TIP4P cache-hit hydrogen-index materialization and cache-field traffic, then deferring `iH1/iH2` loads in `pair_lj_cut_tip4p_long` until an exact oxygen interaction survives the final Coulomb cutoff, while giving `PPPMTIP4P::particle_map()` an `xM`-only cached accessor, would trim hot-path bookkeeping enough to produce another acceptance-grade win without changing exact oxygen geometry, lazy cache semantics, or TIP4P force/tally ordering.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `5840dac0d9f1`'s 70.984 to 70.124, a 1.21% reduction versus the incumbent and a 25.96% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, and `guardrail_errors=[]`, with peak RSS 159.70 MiB. Case-level pair-plus-kspace timings were 70.127 s for `test-32r-long` and 4.7941 s for `test-64r-short`; in the long case, pair time was 50.017 s and kspace time was 20.11 s. The candidate is directionally positive in both benchmark cases, but it does not clear the 2% acceptance threshold, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: this validates that narrower cache-hit accessors and deferring hydrogen-index loads until the exact oxygen path is proven necessary are scientifically safe and modestly beneficial. However, the small gain after iterations 29 and 31 indicates the incumbent is no longer materially limited by cached `iH1/iH2` traffic or by `particle_map()` carrying unused hydrogen indices alone; representation/accessor cleanup at the current scale is now a sub-2% lever.
  Next hypothesis: keep these safe lazy-access ideas only as supporting ingredients and look for a larger composed reduction inside the existing generation-stamped pair+PPPM design. The next candidate should target repeated exact oxygen-interaction math/indexing that still survives after the final cutoff, or another paired hot-path simplification that removes more work end-to-end without adding eager oxygen prepasses, new cache-heavy state, altered TIP4P force/tally/write ordering, or cached-geometry rewrites.
- [2026-04-13T04:09:39Z] Defer cached TIP4P hydrogen-index loads until exact oxygen interactions pass the final Coulomb cutoff in `pair_lj_cut_tip4p_long`, and use an `xM`-only cached M-site accessor in `pppm_tip4p::particle_map()` [Correctness passed, but 70.124 improves incumbent 70.984 by only 1.21%, below the 2% acceptance bar.] (8bfdf92c40a5, weighted_median_pair_plus_kspace_seconds=70.124)
- [2026-04-13T04:10:44.934114Z] Defer cached TIP4P hydrogen-index loads until exact oxygen interactions pass the final Coulomb cutoff in pair_lj_cut_tip4p_long, and use an xM-only cached M-site accessor in pppm_tip4p::particle_map() [Correctness passed, but 70.124 improves incumbent 70.984 by only 1.21%, below the 2% acceptance bar.] (8bfdf92c40a5, weighted_median_pair_plus_kspace_seconds=70.124)
- [2026-04-13T04:35:52Z] iteration 33 review: rejected candidate `ff67ca3c0752` (weighted_median_pair_plus_kspace_seconds=72.294, correctness OK, both cases converged) because it regressed incumbent `5840dac0d9f1` by 1.85% and therefore missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T04:35:52Z] candidate `ff67ca3c07526936e47b0b8405359a53c140568a` rejected as non-incumbent despite passing correctness.
  Hypothesis: extending iteration 32's safe lazy TIP4P cache-access cleanup with a common nonspecial Coulomb fast path in `pair_lj_cut_tip4p_long`, while keeping the `xM`-only PPPM `particle_map()` accessor, would stack pair-side common-case pruning onto the proven-safe lazy hydrogen-index and M-site access reductions and push the incumbent over the 2% acceptance bar.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` regressed from incumbent `5840dac0d9f1`'s 70.984 to 72.294, making the candidate 1.85% slower than the incumbent even though it remains 23.67% better than baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, and `guardrail_errors=[]`, with peak RSS 158.45 MiB. Case-level pair-plus-kspace timings were 77.482 s for `test-32r-long` and 4.9455 s for `test-64r-short`; in the long case, pair time was 52.352 s and kspace time was 25.13 s. This is also materially worse than iteration 32's related lazy-access candidate, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: iteration 32 showed that deferring hydrogen-index loads and narrowing PPPM cache access to `xM` are safe low-level wins, but adding the common nonspecial Coulomb fast path on top of the current inlined cache-hit design overcorrects in the wrong direction. The regression versus both the incumbent and iteration 32 suggests the extra pair-kernel branch and code-shape changes outweigh the saved common-case work, so further pair-side common-nonspecial micro-specialization is not a promising standalone lever here.
  Next hypothesis: revert to the leaner iteration 32-style lazy-access shape as the safe base, and look for a larger composed reduction elsewhere in the surviving exact-oxygen hot path or downstream PPPM work after `particle_map()`. Focus on removing repeated oxygen-path math/indexing without adding another common-case pair fast path, eager oxygen prepasses, new cache-heavy state, or any change to TIP4P force/tally/write ordering.
- [2026-04-13T04:35:52Z] Defer TIP4P pair hydrogen-index loads until exact oxygen-cutoff hits, add a common nonspecial Coulomb fast path in `pair_lj_cut_tip4p_long`, and use an `xM`-only PPPM cache accessor in `particle_map()` [Correctness passed, but candidate `ff67ca3c0752` regressed incumbent `5840dac0d9f1` from 70.984 to 72.294, so it misses the 2% acceptance bar.] (ff67ca3c0752, weighted_median_pair_plus_kspace_seconds=72.294)
- [2026-04-13T04:37:28.799233Z] Defer TIP4P pair hydrogen-index loads until exact oxygen-cutoff hits, add a common nonspecial Coulomb fast path in `pair_lj_cut_tip4p_long`, and use an xM-only PPPM cache accessor in `particle_map()` [Correctness passed, but candidate ff67ca3c0752 regressed the incumbent primary metric from 70.984 to 72.294, so it misses the 2% improvement bar.] (ff67ca3c0752, weighted_median_pair_plus_kspace_seconds=72.294)
- [2026-04-13T04:58:49Z] iteration 34 review: rejected candidate `625e91025a03` (weighted_median_pair_plus_kspace_seconds=70.877, correctness OK, both cases converged) because the 0.15% improvement versus incumbent `5840dac0d9f1` missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T04:58:49Z] candidate `625e91025a035b68b8f4f1f016cbb680c2a8b3a7` rejected as non-incumbent despite passing correctness.
  Hypothesis: splitting the TIP4P pair/PPPM caches into compact `xM` coordinate arrays plus separate hydrogen-index/stamp state, while deferring pair oxygen-neighbor hydrogen-index loads until an exact Coulomb interaction survives the final cutoff, would keep iteration 32's safe lazy-access behavior but cut cache footprint and hot-path cache-field traffic enough to produce an acceptance-grade pair+kspace win.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `5840dac0d9f1`'s 70.984 to 70.877, only a 0.15% reduction versus the incumbent and a 25.17% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 157.51 MiB. Case-level pair-plus-kspace timings were 70.877 s for `test-32r-long` and 4.829 s for `test-64r-short`; in the long case, pair time was 50.704 s and kspace time was 20.173 s. This is directionally positive but still misses the 2% acceptance threshold, and it is also slower than the simpler iteration 32 lazy-access candidate at 70.124, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: the cache split is scientifically safe, but reorganizing the TIP4P cache into separate `xM` and H-index/stamp arrays does not unlock a materially larger win than the leaner accessor cleanup from iteration 32. The extra state separation, pointer traffic, and bookkeeping appear to offset most of the footprint benefit; the long-case pair and kspace times staying above iteration 32 suggest the remaining cost is not dominated by the mixed cache layout alone.
  Next hypothesis: keep the proven-safe idea of loading hydrogen indices only after the exact oxygen cutoff as a supporting tactic, but stop spending iterations on cache-layout surgery by itself. The next candidate should target repeated exact oxygen-path math/indexing or downstream PPPM work after `particle_map()` using the leanest existing cache shape, while avoiding eager oxygen prepasses, new cache-heavy split state, branch-heavy pair specialization, or any change to TIP4P force/tally/write ordering.
- [2026-04-13T04:58:49Z] Split TIP4P pair/PPPM caches into compact M-site coordinate arrays plus separate H-index/stamp state, deferring pair oxygen-neighbor H-index loads until exact Coulomb-cutoff hits [Correctness passed, but 70.877 improves incumbent 70.984 by only 0.15%, and it is weaker than iteration 32's 70.124, so it misses the 2% acceptance bar.] (625e91025a03, weighted_median_pair_plus_kspace_seconds=70.877)
- [2026-04-13T04:59:53.175610Z] Split TIP4P pair/PPPM caches into compact M-site coordinate arrays plus separate H-index/stamp state, deferring pair oxygen-neighbor H-index loads until exact Coulomb-cutoff hits [Correctness passed, but 70.877 improves incumbent 70.984 by only 0.15%, far below the 2% acceptance threshold.] (625e91025a03, weighted_median_pair_plus_kspace_seconds=70.877)
- [2026-04-13T05:22:20Z] iteration 35 review: rejected candidate `44b554c8a168` (weighted_median_pair_plus_kspace_seconds=70.511, correctness OK, both cases converged) because the 0.67% improvement versus incumbent `5840dac0d9f1` missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T05:22:20Z] candidate `44b554c8a168fd6dd95de386b253c73ca47c490e` rejected as non-incumbent despite passing correctness.
  Hypothesis: deferring pair-side TIP4P hydrogen-index and Coulomb-special loads until exact oxygen-path survivors, while narrowing PPPM `particle_map()` to an `xM`-only cache accessor, would preserve iteration 32's safe lazy-access direction but remove a bit more unnecessary hot-path bookkeeping and finally clear the 2% acceptance bar.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `5840dac0d9f1`'s 70.984 to 70.511, a 0.67% reduction versus the incumbent and a 25.56% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 158.17 MiB. Case-level pair-plus-kspace timings were 70.594 s for `test-32r-long` and 4.8522 s for `test-64r-short`; in the long case, pair time was 50.288 s and kspace time was 20.306 s. This is a clean directional improvement and noticeably better than iteration 34, but it is still slower than iteration 32's 70.124, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: further deferring pair-side hydrogen-index and special-neighbor loads is scientifically safe and does recover some of the ground lost to cache-layout surgery, but the remaining exact-oxygen hot path is not dominated by those loads alone. The gain shrinking to sub-1% and still trailing iteration 32 suggests extra control-flow and accessor changes can erase much of the saved bookkeeping unless they remove more work deeper in the exact-oxygen or PPPM hot path.
  Next hypothesis: keep this lazy-load refinement only as a supporting ingredient and stop spending iterations on cache accessor/layout tweaks by themselves. The next candidate should start from the lean iteration 32/35 access pattern and target repeated exact oxygen-survivor math or post-`particle_map()` PPPM indexing that still runs after the final cutoff, without adding eager oxygen prepasses, branch-heavy common-case pair specialization, new cache-heavy state, or any change to TIP4P force/tally/write ordering.
- [2026-04-13T05:22:20Z] Defer pair-side TIP4P hydrogen-index and Coulomb-special loads until exact oxygen-path survivors, plus an xM-only PPPM particle_map cache accessor [Correctness passed, but 70.511 improves incumbent 70.984 by only 0.67% and is still weaker than iteration 32's 70.124, so it misses the 2% acceptance bar.] (44b554c8a168, weighted_median_pair_plus_kspace_seconds=70.511)
- [2026-04-13T05:23:30.434777Z] Defer pair-side TIP4P hydrogen-index and Coulomb-special loads until exact oxygen-path survivors, plus an xM-only PPPM particle_map cache accessor [Correctness passed, but candidate 44b554c8a168 improves the incumbent by only 0.67% (70.984 to 70.511), below the 2% acceptance bar, so 5840dac0d9f1 remains incumbent.] (44b554c8a168, weighted_median_pair_plus_kspace_seconds=70.511)
- [2026-04-13T05:49:16Z] iteration 36 review: rejected candidate `d6aa774723cb` (weighted_median_pair_plus_kspace_seconds=70.737, correctness OK, both cases converged) because the 0.35% improvement versus incumbent `5840dac0d9f1` missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T05:49:16Z] candidate `d6aa774723cbba4aeacff7711bb3ae03a41b4a5a` rejected as non-incumbent despite passing correctness.
  Hypothesis: using the lazy TIP4P pair/PPPM cache-access shape with an `xM`-only PPPM `particle_map()` accessor, while hoisting exact-survivor pair charge and coordinate math, would preserve the safe iteration 32/35 lazy-load direction and remove enough repeated exact-oxygen survivor work to clear the 2% acceptance bar.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `5840dac0d9f1`'s 70.984 to 70.737, only a 0.35% reduction versus the incumbent and a 25.32% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 158.43 MiB. Case-level pair-plus-kspace timings were 70.934 s for `test-32r-long` and 4.8337 s for `test-64r-short`; in the long case, pair time was 50.482 s and kspace time was 20.452 s. This is directionally positive but weaker than both iteration 32's 70.124 and iteration 35's 70.511, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: the lazy `xM`-only PPPM access shape is still scientifically safe, but hoisting exact-survivor pair charge and coordinate math does not add enough leverage on top of it. Relative to iterations 32 and 35, both long-case pair and kspace times moved the wrong way, which suggests the remaining cost is not dominated by those survivor scalar reloads and that the extra hoist/control plumbing can erase most of the saved work.
  Next hypothesis: keep the lean lazy-access pattern only as background context and stop spending full iterations on survivor charge/coordinate hoists or further cache-shape surgery. The next candidate should target a larger end-to-end reduction in repeated exact-oxygen survivor math or post-`particle_map()` PPPM indexing/stencil work that still executes after the final cutoff, while avoiding eager oxygen prepasses, branch-heavy pair specialization, new cache-heavy state, or any change to TIP4P force/tally/write ordering.
- [2026-04-13T05:49:16.174800Z] Use the lazy TIP4P pair/PPPM cache-access shape with an xM-only PPPM particle_map accessor, and hoist exact-survivor pair charge and coordinate math without changing force, tally, or write order [Correctness passed, but 70.737 improves incumbent 70.984 by only 0.35% and is weaker than iterations 32 and 35, so it misses the 2% acceptance bar.] (d6aa774723cb, weighted_median_pair_plus_kspace_seconds=70.737)
- [2026-04-13T05:50:34.677960Z] Use the lazy TIP4P pair/PPPM cache-access shape with an xM-only PPPM particle_map accessor, and hoist exact-survivor pair charge and coordinate math without changing force, tally, or write order [Correctness passed, but candidate d6aa774723cb improves the incumbent by only 0.35% (70.984 to 70.737), well below the 2% acceptance bar.] (d6aa774723cb, weighted_median_pair_plus_kspace_seconds=70.737)
- [2026-04-13T06:13:36Z] iteration 37 review: rejected candidate `e04c1aedddd3` (weighted_median_pair_plus_kspace_seconds=70.108, correctness OK, both cases converged) because the 1.23% improvement versus incumbent `5840dac0d9f1` missed the contract's 2% minimum relative improvement bar.
- [2026-04-13T06:13:36Z] candidate `e04c1aedddd3b91b745b350a3d9e276418611aef` rejected as non-incumbent despite passing correctness.
  Hypothesis: composing typed alias-friendly cached TIP4P M-site coordinates with lazy pair-side hydrogen-index materialization and an `xM`-only PPPM `particle_map()` accessor would stack iteration 29's safe alias/view cleanup with iteration 32's best lazy-load/cache-access reductions and finally clear the 2% acceptance bar without changing cutoff logic or TIP4P force/tally ordering.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` improved from incumbent `5840dac0d9f1`'s 70.984 to 70.108, a 1.23% reduction versus the incumbent and a 25.98% reduction versus baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 157.875 MiB. Case-level pair-plus-kspace timings were 70.108 s for `test-32r-long` and 4.8038 s for `test-64r-short`; in the long case, pair time was 49.743 s and kspace time was 20.365 s. This is the best correctness-clean result since the current incumbent and slightly edges iteration 32's 70.124, but it still misses the 2% acceptance threshold, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: typed alias-friendly cached M-site coordinates do compose safely with lazy pair-side hydrogen-index use and the `xM`-only PPPM accessor, but the extra gain beyond iteration 32 is only 0.016 s on the weighted median. That indicates the remaining pair+kspace cost is not dominated by cache field aliasing or early hydrogen-index loads alone; additional accessor/layout reshaping is now in diminishing-returns territory unless it removes more end-to-end oxygen-path work.
  Next hypothesis: keep the lean lazy-access plus typed-`xM` coordinate shape only as a supporting baseline and target repeated exact-oxygen survivor math or post-`particle_map()` PPPM indexing/stencil work that still executes in the long case. Avoid eager oxygen prepasses, branch-heavy common-case pair specialization, new cache-heavy state, or any change to TIP4P force/tally/write ordering.
- [2026-04-13T06:13:36.828861096Z] Compose typed alias-friendly TIP4P cached M-site coordinates with lazy pair-side hydrogen-index use and an xM-only PPPM particle_map accessor, preserving incumbent cutoff and TIP4P force/tally ordering [Correctness passed, but 70.108 improves incumbent 70.984 by only 1.23% and only marginally beats iteration 32's 70.124, so it misses the 2% acceptance bar.] (e04c1aedddd3, weighted_median_pair_plus_kspace_seconds=70.108)
- [2026-04-13T06:14:38.236230Z] Compose typed alias-friendly TIP4P cached M-site coordinates with lazy pair-side hydrogen-index use and an xM-only PPPM particle_map accessor, preserving incumbent cutoff and TIP4P force/tally ordering [Correctness passed, but 70.108 improves incumbent 70.984 by only 1.23%, below the contract’s 2% acceptance bar.] (e04c1aedddd3, weighted_median_pair_plus_kspace_seconds=70.108)
- [2026-04-13T06:36:12Z] iteration 38 review: rejected candidate `095bee6d96dce755c1b56809b6bd6155c0f00eb6` (weighted_median_pair_plus_kspace_seconds=71.763, correctness OK, both cases converged) because it regressed incumbent `5840dac0d9f1` by 1.10% and therefore misses the contract's 2% minimum improvement bar.
- [2026-04-13T06:36:12Z] candidate `095bee6d96dce755c1b56809b6bd6155c0f00eb6` rejected as non-incumbent despite passing correctness.
  Hypothesis: combining lazy TIP4P `xM`-only cache access, pair-side hydrogen-index deferral, and PPPM per-atom stencil-base reuse computed in `particle_map()` would preserve iteration 37's safe lazy-access direction while cutting repeated PPPM index arithmetic in `make_rho()` and `fieldforce_ik()` enough to convert the recent near-miss into an acceptance-grade win.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` regressed from incumbent `5840dac0d9f1`'s 70.984 to 71.763, making the candidate 1.10% slower than the incumbent even though it remains 24.23% better than baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 157.625 MiB. Case-level pair-plus-kspace timings were 80.727 s for `test-32r-long` and 4.9231 s for `test-64r-short`; in the long case, pair time rose to 51.833 s and kspace time to 28.894 s. This is materially worse than the recent lazy-access variants, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: the lazy `xM`-only access shape and deferred pair hydrogen-index loads are still scientifically safe, but adding cached PPPM stencil-base state is counterproductive in this benchmark. The large long-case kspace regression, together with pair backsliding, indicates that the extra per-atom cache fields, bookkeeping, and memory traffic cost more than the saved stencil-base arithmetic; the leaner iteration 32/37 access pattern is a better foundation than introducing another cache layer.
  Next hypothesis: revert to the lean lazy-access baseline from iterations 32/37 and target a lower-overhead reduction that removes repeated exact-oxygen survivor math or post-`particle_map()` PPPM work without storing extra per-atom stencil-base state. Favor reuse of already-available indices or math only when it fits the existing cache shape, and continue to avoid eager oxygen prepasses, branch-heavy pair specialization, new cache-heavy PPPM state, or any change to TIP4P force/tally/write ordering.
- [2026-04-13T06:36:12Z] Lazy TIP4P xM-only cache access plus pair hydrogen-index deferral and PPPM stencil-base reuse [Correctness passed, but 71.763 regresses incumbent 70.984 by 1.10%, with the long-case kspace cost jumping to 28.894 s, so it misses the 2% acceptance bar.] (095bee6d96dc, weighted_median_pair_plus_kspace_seconds=71.763)
- [2026-04-13T06:37:31.798714Z] Lazy TIP4P xM-only cache access plus pair hydrogen-index deferral and PPPM stencil-base reuse [Correctness passed, but 71.763 regressed incumbent 70.984 by 1.10%, so this candidate cannot replace the incumbent.] (095bee6d96dc, weighted_median_pair_plus_kspace_seconds=71.763)
- [2026-04-13T06:59:12Z] iteration 39 review: rejected candidate `7c4d21219695` (weighted_median_pair_plus_kspace_seconds=71.327, correctness OK, both cases converged) because it regressed incumbent `5840dac0d9f1` by 0.48% and therefore missed the contract's 2% minimum improvement bar.
- [2026-04-13T06:59:12Z] candidate `7c4d21219695d85cd7608c3dfaf04c2ec17491fd` rejected as non-incumbent despite passing correctness.
  Hypothesis: splitting TIP4P cache hits into `xM`-only versus hydrogen-index reads, and memoizing the pair-side oxygen reach scalar (`cut_coul + |OM|`) alongside cached `xM` updates, would keep the recent lazy-access direction scientifically exact while trimming remaining pair/PPPM cache-hit bookkeeping enough to beat incumbent `5840dac0d9f1`.
  Changed files: `src/KSPACE/pair_lj_cut_tip4p_long.cpp`, `src/KSPACE/pair_lj_cut_tip4p_long.h`, `src/KSPACE/pppm_tip4p.cpp`, `src/KSPACE/pppm_tip4p.h`
  Benchmark outcome: `weighted_median_pair_plus_kspace_seconds` regressed from incumbent `5840dac0d9f1`'s 70.984 to 71.327, making the candidate 0.48% slower than the incumbent even though it remains 24.69% better than baseline `e7c0ed95a333`. Both benchmark cases converged, `correctness_ok=true`, `guardrail_errors=[]`, and peak RSS was 158.546875 MiB. Case-level pair-plus-kspace timings were 71.327 s for `test-32r-long` and 4.8799 s for `test-64r-short`; in the long case, pair time was 51.165 s and kspace time was 20.162 s. This is also weaker than the simpler recent lazy-access variants from iterations 32 and 37, so `5840dac0d9f1` remains the incumbent.
  Lesson learned: separating `xM` coordinate reads from hydrogen-index loads and caching the oxygen reach scalar is scientifically safe, but it does not translate into a real end-to-end win on top of the current inlined cache-hit design. The regression versus the incumbent, and versus leaner lazy-access candidates, suggests the added cache field and accessor/control plumbing cost more than the saved `sqrt` and metadata traffic; the remaining bottleneck is not dominated by `cut_coul + |OM|` recomputation or mixed `xM`/H-index cache reads alone.
  Next hypothesis: return to the leanest lazy-access baseline and target work that survives after the exact oxygen interaction is known to matter. Focus on removing repeated exact-oxygen survivor math or post-`particle_map()` PPPM indexing using already-available state, while avoiding eager oxygen prepasses, extra cache fields/layers, branch-heavy pair specialization, or any change to TIP4P force/tally/write ordering.
- [2026-04-13T06:59:12Z] Split TIP4P pair/PPPM cache hits into xM-only versus hydrogen-index reads, and memoize the pair-side oxygen reach scalar alongside cached xM updates while preserving exact surviving M-site geometry [Correctness passed, but 71.327 regressed incumbent 70.984 by 0.48% and underperformed the leaner iteration 32/37 lazy-access variants, so it misses the 2% acceptance bar.] (7c4d21219695, weighted_median_pair_plus_kspace_seconds=71.327)
- [2026-04-13T07:00:13.519469Z] Split TIP4P pair/PPPM cache hits into xM-only versus hydrogen-index reads, and memoize the pair-side oxygen reach scalar alongside cached xM updates while preserving exact surviving M-site geometry [Correctness passed, but candidate 7c4d21219695 regressed the incumbent primary metric from 70.984 to 71.327, so it cannot replace 5840dac0d9f1.] (7c4d21219695, weighted_median_pair_plus_kspace_seconds=71.327)
