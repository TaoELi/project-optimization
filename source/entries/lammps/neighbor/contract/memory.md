# FermiLink Optimize Memory

- package_id: lammps
- benchmark_id: lammps-tip4p-water-nve-neighbor-bin
- benchmark_path: .fermilink-optimize/benchmark.worker.yaml
- optimize_branch: fermilink-optimize/lammps-neighbor
- started_at_utc: 2026-04-12T06:00:59.740714Z
- last_updated_utc: 2026-04-12T23:30:40.819488Z

## Short-Term Memory (Operational)
### Current objective
- Improve benchmark performance while preserving correctness.
### Latest accepted commit
- commit: 11db74893452
- primary_metric: weighted_median_neigh_seconds=0.28487
- status: accepted
### Progress log

- [2026-04-12T06:04:05.996182Z] baseline: baseline (e7c0ed95a333, weighted_median_neigh_seconds=0.36158)
- [2026-04-12T06:30:34Z] iter_0001 review: rejected candidate `2d1eda35b944` after authoritative benchmarking. Hypothesis was that splitting the orthogonal half-bin/newton self-bin traversal and caching per-atom cutoff/special-bond lookups in `src/npair_bin.cpp` would reduce hot-path branch and memory overhead. Correctness passed for both cases, but `weighted_median_neigh_seconds` regressed from 0.36158 to 0.47712, so incumbent `e7c0ed95a333` remains in place.
- [2026-04-12T06:31:16.877051Z] rejected: Split the orthogonal half-bin/newton self-bin traversal in `npair_bin.cpp` and cache per-atom cutoff/special-bond lookups to reduce repeated hot-path branches and memory accesses [Correctness held, but the `src/npair_bin.cpp` refactor regressed `weighted_median_neigh_seconds` from 0.36158 to 0.47712 (+31.95%), so the incumbent stays.] (2d1eda35b944, weighted_median_neigh_seconds=0.47712)
- [2026-04-12T06:47:30Z] iter_0002 review: accepted candidate `32b495fdb66b` after authoritative benchmarking. Hypothesis was that specializing the orthogonal half/bin/newton neighbor build in `src/npair_bin.cpp` for the benchmark-hot template instance would remove generic branches and hoist per-atom special-bond/cutoff setup out of the innermost loop without changing correctness. Correctness passed for both cases with zero failures, and `weighted_median_neigh_seconds` improved from 0.36158 to 0.33535 (-7.25%), so candidate `32b495fdb66b` becomes the new incumbent.
- [2026-04-12T06:47:30Z] accepted: Specialize the orthogonal half/bin/newton neighbor build in `src/npair_bin.cpp` to remove generic branches and hoist per-atom special-bond/cutoff setup out of the innermost loop [Correctness held, and the targeted `src/npair_bin.cpp` specialization improved `weighted_median_neigh_seconds` from 0.36158 to 0.33535 (-7.25%), so the candidate becomes the new incumbent.] (32b495fdb66b, weighted_median_neigh_seconds=0.33535)
- [2026-04-12T06:48:17.589263Z] accepted: Specialize the orthogonal half/bin/newton neighbor build in `src/npair_bin.cpp` to remove generic branches and hoist per-atom special-bond/cutoff setup out of the innermost loop [Correctness held with zero failures, and the targeted `src/npair_bin.cpp` hot-path specialization improved `weighted_median_neigh_seconds` from 0.36158 to 0.33535 (-7.25%), so the candidate becomes the new incumbent.] (32b495fdb66b, weighted_median_neigh_seconds=0.33535)
- [2026-04-12T07:01:49Z] iter_0003 review: accepted candidate `380bf2b0146b` after authoritative benchmarking. Hypothesis was that adding a benchmark-specific fast path in `src/npair_bin.cpp` for standard molecular systems without neighbor exclusions would remove repeated `special_i` setup and `exclude` handling from the accepted orthogonal half/bin/newton loops while preserving correctness. Correctness passed for both cases with zero failures, and `weighted_median_neigh_seconds` improved from 0.33535 to 0.32842 (-2.07% vs incumbent, -9.17% vs baseline), so candidate `380bf2b0146b` becomes the new incumbent.
- [2026-04-12T07:03:15.541928Z] accepted: Add a benchmark-specific fast path in `NPairBin<1,1,0,0,0>::build()` for standard molecular systems without neighbor exclusions, removing repeated `special_i` setup and `exclude` checks from the hot orthogonal half/bin/newton loops. [Candidate improves weighted_median_neigh_seconds from 0.33535 to 0.32842 with correctness intact, so it becomes the new incumbent.] (380bf2b0146b, weighted_median_neigh_seconds=0.32842)
- [2026-04-12T07:20:10Z] iter_0004 review: rejected candidate `3bd7cbc7ee03` after authoritative benchmarking. Hypothesis was that the iteration-3 molecular/no-exclusion fast path in `src/npair_bin.cpp` still spent avoidable time on `find_special()` and `minimum_image_check()` for different-molecule pairs, so bypassing those checks outside same-molecule pairs would trim residual hot-loop overhead without changing correctness. Correctness passed for both cases with zero failures, and `weighted_median_neigh_seconds` improved from 0.32842 to 0.32287 (-1.69% vs incumbent, -10.71% vs baseline), but that gain did not meet the benchmark contract's 2.0% minimum relative improvement threshold, so incumbent `380bf2b0146b` remains in place.
- [2026-04-12T07:20:10Z] rejected: Skip `find_special()` and `minimum_image_check()` for different-molecule pairs inside the incumbent `NPairBin<1,1,0,0,0>::build()` molecular/no-exclusion fast path, keeping same-molecule pairs on the original special-neighbor handling. [Correctness held and the metric improved from 0.32842 to 0.32287, but the -1.69% change did not clear the controller's 2.0% minimum-improvement gate, so the incumbent stays.] (3bd7cbc7ee03, weighted_median_neigh_seconds=0.32287)
- [2026-04-12T07:21:42.202916Z] rejected: Skip `find_special()` and `minimum_image_check()` for different-molecule pairs inside the incumbent `NPairBin<1,1,0,0,0>::build()` molecular/no-exclusion fast path, keeping same-molecule pairs on the original special-neighbor handling. [Correctness held and weighted_median_neigh_seconds improved to 0.32287, but the -1.69% gain versus incumbent missed the benchmark contract’s 2.0% minimum improvement threshold, so 380bf2b0146b remains incumbent.] (3bd7cbc7ee03, weighted_median_neigh_seconds=0.32287)
- [2026-04-12T07:38:02Z] iter_0005 review: rejected candidate `1a1935b59c00` after authoritative benchmarking. Hypothesis was that the iteration-3 molecular/no-exclusion fast path in `src/npair_bin.cpp` could clear the remaining controller gap by specializing the benchmark's `maxspecial <= 2` case, bypassing generic `find_special()` scans for different-molecule pairs and using direct same-molecule tag matching instead. Correctness passed for both cases with zero failures, and `weighted_median_neigh_seconds` improved from 0.32842 to 0.32384 (-1.39% vs incumbent, -10.44% vs baseline), but that gain still missed the benchmark contract's 2.0% minimum relative improvement threshold, so incumbent `380bf2b0146b` remains in place.
- [2026-04-12T07:38:02Z] rejected: Add a `maxspecial <= 2` molecular/no-exclusion fast path in `NPairBin<1,1,0,0,0>::build()` that bypasses generic `find_special()` scans for different-molecule pairs and uses direct same-molecule special-tag matching. [Correctness held and weighted_median_neigh_seconds improved to 0.32384, but the -1.39% gain versus incumbent missed the benchmark contract’s 2.0% minimum improvement threshold, so 380bf2b0146b remains incumbent.] (1a1935b59c00, weighted_median_neigh_seconds=0.32384)
- [2026-04-12T07:39:27.276335Z] rejected: Add a `maxspecial <= 2` molecular/no-exclusion fast path in `NPairBin<1,1,0,0,0>::build()` that bypasses generic `find_special()` scans for different-molecule pairs and uses direct same-molecule special-tag matching. [Correctness held and weighted_median_neigh_seconds improved to 0.32384, but the 1.39% gain versus incumbent 380bf2b0146b missed the benchmark contract’s 2.0% minimum improvement threshold.] (1a1935b59c00, weighted_median_neigh_seconds=0.32384)
- [2026-04-12T14:54:59Z] iter_0006 review: accepted candidate `c4128d3970b2` after authoritative benchmarking. Hypothesis was that combining the previously sub-threshold different-molecule early accept with an owned same-molecule encoded-special shortcut in `src/npair_bin.cpp` would remove enough remaining `find_special()` and `minimum_image_check()` overhead inside the incumbent `NPairBin<1,1,0,0,0>::build()` fast path to clear the controller gate without changing list semantics. Correctness passed for both cases with zero failures, and `weighted_median_neigh_seconds` improved from 0.32842 to 0.31969 (-2.66% vs incumbent, -11.59% vs baseline), so candidate `c4128d3970b2` becomes the new incumbent.
- [2026-04-12T14:54:59Z] accepted: Refine the incumbent `NPairBin<1,1,0,0,0>::build()` fast path by early-accepting different-molecule pairs and skipping `minimum_image_check()` for owned same-molecule specials when `special_flag[1..3] == 2`. [Correctness held and weighted_median_neigh_seconds improved to 0.31969, a 2.66% gain versus incumbent 380bf2b0146b that clears the 2.0% promotion gate, so the candidate becomes the new incumbent.] (c4128d3970b2, weighted_median_neigh_seconds=0.31969)
- [2026-04-12T14:57:39.340307Z] accepted: Refine the incumbent `NPairBin<1,1,0,0,0>::build()` fast path by early-accepting different-molecule pairs and skipping `minimum_image_check()` for owned same-molecule specials when `special_flag[1..3] == 2`. [Correctness held and weighted_median_neigh_seconds improved from 0.32842 to 0.31969 (-2.66% vs incumbent), clearing the benchmark contract’s 2.0% promotion gate.] (c4128d3970b2, weighted_median_neigh_seconds=0.31969)
- [2026-04-12T15:10:12Z] iter_0007 review: rejected candidate `d89e6f820474` after authoritative benchmarking. Hypothesis was that the incumbent `NPairBin<1,1,0,0,0>::build()` fast path still paid avoidable same-molecule overhead in the remaining `find_special()` scan, so replacing that scan with direct two-entry encoded-special tag checks when `nspecial[i][2] <= 2` would preserve correctness while trimming enough hot-loop work to build on iteration 6. Correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.31969 to 0.32426 (+1.43% vs incumbent, still -10.32% vs baseline), so incumbent `c4128d3970b2` remains in place.
- [2026-04-12T15:10:12Z] rejected: Replace the remaining same-molecule `find_special()` scan in the incumbent `NPairBin<1,1,0,0,0>::build()` fast path with direct two-entry encoded-special tag checks when `nspecial[i][2] <= 2`, preserving the incumbent different-molecule early accept and ghost minimum-image fallback. [Correctness held, but `weighted_median_neigh_seconds` regressed from 0.31969 to 0.32426 (+1.43% vs incumbent), so the incumbent stays.] (d89e6f820474, weighted_median_neigh_seconds=0.32426)
- [2026-04-12T15:11:26.215521Z] rejected: Replace the remaining same-molecule `find_special()` scan in the incumbent `NPairBin<1,1,0,0,0>::build()` fast path with direct two-entry encoded-special tag checks when `nspecial[i][2] <= 2`, preserving the incumbent different-molecule early accept and ghost minimum-image fallback. [Correctness held, but the candidate regressed weighted_median_neigh_seconds from 0.31969 to 0.32426 (+1.43% vs incumbent), so c4128d3970b2 remains incumbent.] (d89e6f820474, weighted_median_neigh_seconds=0.32426)
- [2026-04-12T15:25:53.198255Z] rejected: Split the benchmark-hot molecular/no-exclusion neighbor build into encoded-special and generic variants in `src/npair_bin.cpp`, using an encoded-only special lookup and special-hit-first branch order to cut residual same-molecule hot-loop branching. [Correctness held, but splitting the same-molecule fast path into encoded-special and generic variants regressed weighted_median_neigh_seconds from 0.31969 to 0.33329 (+4.25%), so c4128d3970b2 remains incumbent.] (d381c38751ec, weighted_median_neigh_seconds=0.33329)
- [2026-04-12T15:41:17Z] iter_0009 review: rejected candidate `2894990122f9` after authoritative benchmarking. Hypothesis was that caching per-neighbor ownership/tag/molecule values and using an encoded-only special lookup inside the incumbent molecular/no-exclusion half/bin/newton fast path in `src/npair_bin.cpp` would remove enough repeated loads and generic special-lookup overhead to extend the iteration-6 win without changing correctness. Correctness passed for both cases with zero failures, and `weighted_median_neigh_seconds` improved from 0.31969 to 0.31870 (-0.31% vs incumbent, -11.86% vs baseline), but that gain did not meet the benchmark contract's 2.0% minimum relative improvement threshold, so incumbent `c4128d3970b2` remains in place.
- [2026-04-12T15:43:00.090847Z] rejected: Cache per-neighbor ownership/tag/molecule values and use an encoded-only special lookup inside the incumbent molecular/no-exclusion half/bin/newton fast path in `src/npair_bin.cpp` [Correctness held, but 0.31870 improves on incumbent 0.31969 by only 0.31%, below the 2.0% promotion threshold.] (2894990122f9, weighted_median_neigh_seconds=0.3187)
- [2026-04-12T16:01:11.348123450Z] iter_0010 review: rejected candidate `b21ee6319e89` after authoritative benchmarking. Hypothesis was that splitting the incumbent molecular/no-exclusion self-bin walk in `src/npair_bin.cpp` into owned and ghost tails would remove repeated ghost-order checks for owned self-bin neighbors while preserving the accepted stencil path and correctness. Correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.31969 to 0.32210 (+0.75% vs incumbent, still -10.92% vs baseline), so incumbent `c4128d3970b2` remains in place.
- [2026-04-12T16:01:11.348123450Z] rejected: Split the incumbent molecular/no-exclusion self-bin walk in `NPairBin<1,1,0,0,0>::build()` into owned and ghost tails so owned self-bin neighbors avoid repeated ghost-order checks while the accepted stencil path stays unchanged. [Correctness held, but weighted_median_neigh_seconds regressed from 0.31969 to 0.32210 (+0.75% vs incumbent), so c4128d3970b2 remains incumbent.] (b21ee6319e89, weighted_median_neigh_seconds=0.3221)
- [2026-04-12T16:02:45.244196Z] rejected: Split the incumbent molecular/no-exclusion self-bin walk in `NPairBin<1,1,0,0,0>::build()` into owned and ghost tails so owned self-bin neighbors avoid repeated ghost-order checks while the accepted stencil path stays unchanged. [Correctness held, but weighted_median_neigh_seconds regressed from 0.31969 to 0.32210 (+0.75% vs incumbent), so the candidate cannot replace the incumbent or clear the 2.0% promotion gate.] (b21ee6319e89, weighted_median_neigh_seconds=0.3221)
- [2026-04-12T16:21:24.592364Z] accepted: Use contiguous `_noalias` aliases for hot coordinate/type/tag/molecule arrays in the incumbent `NPairBin<1,1,0,0,0>::build()` path [Correctness held and the candidate cut weighted_median_neigh_seconds from 0.31969 to 0.31150 (-2.56% vs incumbent), clearing the 2.0% promotion gate.] (8c29b124a2c6, weighted_median_neigh_seconds=0.3115)
- [2026-04-12T16:33:11Z] iter_0012 review: rejected candidate `e5d6d4548fe4ab089cc4bc1a24e8a2402cf40f71` after authoritative benchmarking. Hypothesis was that extending the accepted iteration-11 `NPairBin<1,1,0,0,0>::build()` fast path in `src/npair_bin.cpp` with more `_noalias` neighbor-array aliases, one-time neighbor coordinate/tag/molecule caching, and cached half-box minimum-image checks would compound the incumbent's memory-access win without disturbing its single-path control flow. Correctness passed for both cases with zero failures, and `weighted_median_neigh_seconds` improved slightly from 0.31150 to 0.31003 (-0.47% vs incumbent, -14.26% vs baseline), but that gain did not meet the benchmark contract's 2.0% minimum relative improvement threshold, so incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains in place.
- [2026-04-12T16:33:11Z] rejected: Extend the incumbent `NPairBin<1,1,0,0,0>::build()` hot path with additional neighbor-array aliasing, one-time neighbor coordinate/tag/molecule caching, and cached half-box minimum-image checks [Correctness held and `weighted_median_neigh_seconds` improved from 0.31150 to 0.31003, but the -0.47% gain versus incumbent missed the 2.0% promotion threshold, so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent.] (e5d6d4548fe4ab089cc4bc1a24e8a2402cf40f71, weighted_median_neigh_seconds=0.31003)
- [2026-04-12T16:37:44.430333Z] rejected: Extend the incumbent `NPairBin<1,1,0,0,0>::build()` hot path with additional neighbor-array aliasing, one-time neighbor coordinate/tag/molecule caching, and cached half-box minimum-image checks [Correctness held and weighted_median_neigh_seconds improved to 0.31003, but the -0.47% gain versus incumbent missed the benchmark contract’s 2.0% promotion threshold.] (e5d6d4548fe4, weighted_median_neigh_seconds=0.31003)
- [2026-04-12T16:58:48.160626338Z] iter_0013 review: rejected candidate `7adf3bfc72a840d0627a8ede4cdb5dadc088e51e` after authoritative benchmarking. Hypothesis was that replacing the incumbent molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` special handling with a direct encoded-two-special tag lookup path when `nspecial[i][2] <= 2` would bypass the hot `molecule[j]`/`find_special()` work for non-matching neighbors while preserving the incumbent fallback and ghost minimum-image behavior. Correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.31150 to 0.31711 (+1.80% vs incumbent, still -12.30% vs baseline), so incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains in place.
- [2026-04-12T16:58:48.160626338Z] rejected: Replace the incumbent molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` special handling with a direct encoded-two-special tag lookup path when `nspecial[i][2] <= 2`, bypassing the hot `molecule[j]`/`find_special()` path for non-matching neighbors while preserving the incumbent fallback and ghost minimum-image behavior. [Correctness held, but weighted_median_neigh_seconds regressed from 0.31150 to 0.31711 (+1.80% vs incumbent), so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent.] (7adf3bfc72a8, weighted_median_neigh_seconds=0.31711)
- [2026-04-12T17:00:50.342308Z] rejected: Replace the incumbent molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` special handling with a direct encoded-two-special tag lookup path when `nspecial[i][2] <= 2`, bypassing the hot `molecule[j]`/`find_special()` path for non-matching neighbors while preserving the incumbent fallback and ghost minimum-image behavior. [Correctness held, but the candidate regressed weighted_median_neigh_seconds from 0.31150 to 0.31711 (+1.80% vs incumbent), so 8c29b124a2c68bbc7efea2fbd01f26b5ad40288a remains incumbent.] (7adf3bfc72a8, weighted_median_neigh_seconds=0.31711)
- [2026-04-12T17:25:36.389837Z] rejected: Add explicit `jnext` traversal and light next-neighbor coordinate prefetching to the incumbent `NPairBin<1,1,0,0,0>::build()` hot path, alongside local `_noalias` aliases for bin traversal arrays, while preserving the accepted single-path molecular/no-exclusion logic. [Correct and faster at 0.30666 neigh seconds, but the 1.55% gain versus incumbent 8c29b124a2c68bbc7efea2fbd01f26b5ad40288a misses the 2.0% promotion gate.] (e536920bfbe5, weighted_median_neigh_seconds=0.30666)
- [2026-04-12T17:40:55.315946Z] rejected: Combine explicit jnext traversal and next-neighbor coordinate prefetching with cached periodic half-box checks, bin-array noalias aliases, and per-neighbor coordinate/tag/molecule caching in the incumbent NPairBin&lt;1,1,0,0,0&gt; hot path [Correctness held and weighted_median_neigh_seconds improved to 0.30618, but the 1.71% gain versus incumbent missed the benchmark contract’s 2.0% promotion threshold.] (e5eef089d2d0, weighted_median_neigh_seconds=0.30618)
- [2026-04-12T18:01:37.081991Z] rejected: Refine the single-path molecular half/bin/newton builder in src/npair_bin.cpp with explicit jnext traversal, x-only next-neighbor prefetch, cached half-box minimum-image checks, bin/list _noalias aliases, and delayed generic-only setup after the fast-path return [Correctness held and weighted_median_neigh_seconds improved to 0.30551, but the 1.92% gain versus incumbent missed the 2.0% promotion threshold.] (d5b0238dc967, weighted_median_neigh_seconds=0.30551)
- [2026-04-12T18:19:00.616509Z] rejected: Combine iteration-16 style `NPairBin<1,1,0,0,0>` jnext/prefetch/half-box fast-path refinements with contiguous `Neighbor` `xhold` copy and `check_distance()` alias cleanup [Correctness passed and weighted_median_neigh_seconds improved to 0.30695, but the 1.46% gain versus incumbent missed the benchmark contract’s 2.0% promotion threshold.] (4a9dda87cf45, weighted_median_neigh_seconds=0.30695)
- [2026-04-12T18:38:00.346929664Z] iter_0018 review: accepted candidate `08f5ab4a4e026b06a3d38c7e3ebd6ecca2a25635` after authoritative benchmarking. Hypothesis was that keeping the accepted iteration-11 molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` fast-path semantics but switching the self-bin and stencil walks to explicit `jnext` linked-list traversal, exposing bin/list arrays through local `_noalias` aliases, and delaying generic-only setup until after the fast-path return would cut traversal and aliasing overhead without the extra structural cost of the larger iteration-14/16/17 bundles. Changed files: `src/npair_bin.cpp` (43 insertions, 17 deletions). Benchmark outcome: correctness passed for both cases with zero failures; `weighted_median_neigh_seconds` improved from 0.31150 to 0.29094 (-6.60% vs incumbent, -19.54% vs baseline), with `test-16r-short` at `neigh_seconds=1.0108`, `test-64r-short` at `neigh_seconds=0.28529`, `weighted_median_loop_seconds=7.31879`, and `peak_rss_mb=157.09`. Lesson learned: the explicit linked-list traversal plus focused bin/list alias cleanup is a materially better direction than the heavier prefetch/half-box/caching bundles tried in iterations 14-17; simplifying the hot traversal shape and deferring generic-only work gave the compiler/runtime a larger win than stacking more micro-optimizations onto the old structure. Decision: accept for incumbent promotion because the candidate is correct and clears the benchmark contract's 2.0% minimum relative improvement threshold by a wide margin, so `08f5ab4a4e026b06a3d38c7e3ebd6ecca2a25635` becomes the new incumbent. Next hypothesis: treat the iteration-18 explicit-`jnext`/`_noalias` fast-path shape as the new base and probe adjacent, similarly local opportunities that compose with it, such as remaining same-molecule special handling or owned-versus-ghost bin-walk asymmetries, while avoiding a return to broad prefetch/half-box/cross-file bundles unless they show standalone value on top of this new incumbent.
- [2026-04-12T18:38:00.346929664Z] accepted: Use explicit `jnext` linked-list traversal with bin/list `_noalias` aliases and delayed generic-only setup in the incumbent `NPairBin<1,1,0,0,0>` fast path [Correctness held and `weighted_median_neigh_seconds` improved from 0.31150 to 0.29094 (-6.60% vs incumbent), comfortably clearing the 2.0% promotion gate so `08f5ab4a4e026b06a3d38c7e3ebd6ecca2a25635` becomes the new incumbent.] (08f5ab4a4e02, weighted_median_neigh_seconds=0.29094)
- [2026-04-12T18:40:58.834956Z] accepted: Use explicit `jnext` linked-list traversal with bin/list `_noalias` aliases and delayed generic-only setup in the incumbent `NPairBin<1,1,0,0,0>` fast path [Correctness passed and weighted_median_neigh_seconds dropped from 0.31150 to 0.29094 (-6.60% vs incumbent), comfortably clearing the 2.0% promotion gate.] (08f5ab4a4e02, weighted_median_neigh_seconds=0.29094)
- [2026-04-12T18:56:23.053779823Z] iter_0019 review: accepted candidate `11db748934527306a5e9d2d8c0c51eb47b03158a` after authoritative benchmarking. Hypothesis was that isolating `Neighbor` movement-tracking cleanup in `src/neighbor.cpp` by scanning contiguous `x`/`xhold` arrays in `check_distance()` and bulk-copying `xhold` in `build()` would trim rebuild-time coordinate traffic enough to complement the accepted iteration-18 `NPairBin` fast path without perturbing it. Changed files: `src/neighbor.cpp` (37 insertions, 8 deletions). Benchmark outcome: correctness passed for both cases with zero failures; `weighted_median_neigh_seconds` improved from 0.29094 to 0.28487 (-2.09% vs incumbent, -21.22% vs baseline), with `test-16r-short` at `neigh_seconds=1.0002`, `test-64r-short` at `neigh_seconds=0.28376`, `weighted_median_loop_seconds=7.27141`, and `peak_rss_mb=156.53`. Lesson learned: the earlier iteration-17 cross-file bundle understated the value of `Neighbor` movement-tracking cleanup; when applied alone on top of the strong iteration-18 `npair_bin` shape, the contiguous scan and bulk-copy changes clear the promotion gate and improve overall loop time, showing there was still standalone headroom in `Neighbor` after all. Decision: accept for incumbent promotion because the candidate is correct and clears the benchmark contract's 2.0% minimum relative improvement threshold, so `11db748934527306a5e9d2d8c0c51eb47b03158a` becomes the new incumbent. Next hypothesis: treat the accepted iteration-19 `Neighbor` movement-tracking cleanup as the new base and probe other similarly self-contained `Neighbor` rebuild-accounting or data-movement costs, while keeping the accepted iteration-18 `npair_bin` fast path unchanged unless a future cross-file combination shows clear standalone value first.
- [2026-04-12T18:56:23.053779823Z] accepted: Optimize Neighbor movement tracking by scanning contiguous `x`/`xhold` arrays in `check_distance()` and bulk-copying `xhold` in `build()` while keeping the incumbent `npair_bin` fast path unchanged [Correctness passed and `weighted_median_neigh_seconds` dropped from 0.29094 to 0.28487 (-2.09% vs incumbent), clearing the 2.0% promotion gate so `11db748934527306a5e9d2d8c0c51eb47b03158a` becomes the new incumbent.] (11db74893452, weighted_median_neigh_seconds=0.28487)
- [2026-04-12T18:59:02.647869Z] accepted: Optimize Neighbor movement tracking by scanning contiguous x/xhold arrays in check_distance() and bulk-copying xhold in build() while keeping the incumbent npair_bin fast path unchanged [Correctness held and weighted_median_neigh_seconds improved from 0.29094 to 0.28487 (-2.09% vs incumbent), clearing the 2.0% promotion gate.] (11db74893452, weighted_median_neigh_seconds=0.28487)
- [2026-04-12T20:26:57.210945Z] worker_incomplete: optimize iteration 20 [worker loop did not finish cleanly: incomplete_max_iterations] (11db74893452, weighted_median_neigh_seconds=nan)
- [2026-04-12T20:45:47.305528Z] rejected: Widen Neighbor::check_distance() to 8-atom unrolled blocks and compare one blockwise max displacement squared against deltasq, keeping the incumbent semantics while reducing hot-loop branch frequency [Correctness held, but weighted_median_neigh_seconds regressed from 0.28487 to 0.28995 (+1.78% vs incumbent), so 11db74893452 remains incumbent.] (e35c58b86e07, weighted_median_neigh_seconds=0.28995)
- [2026-04-12T21:03:33.513135Z] rejected: Specialize the incumbent `NPairBin<1,1,0,0,0>::build()` fast path for all-zero special-bond weights so same-molecule pairs use a cheaper boolean special-neighbor membership test while preserving the existing different-molecule accept path and periodic-image ghost handling. [Correctness held, but the all-zero special-neighbor fast path slightly regressed `weighted_median_neigh_seconds` from 0.28487 to 0.28503, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` stays.] (708f9b2d16b1, weighted_median_neigh_seconds=0.28503)
- [2026-04-12T21:34:19.641634Z] rejected: Add a runtime uniform-cutoff fast path to the specialized `NPairBin<1,1,0,0,0>` molecular/no-exclusion builder so equal-cutoff systems skip per-pair `type[j]` and cutoff-matrix loads, while retaining the local Neighbor cadence cleanups that compiled and smoked correctly. [Correctness held, but the candidate’s 0.06% improvement to 0.28469 `weighted_median_neigh_seconds` is far below the 2.0% promotion threshold, so incumbent `11db74893452` stays.] (a3f1052997d8, weighted_median_neigh_seconds=0.28469)
- [2026-04-12T21:54:07Z] iter_0024 review: rejected candidate `84babbbb36e3b112c5751b47e12a078b73110c15` after authoritative benchmarking. Hypothesis was that adding an axis-aligned early reject to the specialized `NPairBin<1,1,0,0,0>` fast path would let pairs that already exceed `neighbor->cuttype[itype]` on one coordinate skip the `type[j]` load and full squared-distance check, reducing per-pair work in the incumbent hot loop. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.49372 (+73.31% vs incumbent, -36.55% vs baseline), with `test-16r-short` at `neigh_seconds=1.7855`, `test-64r-short` at `neigh_seconds=0.49217`, `weighted_median_loop_seconds=7.50006`, and `peak_rss_mb=156.69`. Lesson learned: on top of the accepted iteration-18/19 incumbent, inserting extra axis-wise cutoff comparisons into the hot path is strongly counterproductive; the added branches and per-coordinate tests cost far more than the avoided `type[j]` load and rare full-distance work, and the regression appears across both held-out ranks rather than as noise. Decision: reject for incumbent promotion because the candidate is correct but catastrophically slower on the primary metric and does not satisfy the benchmark contract's 2.0% minimum-improvement gate, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` remains in place. Next hypothesis: avoid further per-pair early-reject branching inside the incumbent `NPairBin<1,1,0,0,0>` fast path and instead target optimizations that remove unconditional traversal or data-movement work without adding new coordinate-wise conditionals, either in the existing `npair_bin` traversal shape or in isolated `Neighbor` rebuild-accounting code.
- [2026-04-12T21:54:07Z] rejected: Add an axis-aligned early reject in the specialized `NPairBin<1,1,0,0,0>` fast path so pairs exceeding `neighbor->cuttype[itype]` on any single coordinate skip the `type[j]` load and full squared-distance check. [Correctness held, but `weighted_median_neigh_seconds` regressed sharply from 0.28487 to 0.49372 (+73.31% vs incumbent), so incumbent `11db74893452` stays.] (84babbbb36e3, weighted_median_neigh_seconds=0.49372)
- [2026-04-12T21:56:46.634828Z] rejected: Add an axis-aligned early reject in the specialized `NPairBin<1,1,0,0,0>` fast path so pairs exceeding `neighbor->cuttype[itype]` on any single coordinate skip the `type[j]` load and full squared-distance check. [Correctness held, but the axis-aligned early-reject path regressed weighted_median_neigh_seconds from 0.28487 to 0.49372 (+73.31% vs incumbent), so the incumbent stays.] (84babbbb36e3, weighted_median_neigh_seconds=0.49372)
- [2026-04-12T22:21:56.843926Z] rejected: Isolate the iteration-23 Neighbor cadence fast-check by caching the benchmark-common every=1/delay=0/dist_check=yes no-box path in Neighbor::decide()/check_distance() without any accompanying npair_bin specialization [Correctness held, but the isolated Neighbor cadence-cache path regressed weighted_median_neigh_seconds versus incumbent and missed the 2.0% promotion gate.] (b78acabc90a5, weighted_median_neigh_seconds=0.28592)
- [2026-04-12T22:46:57.607368Z] rejected: Add a benchmark-common `NPairBin<1,1,0,0,0>` fast path for the uniform-cutoff plus all-special-encoded case, using encoded-special lookup and pointer-style neighbor writes to remove generic cutoff and special-flag work from the hot loop. [Correctness passed, but the candidate’s weighted_median_neigh_seconds improved only 0.15% versus incumbent, far below the 2.0% promotion threshold.] (ba18979d7807, weighted_median_neigh_seconds=0.28444)
- [2026-04-12T23:27:18Z] rejected: Cache j-side neighbor coords/tag/molecule once and inline orthogonal half-box minimum-image checks in the specialized `NPairBin<1,1,0,0,0>` molecular/no-exclusion fast path. [Correctness held, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.28559 (+0.25% vs incumbent), so incumbent `11db74893452` stays.] (321fb756a836, weighted_median_neigh_seconds=0.28559)
- [2026-04-12T23:30:40.819488Z] rejected: Cache j-side neighbor coords/tag/molecule once and inline orthogonal half-box minimum-image checks in the specialized NPairBin&lt;1,1,0,0,0&gt; molecular/no-exclusion fast path [Correctness held, but weighted_median_neigh_seconds regressed 0.25% versus incumbent and missed the 2.0% promotion gate.] (321fb756a836, weighted_median_neigh_seconds=0.28559)
## Long-Term Memory (Persistent)
### Benchmark contract
- Benchmark file: `.fermilink-optimize/benchmark.worker.yaml`
### Accepted experiments
- [2026-04-12T06:04:05.996182Z] baseline (e7c0ed95a333, weighted_median_neigh_seconds=0.36158)
- [2026-04-12T06:47:30Z] Candidate `32b495fdb66b` (`src/npair_bin.cpp`) specialized `NPairBin<1,1,0,0,0>::build()` for the orthogonal half/bin/newton/non-size hot path, adding a dedicated fast path that hoists per-atom cutoff and special-bond setup outside the innermost neighbor loops and removes generic `SIZE`/`ATOMONLY`/`TRI` branching from this benchmark-dominant variant. Hypothesis: a narrow compile-time specialization of the exact hot template instance would cut branch and setup overhead without the broader structural cost seen previously. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` improved by 0.02623 s (-7.25%) from 0.36158 to 0.33535, with `test-16r-short` at `neigh_seconds=1.1465` and `test-64r-short` at `neigh_seconds=0.32694`. Lesson learned: targeted specialization of the real hot instantiation can pay off, while the broader traversal split attempted in iteration 1 added enough structure to lose performance. Decision: accept and promote this candidate to incumbent. Next hypothesis: continue with similarly local hot-path work in `NPairBin::build()`, such as hoisting stencil/bin invariants or trimming repeated exclusion/minimum-image overhead, but avoid shared helper layers or control-flow refactors that reintroduce dispatch cost.
- [2026-04-12T06:48:17.589263Z] Specialize the orthogonal half/bin/newton neighbor build in `src/npair_bin.cpp` to remove generic branches and hoist per-atom special-bond/cutoff setup out of the innermost loop [Correctness held with zero failures, and the targeted `src/npair_bin.cpp` hot-path specialization improved `weighted_median_neigh_seconds` from 0.36158 to 0.33535 (-7.25%), so the candidate becomes the new incumbent.] (32b495fdb66b, weighted_median_neigh_seconds=0.33535)
- [2026-04-12T07:01:49Z] Candidate `380bf2b0146b` (`src/npair_bin.cpp`) added an early fast path in `NPairBin<1,1,0,0,0>::build()` for `!exclude && molecular == Atom::MOLECULAR`, keeping the accepted iteration-2 traversal structure but removing repeated `special_i` setup and generic exclusion handling from the hot orthogonal half/bin/newton loops for the benchmark's standard molecular runtime case. Hypothesis: layering a narrow runtime specialization on top of the successful template specialization would cut remaining branch/setup overhead without changing list semantics or disturbing slower paths. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` improved by 0.00693 s (-2.07%) versus incumbent `32b495fdb66b` and by 0.03316 s (-9.17%) versus baseline, reaching 0.32842. Lesson learned: after the dominant template instance is specialized, benchmark-aligned runtime guards can still deliver measurable gains, but the margin is now small enough that future wins need to stay tightly scoped and explicit about their runtime assumptions. Decision: accept and promote this candidate to incumbent. Next hypothesis: keep working inside the same `NPairBin<1,1,0,0,0>::build()` fast path and test whether `minimum_image_check`, `find_special`, or stencil/bin invariants can be hoisted or short-circuited further without reintroducing the broader control-flow costs that hurt iteration 1.
- [2026-04-12T07:03:15.541928Z] Add a benchmark-specific fast path in `NPairBin<1,1,0,0,0>::build()` for standard molecular systems without neighbor exclusions, removing repeated `special_i` setup and `exclude` checks from the hot orthogonal half/bin/newton loops. [Candidate improves weighted_median_neigh_seconds from 0.33535 to 0.32842 with correctness intact, so it becomes the new incumbent.] (380bf2b0146b, weighted_median_neigh_seconds=0.32842)
- [2026-04-12T14:54:59Z] Candidate `c4128d3970b2` (`src/npair_bin.cpp`) refined the accepted iteration-3 molecular/no-exclusion fast path by restoring the iteration-4 different-molecule early accept and adding an owned same-molecule encoded-special shortcut when `special_flag[1..3] == 2`, allowing the hot loop to skip both generic special handling on cross-molecule pairs and `minimum_image_check()` on owned encoded specials. Hypothesis: iterations 4 and 5 each exposed a real but sub-threshold slice of residual special-neighbor overhead, so combining those compatible shortcuts inside the incumbent fast path would remove a larger contiguous cost and finally clear the controller's 2.0% promotion gate without changing correctness. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` improved by 0.00873 s (-2.66%) versus incumbent `380bf2b0146b` and by 0.04189 s (-11.59%) versus baseline, reaching 0.31969, with `test-16r-short` at `neigh_seconds=1.1127` and `test-64r-short` at `neigh_seconds=0.32346`. Lesson learned: the rejected iterations 4 and 5 were not dead ends individually; each isolated a real residual cost, and combining the complementary shortcuts inside the stable fast path was enough to exceed the promotion threshold without needing a broader refactor. Decision: accept and promote this candidate to incumbent. Next hypothesis: keep working inside `NPairBin<1,1,0,0,0>::build()`, but target another paired simplification that removes a broader chunk of same-molecule work, such as reducing repeated `tag[j]`, `molecule[j]`, or cutoff loads while streamlining the remaining `find_special()` path for non-encoded or ghost cases.
- [2026-04-12T14:54:59Z] Refine the incumbent `NPairBin<1,1,0,0,0>::build()` fast path by early-accepting different-molecule pairs and skipping `minimum_image_check()` for owned same-molecule specials when `special_flag[1..3] == 2`. [Correctness held and weighted_median_neigh_seconds improved to 0.31969, a 2.66% gain versus incumbent 380bf2b0146b that clears the 2.0% promotion gate, so the candidate becomes the new incumbent.] (c4128d3970b2, weighted_median_neigh_seconds=0.31969)
- [2026-04-12T14:57:39.340307Z] Refine the incumbent `NPairBin<1,1,0,0,0>::build()` fast path by early-accepting different-molecule pairs and skipping `minimum_image_check()` for owned same-molecule specials when `special_flag[1..3] == 2`. [Correctness held and weighted_median_neigh_seconds improved from 0.32842 to 0.31969 (-2.66% vs incumbent), clearing the benchmark contract’s 2.0% promotion gate.] (c4128d3970b2, weighted_median_neigh_seconds=0.31969)
- [2026-04-12T16:21:24.592364Z] Use contiguous `_noalias` aliases for hot coordinate/type/tag/molecule arrays in the incumbent `NPairBin<1,1,0,0,0>::build()` path [Correctness held and the candidate cut weighted_median_neigh_seconds from 0.31969 to 0.31150 (-2.56% vs incumbent), clearing the 2.0% promotion gate.] (8c29b124a2c6, weighted_median_neigh_seconds=0.3115)
- [2026-04-12T18:40:58.834956Z] Use explicit `jnext` linked-list traversal with bin/list `_noalias` aliases and delayed generic-only setup in the incumbent `NPairBin<1,1,0,0,0>` fast path [Correctness passed and weighted_median_neigh_seconds dropped from 0.31150 to 0.29094 (-6.60% vs incumbent), comfortably clearing the 2.0% promotion gate.] (08f5ab4a4e02, weighted_median_neigh_seconds=0.29094)
- [2026-04-12T18:56:23.053779823Z] Candidate `11db748934527306a5e9d2d8c0c51eb47b03158a` (`src/neighbor.cpp`) optimized `Neighbor` movement tracking by scanning contiguous `x`/`xhold` coordinate arrays in `check_distance()` and bulk-copying `xhold` during `build()`, while leaving the accepted iteration-18 `NPairBin<1,1,0,0,0>` fast path untouched. Hypothesis: isolating the `Neighbor`-side movement-tracking cleanup on top of the strong iteration-18 base would recover rebuild-time memory-traffic savings without diluting them through additional `npair_bin` changes, unlike the broader iteration-17 bundle. Changed files: `src/neighbor.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` improved by 0.00607 s (-2.09%) versus incumbent `08f5ab4a4e026b06a3d38c7e3ebd6ecca2a25635` and by 0.07671 s (-21.22%) versus baseline, finishing at 0.28487 with `test-16r-short` at `neigh_seconds=1.0002`, `test-64r-short` at `neigh_seconds=0.28376`, `weighted_median_loop_seconds=7.27141`, and `peak_rss_mb=156.53`. Lesson learned: `Neighbor` movement-tracking work still had standalone headroom after iteration 18; the earlier cross-file miss was more about bundling than about this direction being exhausted. Decision: accept and promote this candidate to incumbent. Next hypothesis: keep the new incumbent's `Neighbor` cleanup and iteration-18 `npair_bin` shape intact, then test adjacent self-contained `Neighbor` rebuild-accounting or data-movement reductions before attempting another cross-file combination.
- [2026-04-12T18:56:23.053779823Z] Optimize Neighbor movement tracking by scanning contiguous `x`/`xhold` arrays in `check_distance()` and bulk-copying `xhold` in `build()` while keeping the incumbent `npair_bin` fast path unchanged [Correctness passed and `weighted_median_neigh_seconds` dropped from 0.29094 to 0.28487 (-2.09% vs incumbent), clearing the 2.0% promotion gate so `11db748934527306a5e9d2d8c0c51eb47b03158a` becomes the new incumbent.] (11db74893452, weighted_median_neigh_seconds=0.28487)
- [2026-04-12T18:59:02.647869Z] Optimize Neighbor movement tracking by scanning contiguous x/xhold arrays in check_distance() and bulk-copying xhold in build() while keeping the incumbent npair_bin fast path unchanged [Correctness held and weighted_median_neigh_seconds improved from 0.29094 to 0.28487 (-2.09% vs incumbent), clearing the 2.0% promotion gate.] (11db74893452, weighted_median_neigh_seconds=0.28487)
### Rejected patterns
- [2026-04-12T07:20:10Z] Candidate `3bd7cbc7ee03` (`src/npair_bin.cpp`) extended the accepted iteration-3 `!exclude && molecular == Atom::MOLECULAR` fast path by loading `molecule[i]` once and bypassing `find_special()` and `minimum_image_check()` for different-molecule pairs, while leaving same-molecule pairs on the original special-neighbor logic. Hypothesis: in this benchmark's water system, most cross-molecule pairs should not require special-bond handling, so skipping those checks in the hot loop would shave remaining neighbor-build overhead without affecting correctness. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` improved by 0.00555 s (-1.69%) versus incumbent `380bf2b0146b` and by 0.03871 s (-10.71%) versus baseline, with `test-16r-short` at `neigh_seconds=1.1262` and `test-64r-short` at `neigh_seconds=0.33031`. Lesson learned: the remaining different-molecule `find_special`/minimum-image work is real but already close to the noise floor for controller promotion, so micro-optimizations in the incumbent fast path now need to attack a somewhat larger residual cost to clear the 2% acceptance gate. Decision: reject for incumbent promotion despite correctness and a small speedup, because the benchmark contract requires at least a 2.0% relative improvement. Next hypothesis: stay inside the same benchmark-specific fast path and target a larger chunk of residual work, such as reducing repeated molecule/tag loads or simplifying the same-molecule special-pair branch structure, while avoiding the broader traversal/control-flow refactors that regressed iteration 1.
- [2026-04-12T06:30:34Z] Candidate `2d1eda35b944` (`src/npair_bin.cpp`) split the orthogonal half-bin/newton self-bin walk from the remaining stencil traversal and cached per-atom cutoff/special-bond pointers. Benchmark outcome: correctness ok with zero failures, but the primary metric worsened by 0.11554 s (+31.95%) from 0.36158 to 0.47712; case `test-16r-short` reported `neigh_seconds=1.649` and case `test-64r-short` reported `neigh_seconds=0.47712`. Lesson learned: this refactor did not pay for itself on the benchmarked path; the added structure and extra hot-loop machinery outweighed the avoided repeated lookups/branches, so this candidate must not replace the incumbent.
- [2026-04-12T06:31:16.877051Z] Split the orthogonal half-bin/newton self-bin traversal in `npair_bin.cpp` and cache per-atom cutoff/special-bond lookups to reduce repeated hot-path branches and memory accesses [Correctness held, but the `src/npair_bin.cpp` refactor regressed `weighted_median_neigh_seconds` from 0.36158 to 0.47712 (+31.95%), so the incumbent stays.] (2d1eda35b944, weighted_median_neigh_seconds=0.47712)
- [2026-04-12T07:21:42.202916Z] Skip `find_special()` and `minimum_image_check()` for different-molecule pairs inside the incumbent `NPairBin<1,1,0,0,0>::build()` molecular/no-exclusion fast path, keeping same-molecule pairs on the original special-neighbor handling. [Correctness held and weighted_median_neigh_seconds improved to 0.32287, but the -1.69% gain versus incumbent missed the benchmark contract’s 2.0% minimum improvement threshold, so 380bf2b0146b remains incumbent.] (3bd7cbc7ee03, weighted_median_neigh_seconds=0.32287)
- [2026-04-12T07:38:02Z] Candidate `1a1935b59c00` (`src/npair_bin.cpp`) added a narrower early fast path in `NPairBin<1,1,0,0,0>::build()` for the benchmark's `!exclude && molecular == Atom::MOLECULAR && atom->maxspecial <= 2` case, bypassing generic `find_special()` scans for different-molecule pairs and replacing same-molecule special detection with direct one- or two-entry special-tag checks. Hypothesis: because the benchmark reports at most two special neighbors per atom, specializing that case would remove enough residual special-neighbor overhead to beat the controller threshold without changing list semantics. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` improved by 0.00458 s (-1.39%) versus incumbent `380bf2b0146b` and by 0.03774 s (-10.44%) versus baseline, reaching 0.32384, with `test-16r-short` at `neigh_seconds=1.1416` and `test-64r-short` at `neigh_seconds=0.32717`. Lesson learned: narrowing same-molecule special handling beyond iteration 4 still buys only a sub-threshold increment, and the 16-rank case regressed relative to iteration 4 even while the weighted median improved, so the remaining generic special-neighbor scan cost is probably too small or too rank-sensitive to clear the 2% gate by itself. Decision: reject for incumbent promotion despite correctness and a modest speedup, because the benchmark contract requires at least a 2.0% relative improvement. Next hypothesis: target a larger contiguous cost center inside the incumbent fast path, such as reducing repeated cutoff/tag/molecule loads or simplifying same-molecule special handling and minimum-image work together, rather than adding still narrower special-case branches around `find_special()`.
- [2026-04-12T07:39:27.276335Z] Add a `maxspecial <= 2` molecular/no-exclusion fast path in `NPairBin<1,1,0,0,0>::build()` that bypasses generic `find_special()` scans for different-molecule pairs and uses direct same-molecule special-tag matching. [Correctness held and weighted_median_neigh_seconds improved to 0.32384, but the 1.39% gain versus incumbent 380bf2b0146b missed the benchmark contract’s 2.0% minimum improvement threshold.] (1a1935b59c00, weighted_median_neigh_seconds=0.32384)
- [2026-04-12T15:10:12Z] Candidate `d89e6f820474` (`src/npair_bin.cpp`) kept the iteration-6 molecular/no-exclusion fast path intact but replaced the remaining same-molecule `find_special()` scan with predecoded one- or two-entry encoded-special tag checks when `nspecial[i][2] <= 2`, preserving the incumbent different-molecule early accept and ghost `minimum_image_check()` fallback. Hypothesis: after iteration 6, the residual same-molecule `find_special()` call looked like the next removable hot-path slice, and turning it into direct tag comparisons for the benchmark-common two-entry case might stack a final low-risk win on top of the incumbent shortcut set. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` regressed by 0.00457 s (+1.43%) versus incumbent `c4128d3970b2`, although it remained 0.03732 s (-10.32%) better than baseline, finishing at 0.32426 with `test-16r-short` at `neigh_seconds=1.1405` and `test-64r-short` at `neigh_seconds=0.32289`. Lesson learned: another isolated micro-specialization of same-molecule special lookup did not compose with the incumbent fast path; the extra predecode/state setup cost appears to outweigh the saved scan on the held-out objective, and the small 64-rank gain was more than erased by 16-rank slowdown. Decision: reject for incumbent promotion because the candidate both regressed the objective and failed the benchmark contract’s 2.0% improvement gate. Next hypothesis: stop pursuing standalone `find_special()` micro-shortcuts for the `nspecial<=2` case, and instead test a broader simplification that removes repeated same-molecule loads or branch work across both encoded and fallback paths, such as caching `tag[j]`/`molecule[j]`/cutoff data once per neighbor candidate while keeping the incumbent control flow shape.
- [2026-04-12T15:11:26.215521Z] Replace the remaining same-molecule `find_special()` scan in the incumbent `NPairBin<1,1,0,0,0>::build()` fast path with direct two-entry encoded-special tag checks when `nspecial[i][2] <= 2`, preserving the incumbent different-molecule early accept and ghost minimum-image fallback. [Correctness held, but the candidate regressed weighted_median_neigh_seconds from 0.31969 to 0.32426 (+1.43% vs incumbent), so c4128d3970b2 remains incumbent.] (d89e6f820474, weighted_median_neigh_seconds=0.32426)
- [2026-04-12T15:24:36Z] Candidate `d381c38751ec` (`src/npair_bin.cpp`) split the incumbent molecular/no-exclusion neighbor build into encoded-special and generic variants, using an encoded-only special lookup and special-hit-first branch ordering to trim the residual same-molecule hot-loop branching. Hypothesis: after iteration 7 showed that a direct encoded-special lookup alone was not enough, separating the encoded-special case into its own path would cut the remaining same-molecule branch overhead without disturbing correctness. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` regressed by 0.01360 s (+4.25%) versus incumbent `c4128d3970b2`, though it remained 0.02829 s (-7.82%) better than baseline, finishing at 0.33329 with `test-16r-short` at `neigh_seconds=1.1864` and `test-64r-short` at `neigh_seconds=0.33329`. Lesson learned: splitting the hot path into encoded-special and generic variants added more control-flow and code-shape cost than it removed; the incumbent's straight-line fast path composes better than a special-hit-first branch structure, and same-molecule special handling is now sensitive to extra path duplication. Decision: reject for incumbent promotion because the candidate regressed the objective and failed the benchmark contract's 2.0% improvement gate, so `c4128d3970b2` remains incumbent. Next hypothesis: keep the iteration-6 control-flow shape intact and target broader repeated-load cleanup inside that single fast path, such as hoisting or reusing `tag[j]`, `molecule[j]`, cutoff, or ownership data across same-molecule checks instead of splitting encoded-special and generic handling into separate variants.
- [2026-04-12T15:25:53.198255Z] Split the benchmark-hot molecular/no-exclusion neighbor build into encoded-special and generic variants in `src/npair_bin.cpp`, using an encoded-only special lookup and special-hit-first branch order to cut residual same-molecule hot-loop branching. [Correctness held, but splitting the same-molecule fast path into encoded-special and generic variants regressed weighted_median_neigh_seconds from 0.31969 to 0.33329 (+4.25%), so c4128d3970b2 remains incumbent.] (d381c38751ec, weighted_median_neigh_seconds=0.33329)
- [2026-04-12T15:41:17Z] Candidate `2894990122f9` (`src/npair_bin.cpp`) kept the iteration-6 single fast-path shape but cached `j` ownership, `tag[j]`, and `molecule[j]` once per neighbor candidate and added a local encoded-only special lookup helper so the encoded-special case could avoid the generic `find_special()` call. Hypothesis: broadening the incumbent's repeated-load cleanup and specializing the encoded path inside the same control-flow shape would recover a larger chunk of residual same-molecule overhead than the rejected iterations 7 and 8 while preserving correctness. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` improved by 0.00099 s (-0.31%) versus incumbent `c4128d3970b2` and by 0.04288 s (-11.86%) versus baseline, finishing at 0.31870 with `test-16r-short` at `neigh_seconds=1.1165` and `test-64r-short` at `neigh_seconds=0.31988`. Lesson learned: caching per-neighbor ownership/tag/molecule data inside the incumbent fast path composes better than path splitting, but the gain is still tiny and rank-sensitive: the 64-rank case improved versus the incumbent while the 16-rank case regressed slightly, leaving the overall win well below the 2.0% promotion gate. Decision: reject for incumbent promotion despite correctness and a marginal speedup, because the benchmark contract requires at least a 2.0% relative improvement. Next hypothesis: stop spending iterations on isolated same-path bookkeeping and encoded-special micro-optimizations alone; future candidates should either remove a larger contiguous block of same-molecule work inside the existing straight-line fast path or pivot to another measurable rebuild cost center that can plausibly beat the promotion threshold without reintroducing control-flow duplication.
- [2026-04-12T15:43:00.090847Z] Cache per-neighbor ownership/tag/molecule values and use an encoded-only special lookup inside the incumbent molecular/no-exclusion half/bin/newton fast path in `src/npair_bin.cpp` [Correctness held, but 0.31870 improves on incumbent 0.31969 by only 0.31%, below the 2.0% promotion threshold.] (2894990122f9, weighted_median_neigh_seconds=0.3187)
- [2026-04-12T16:01:11.348123450Z] Candidate `b21ee6319e89` (`src/npair_bin.cpp`) split the incumbent molecular/no-exclusion self-bin walk in `NPairBin<1,1,0,0,0>::build()` into owned and ghost tails so owned self-bin neighbors could bypass repeated ghost-order checks while leaving the accepted stencil traversal unchanged. Hypothesis: after the rejected bookkeeping-only cleanup in iteration 9, isolating the self-bin-specific ghost-order test into a ghost-only tail might trim a larger contiguous slice of branch work in the benchmark-hot self-bin path without disturbing correctness or the incumbent stencil path. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` regressed by 0.00241 s (+0.75%) versus incumbent `c4128d3970b2`, though it remained 0.03948 s (-10.92%) better than baseline, finishing at 0.32210 with `test-16r-short` at `neigh_seconds=1.1260` and `test-64r-short` at `neigh_seconds=0.32221`. Lesson learned: even a narrow self-bin tail split is still too much structural change for this hot path; the tiny 64-rank improvement did not offset the 16-rank slowdown, which suggests the saved ghost-order checks are lower value than the added control-flow/code-shape cost. Decision: reject for incumbent promotion because the candidate regressed the objective and failed the benchmark contract's 2.0% improvement gate, so `c4128d3970b2` remains incumbent. Next hypothesis: keep the incumbent single-path shape for both self-bin and stencil handling, and look for broader straight-line simplifications inside that shape, such as removing a larger contiguous block of same-molecule/self-bin work or pivoting to another measurable rebuild cost center instead of splitting tails.
- [2026-04-12T16:01:11.348123450Z] Split the incumbent molecular/no-exclusion self-bin walk in `NPairBin<1,1,0,0,0>::build()` into owned and ghost tails so owned self-bin neighbors avoid repeated ghost-order checks while the accepted stencil path stays unchanged. [Correctness held, but weighted_median_neigh_seconds regressed from 0.31969 to 0.32210 (+0.75% vs incumbent), so c4128d3970b2 remains incumbent.] (b21ee6319e89, weighted_median_neigh_seconds=0.3221)
- [2026-04-12T16:02:45.244196Z] Split the incumbent molecular/no-exclusion self-bin walk in `NPairBin<1,1,0,0,0>::build()` into owned and ghost tails so owned self-bin neighbors avoid repeated ghost-order checks while the accepted stencil path stays unchanged. [Correctness held, but weighted_median_neigh_seconds regressed from 0.31969 to 0.32210 (+0.75% vs incumbent), so the candidate cannot replace the incumbent or clear the 2.0% promotion gate.] (b21ee6319e89, weighted_median_neigh_seconds=0.3221)
- [2026-04-12T16:20:21.340794720Z] iter_0011 review: accepted candidate `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` after authoritative benchmarking. Hypothesis was that exposing the incumbent `NPairBin<1,1,0,0,0>::build()` hot arrays as contiguous `_noalias` aliases in `src/npair_bin.cpp` would reduce alias pessimism and repeated memory-access overhead without changing the accepted single-path control-flow shape. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness passed for both cases with zero failures; `weighted_median_neigh_seconds` improved by 0.00819 s (-2.56%) versus incumbent `c4128d3970b2` and by 0.05008 s (-13.85%) versus baseline, finishing at 0.31150 with `test-16r-short` at `neigh_seconds=1.0887` and `test-64r-short` at `neigh_seconds=0.31063`. Lesson learned: this benchmark still responds to memory-access/compiler-visibility cleanup inside the existing fast path, and a narrow aliasing/data-access improvement can outperform recent control-flow reshaping attempts while preserving correctness. Decision: accept for incumbent promotion because the candidate clears the benchmark contract's 2.0% improvement gate with correctness intact, so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` becomes the new incumbent. Next hypothesis: keep the iteration-11 single-path structure and look for adjacent alias/load cleanup opportunities in the same hot loop, such as extending contiguous `_noalias` treatment or one-time local caching to other repeatedly touched per-atom data that does not require splitting traversal paths.
- [2026-04-12T16:20:21.340794720Z] Use contiguous `_noalias` aliases for hot coordinate/type/tag/molecule arrays in the incumbent `NPairBin<1,1,0,0,0>::build()` path [Correctness held, and weighted_median_neigh_seconds improved from 0.31969 to 0.31150 (-2.56% vs incumbent), clearing the 2.0% promotion gate so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` becomes the new incumbent.] (8c29b124a2c68bbc7efea2fbd01f26b5ad40288a, weighted_median_neigh_seconds=0.3115)
- [2026-04-12T16:33:11Z] Candidate `e5d6d4548fe4ab089cc4bc1a24e8a2402cf40f71` (`src/npair_bin.cpp`) extended the accepted iteration-11 molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` fast path with extra `_noalias` aliases for neighbor-structure arrays, one-time caching of neighbor coordinates/tag/molecule values, and cached periodic half-box scalars in place of repeated `domain->minimum_image_check()` member access. Hypothesis: broadening the incumbent alias/load cleanup and caching the minimum-image thresholds inside the same straight-line control-flow shape would stack another meaningful memory-side win on top of iteration 11 without reintroducing the path-splitting regressions from iterations 7, 8, and 10. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` improved by 0.00147 s (-0.47%) versus incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` and by 0.05155 s (-14.26%) versus baseline, finishing at 0.31003 with `weighted_median_loop_seconds=7.29333` and `peak_rss_mb=156.80`. Lesson learned: the same-path alias/load/minimum-image cleanup direction is still valid, but the marginal gain has become too small to clear the controller gate; extending local caching around existing checks produces diminishing returns once the easy alias pessimism has already been removed. Decision: reject for incumbent promotion because the candidate is correct and slightly faster but does not satisfy the benchmark contract's 2.0% minimum relative improvement, so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent. Next hypothesis: stop layering narrowly incremental load/alias cleanups onto the iteration-11 shape unless they remove a larger contiguous block of work; future candidates should either collapse more of the same-molecule/minimum-image handling in the existing straight-line path or pivot to another rebuild cost center with enough headroom to plausibly beat the 2.0% gate.
- [2026-04-12T16:33:11Z] Extend the incumbent `NPairBin<1,1,0,0,0>::build()` hot path with additional neighbor-array aliasing, one-time neighbor coordinate/tag/molecule caching, and cached half-box minimum-image checks [Correctness held and `weighted_median_neigh_seconds` improved from 0.31150 to 0.31003, but the -0.47% gain versus incumbent missed the 2.0% promotion threshold, so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent.] (e5d6d4548fe4ab089cc4bc1a24e8a2402cf40f71, weighted_median_neigh_seconds=0.31003)
- [2026-04-12T16:37:44.430333Z] Extend the incumbent `NPairBin<1,1,0,0,0>::build()` hot path with additional neighbor-array aliasing, one-time neighbor coordinate/tag/molecule caching, and cached half-box minimum-image checks [Correctness held and weighted_median_neigh_seconds improved to 0.31003, but the -0.47% gain versus incumbent missed the benchmark contract’s 2.0% promotion threshold.] (e5d6d4548fe4, weighted_median_neigh_seconds=0.31003)
- [2026-04-12T16:58:48.160626338Z] Candidate `7adf3bfc72a840d0627a8ede4cdb5dadc088e51e` (`src/npair_bin.cpp`) replaced the incumbent molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` special handling with a direct encoded-two-special tag lookup path when `nspecial[i][2] <= 2`, duplicating the self-bin and stencil traversals into a specialized branch that accepts all non-matching tags directly while keeping the incumbent fallback and ghost minimum-image behavior for the remaining cases. Hypothesis: after iteration 12 showed diminishing returns from more alias/load cleanup, bypassing both `molecule[j]` and `find_special()` on the common non-match case for the benchmark's small-special-count atoms would remove a larger contiguous chunk of residual same-molecule overhead without breaking correctness. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness ok with zero failures; `weighted_median_neigh_seconds` regressed by 0.00561 s (+1.80%) versus incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a`, though it remained 0.04447 s (-12.30%) better than baseline, finishing at 0.31711 with `test-16r-short` at `neigh_seconds=1.1073`, `test-64r-short` at `neigh_seconds=0.31711`, `weighted_median_loop_seconds=7.27137`, and `peak_rss_mb=155.77`. Lesson learned: the encoded-two-special bypass direction remains unconvincing in this benchmark once it duplicates loop bodies and adds per-atom setup; unlike iteration 11's alias cleanup, this rewrite slowed both validation cases, which suggests the control-flow/code-shape cost outweighs the saved `molecule[j]`/`find_special()` work. Decision: reject for incumbent promotion because the candidate regressed the primary metric and does not satisfy the benchmark contract's 2.0% improvement gate, so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent. Next hypothesis: avoid further `nspecial <= 2` encoded-special branch duplication unless it can preserve the incumbent's single-path shape while removing a materially larger block of work; otherwise pivot back to broader straight-line memory-access simplifications or another rebuild cost center with enough headroom to clear the controller gate.
- [2026-04-12T16:58:48.160626338Z] Replace the incumbent molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` special handling with a direct encoded-two-special tag lookup path when `nspecial[i][2] <= 2`, bypassing the hot `molecule[j]`/`find_special()` path for non-matching neighbors while preserving the incumbent fallback and ghost minimum-image behavior. [Correctness held, but weighted_median_neigh_seconds regressed from 0.31150 to 0.31711 (+1.80% vs incumbent), so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent.] (7adf3bfc72a8, weighted_median_neigh_seconds=0.31711)
- [2026-04-12T17:00:50.342308Z] Replace the incumbent molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` special handling with a direct encoded-two-special tag lookup path when `nspecial[i][2] <= 2`, bypassing the hot `molecule[j]`/`find_special()` path for non-matching neighbors while preserving the incumbent fallback and ghost minimum-image behavior. [Correctness held, but the candidate regressed weighted_median_neigh_seconds from 0.31150 to 0.31711 (+1.80% vs incumbent), so 8c29b124a2c68bbc7efea2fbd01f26b5ad40288a remains incumbent.] (7adf3bfc72a8, weighted_median_neigh_seconds=0.31711)
- [2026-04-12T17:24:30Z] iter_0014 review: rejected candidate `e536920bfbe543476b44ee9650a5c1bbab0ca8f2` after authoritative benchmarking. Hypothesis was that preserving the accepted iteration-11 single-path molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` logic while switching the bin-chain loops to explicit `jnext` traversal, adding `_noalias` aliases for the bin traversal arrays, and lightly prefetching the next neighbor coordinate would trim pointer-chasing and load-latency overhead without reintroducing the control-flow regressions seen in earlier loop-splitting experiments. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness passed for both cases with zero failures; `weighted_median_neigh_seconds` improved from 0.31150 to 0.30666 (-1.55% vs incumbent, -15.19% vs baseline), with `test-16r-short` at `neigh_seconds=1.0761`, `test-64r-short` at `neigh_seconds=0.31016`, `weighted_median_loop_seconds=7.30497`, and `peak_rss_mb=156.56`. Lesson learned: traversal-scheduling and light prefetching can help when they preserve the incumbent's straight-line structure, but the benefit is modest and appears concentrated in the lower-rank case; by themselves these micro-optimizations do not create enough headroom to clear the contract's 2.0% promotion gate. Decision: reject for incumbent promotion because the candidate is correct and faster but misses the minimum relative improvement threshold, so incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains in place. Next hypothesis: keep the single-path shape and only pursue traversal-side tweaks if they can be paired with a larger same-molecule/minimum-image or bin-walk cost reduction that composes with the incumbent alias cleanup; otherwise pivot to another rebuild cost center with more remaining headroom.
- [2026-04-12T17:24:30Z] Add explicit `jnext` traversal and light next-neighbor coordinate prefetching to the incumbent `NPairBin<1,1,0,0,0>::build()` hot path, alongside local `_noalias` aliases for bin traversal arrays, while preserving the accepted single-path molecular/no-exclusion logic. [Correctness held and weighted_median_neigh_seconds improved from 0.31150 to 0.30666, but the -1.55% gain versus incumbent missed the benchmark contract's 2.0% promotion threshold, so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent.] (e536920bfbe543476b44ee9650a5c1bbab0ca8f2, weighted_median_neigh_seconds=0.30666)
- [2026-04-12T17:25:36.389837Z] Add explicit `jnext` traversal and light next-neighbor coordinate prefetching to the incumbent `NPairBin<1,1,0,0,0>::build()` hot path, alongside local `_noalias` aliases for bin traversal arrays, while preserving the accepted single-path molecular/no-exclusion logic. [Correct and faster at 0.30666 neigh seconds, but the 1.55% gain versus incumbent 8c29b124a2c68bbc7efea2fbd01f26b5ad40288a misses the 2.0% promotion gate.] (e536920bfbe5, weighted_median_neigh_seconds=0.30666)
- [2026-04-12T17:39:06Z] iter_0015 review: rejected candidate `e5eef089d2d030a58d2bc36261429e8330718a00` after authoritative benchmarking. Hypothesis was that composing iteration 14's explicit `jnext` traversal and light next-neighbor coordinate prefetch with iteration 12's cached periodic half-box fields, `_noalias` bin-array aliases, and per-neighbor coordinate/tag/molecule caching in `src/npair_bin.cpp` would stack complementary memory-latency reductions while preserving the accepted iteration-11 single-path molecular/no-exclusion logic. Changed files: `src/npair_bin.cpp` (90 insertions, 26 deletions). Benchmark outcome: correctness passed for both cases with zero failures; `weighted_median_neigh_seconds` improved from 0.31150 to 0.30618 (-1.71% vs incumbent, -15.32% vs baseline), with `test-16r-short` at `neigh_seconds=1.0624`, `test-64r-short` at `neigh_seconds=0.30422`, `weighted_median_loop_seconds=7.34147`, and `peak_rss_mb=156.66`. Lesson learned: the iteration-12 and iteration-14 micro-optimizations do compose and slightly outperform either ingredient alone, but the combined win is still too small to clear the contract's 2.0% promotion gate; remaining headroom in this single-path neighbor hot loop appears too limited for incremental alias/prefetch/cache cleanup alone. Decision: reject for incumbent promotion because the candidate is correct and faster but does not satisfy the benchmark contract's minimum relative improvement threshold, so incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains in place. Next hypothesis: only revisit this traversal-and-memory-access direction if it can be paired with a materially larger reduction in same-molecule special handling or another rebuild cost center with higher upside; otherwise pivot away from compounding micro-optimizations in the current hot path.
- [2026-04-12T17:39:06Z] Combine explicit `jnext` traversal and next-neighbor coordinate prefetching with cached periodic half-box checks, bin-array `_noalias` aliases, and per-neighbor coordinate/tag/molecule caching in the incumbent `NPairBin<1,1,0,0,0>::build()` hot path. [Correctness held and `weighted_median_neigh_seconds` improved from 0.31150 to 0.30618, but the -1.71% gain versus incumbent still missed the 2.0% promotion threshold, so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent.] (e5eef089d2d0, weighted_median_neigh_seconds=0.30618)
- [2026-04-12T17:40:55.315946Z] Combine explicit jnext traversal and next-neighbor coordinate prefetching with cached periodic half-box checks, bin-array noalias aliases, and per-neighbor coordinate/tag/molecule caching in the incumbent NPairBin&lt;1,1,0,0,0&gt; hot path [Correctness held and weighted_median_neigh_seconds improved to 0.30618, but the 1.71% gain versus incumbent missed the benchmark contract’s 2.0% promotion threshold.] (e5eef089d2d0, weighted_median_neigh_seconds=0.30618)
- [2026-04-12T17:59:31.211081701Z] iter_0016 review: rejected candidate `d5b0238dc96732cfe06bed2fb0ac2318b5a04ccb` after authoritative benchmarking. Hypothesis was that refining the accepted single-path molecular/no-exclusion builder in `src/npair_bin.cpp` with explicit `jnext` traversal, x-only next-neighbor prefetch, cached half-box minimum-image checks, bin/list `_noalias` aliases, and delayed generic-only setup after the fast-path return would preserve the incumbent control-flow shape while stacking enough traversal and memory-latency savings to clear the 2.0% promotion gate. Changed files: `src/npair_bin.cpp` (107 insertions, 39 deletions). Benchmark outcome: correctness passed for both cases with zero failures; `weighted_median_neigh_seconds` improved from 0.31150 to 0.30551 (-1.92% vs incumbent, -15.51% vs baseline), with `test-16r-short` at `neigh_seconds=1.0578`, `test-64r-short` at `neigh_seconds=0.30921`, `weighted_median_loop_seconds=7.34366`, and `peak_rss_mb=156.51`. Lesson learned: preserving the single-path shape and deferring generic-only setup lets the traversal/cache/prefetch bundle compose slightly better than iteration 15, but the extra structural work still lands just short of the controller gate; this hot loop appears close to saturated for incremental traversal-plus-memory micro-optimizations alone. Decision: reject for incumbent promotion because the candidate is correct and faster but misses the benchmark contract's 2.0% minimum relative improvement threshold, so incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains in place. Next hypothesis: stop investing in larger bundles of `jnext`/prefetch/alias/half-box cleanup inside the current single-path fast loop unless a patch also removes a materially larger same-molecule or bin-walk cost center; otherwise pivot to another neighbor rebuild component with more remaining headroom.
- [2026-04-12T17:59:31.211081701Z] Refine the single-path molecular half/bin/newton builder in `src/npair_bin.cpp` with explicit `jnext` traversal, x-only next-neighbor prefetch, cached half-box minimum-image checks, bin/list noalias aliases, and delayed generic-only setup after the fast-path return. [Correctness held and `weighted_median_neigh_seconds` improved to 0.30551, but the 1.92% gain versus incumbent missed the 2.0% promotion threshold, so `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains incumbent.] (d5b0238dc967, weighted_median_neigh_seconds=0.30551)
- [2026-04-12T18:01:37.081991Z] Refine the single-path molecular half/bin/newton builder in src/npair_bin.cpp with explicit jnext traversal, x-only next-neighbor prefetch, cached half-box minimum-image checks, bin/list _noalias aliases, and delayed generic-only setup after the fast-path return [Correctness held and weighted_median_neigh_seconds improved to 0.30551, but the 1.92% gain versus incumbent missed the 2.0% promotion threshold.] (d5b0238dc967, weighted_median_neigh_seconds=0.30551)
- [2026-04-12T18:17:35.005279698Z] iter_0017 review: rejected candidate `4a9dda87cf453ca3db2b8e1e167ffbd904af8af5` after authoritative benchmarking. Hypothesis was that combining the iteration-16 `NPairBin<1,1,0,0,0>` fast-path bundle in `src/npair_bin.cpp` with contiguous `Neighbor` `xhold` copying and `check_distance()` alias cleanup in `src/neighbor.cpp` would stack complementary memory-traffic reductions across neighbor rebuild setup and hot-list construction, pushing the incumbent past the 2.0% promotion gate without affecting correctness. Changed files: `src/neighbor.cpp`, `src/npair_bin.cpp` (118 insertions, 46 deletions overall). Benchmark outcome: correctness passed for both cases with zero failures; `weighted_median_neigh_seconds` improved from 0.31150 to 0.30695 (-1.46% vs incumbent, -15.11% vs baseline), with `test-16r-short` at `neigh_seconds=1.0613`, `test-64r-short` at `neigh_seconds=0.30358`, `weighted_median_loop_seconds=7.31546`, and `peak_rss_mb=156.36`. Lesson learned: the added `Neighbor` `xhold`/`check_distance()` cleanup did not compound enough with the iteration-16-style `NPairBin` traversal/prefetch/half-box refinements on the controller metric; despite a respectable overall loop time, the primary metric gain fell short of the 2.0% gate and even trailed iteration 16's 0.30551 best sub-threshold `neigh_seconds`, which suggests this cross-file memory-access bundle is not the right path to promotion. Decision: reject for incumbent promotion because the candidate is correct and faster but misses the benchmark contract's minimum relative improvement threshold, so incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` remains in place. Next hypothesis: if `Neighbor` is revisited, isolate `src/neighbor.cpp` rebuild-distance and `xhold` copy changes on top of the incumbent to measure their standalone value, or pivot to a different rebuild cost center with more remaining headroom instead of bundling more traversal/prefetch cleanup into the already saturated `NPairBin` fast path.
- [2026-04-12T18:17:35.005279698Z] Combine iteration-16-style `NPairBin<1,1,0,0,0>` `jnext`/prefetch/half-box fast-path refinements with contiguous `Neighbor` `xhold` copy and `check_distance()` alias cleanup. [Correctness held and `weighted_median_neigh_seconds` improved to 0.30695, but the 1.46% gain versus incumbent `8c29b124a2c68bbc7efea2fbd01f26b5ad40288a` missed the 2.0% promotion threshold, so the incumbent remains in place.] (4a9dda87cf45, weighted_median_neigh_seconds=0.30695)
- [2026-04-12T18:19:00.616509Z] Combine iteration-16 style `NPairBin<1,1,0,0,0>` jnext/prefetch/half-box fast-path refinements with contiguous `Neighbor` `xhold` copy and `check_distance()` alias cleanup [Correctness passed and weighted_median_neigh_seconds improved to 0.30695, but the 1.46% gain versus incumbent missed the benchmark contract’s 2.0% promotion threshold.] (4a9dda87cf45, weighted_median_neigh_seconds=0.30695)
- [2026-04-12T18:38:00.346929664Z] iter_0018 review: accepted candidate `08f5ab4a4e026b06a3d38c7e3ebd6ecca2a25635` after authoritative benchmarking. Hypothesis was that keeping the accepted iteration-11 molecular/no-exclusion `NPairBin<1,1,0,0,0>::build()` fast-path semantics but switching the self-bin and stencil walks to explicit `jnext` linked-list traversal, exposing bin/list arrays through local `_noalias` aliases, and delaying generic-only setup until after the fast-path return would cut traversal and aliasing overhead without the extra structural cost of the larger iteration-14/16/17 bundles. Changed files: `src/npair_bin.cpp` (43 insertions, 17 deletions). Benchmark outcome: correctness passed for both cases with zero failures; `weighted_median_neigh_seconds` improved from 0.31150 to 0.29094 (-6.60% vs incumbent, -19.54% vs baseline), with `test-16r-short` at `neigh_seconds=1.0108`, `test-64r-short` at `neigh_seconds=0.28529`, `weighted_median_loop_seconds=7.31879`, and `peak_rss_mb=157.09`. Lesson learned: the explicit linked-list traversal plus focused bin/list alias cleanup is a materially better direction than the heavier prefetch/half-box/caching bundles tried in iterations 14-17; simplifying the hot traversal shape and deferring generic-only work gave the compiler/runtime a larger win than stacking more micro-optimizations onto the old structure. Decision: accept for incumbent promotion because the candidate is correct and clears the benchmark contract's 2.0% minimum relative improvement threshold by a wide margin, so `08f5ab4a4e026b06a3d38c7e3ebd6ecca2a25635` becomes the new incumbent. Next hypothesis: treat the iteration-18 explicit-`jnext`/`_noalias` fast-path shape as the new base and probe adjacent, similarly local opportunities that compose with it, such as remaining same-molecule special handling or owned-versus-ghost bin-walk asymmetries, while avoiding a return to broad prefetch/half-box/cross-file bundles unless they show standalone value on top of this new incumbent.
- [2026-04-12T18:38:00.346929664Z] Use explicit `jnext` linked-list traversal with bin/list `_noalias` aliases and delayed generic-only setup in the incumbent `NPairBin<1,1,0,0,0>` fast path [Correctness held and `weighted_median_neigh_seconds` improved from 0.31150 to 0.29094 (-6.60% vs incumbent), comfortably clearing the 2.0% promotion gate so `08f5ab4a4e026b06a3d38c7e3ebd6ecca2a25635` becomes the new incumbent.] (08f5ab4a4e02, weighted_median_neigh_seconds=0.29094)
- [2026-04-12T20:26:57.210945Z] optimize iteration 20 [worker loop did not finish cleanly: incomplete_max_iterations] (11db74893452, weighted_median_neigh_seconds=nan)
- [2026-04-12T20:44:47Z] iter_0021 review: rejected candidate `e35c58b86e076cb2e38bc4c2c993876fe6dbcd4a` after authoritative benchmarking. Hypothesis was that widening `Neighbor::check_distance()` in `src/neighbor.cpp` to 8-atom unrolled blocks and reducing the hot-loop branch rate to one blockwise max-displacement comparison per block would preserve the accepted iteration-19 semantics while speeding the common no-rebuild path. Changed files: `src/neighbor.cpp` (30 insertions, 2 deletions). Benchmark outcome: correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.28995 (+1.78% vs incumbent, -19.81% vs baseline), with `test-16r-short` at `neigh_seconds=0.99624`, `test-64r-short` at `neigh_seconds=0.28995`, `weighted_median_loop_seconds=7.30794`, and `peak_rss_mb=156.54`. Lesson learned: after the accepted contiguous-scan cleanup in iteration 19, extra 8-way unrolling and per-block max reduction in `check_distance()` does not compose; the small 16-rank gain is outweighed by a 64-rank slowdown, which suggests the wider block form adds enough reduction/register/code-shape overhead to erase any saved branch cost in the scaling-sensitive case. Decision: reject for incumbent promotion because the candidate is correct but regresses the primary metric, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` remains in place. Next hypothesis: keep the accepted iteration-19 contiguous scan/bulk-copy structure intact and look for more localized `Neighbor` rebuild-accounting or memory-movement reductions that avoid wider `check_distance()` unrolling, or pivot to another self-contained `Neighbor` cost center before retrying branch-frequency-focused loop reshaping.
- [2026-04-12T20:44:47Z] Widen `Neighbor::check_distance()` to 8-atom unrolled blocks and compare one blockwise max displacement squared against `deltasq`, keeping the incumbent semantics while reducing hot-loop branch frequency [Correctness held, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.28995 (+1.78% vs incumbent), so `11db748934527306a5e9d2d8c0c51eb47b03158a` remains incumbent.] (e35c58b86e07, weighted_median_neigh_seconds=0.28995)
- [2026-04-12T20:45:47.305528Z] Widen Neighbor::check_distance() to 8-atom unrolled blocks and compare one blockwise max displacement squared against deltasq, keeping the incumbent semantics while reducing hot-loop branch frequency [Correctness held, but weighted_median_neigh_seconds regressed from 0.28487 to 0.28995 (+1.78% vs incumbent), so 11db74893452 remains incumbent.] (e35c58b86e07, weighted_median_neigh_seconds=0.28995)
- [2026-04-12T21:02:07Z] iter_0022 review: rejected candidate `708f9b2d16b193cdbbcb8f2f86de97e0879a1f79` after authoritative benchmarking. Hypothesis was that because this TIP4P water benchmark runs with all-zero special-bond weights, specializing the incumbent `NPairBin<1,1,0,0,0>::build()` fast path so same-molecule pairs use a cheaper boolean special-neighbor membership test would remove residual encoded special-handling overhead while preserving the accepted different-molecule fast accept and periodic-image ghost handling. Changed files: `src/npair_bin.cpp` (28 insertions). Benchmark outcome: correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.28503 (+0.06% vs incumbent, -21.17% vs baseline), with `test-16r-short` at `neigh_seconds=1.0042`, `test-64r-short` at `neigh_seconds=0.28503`, `weighted_median_loop_seconds=7.33955`, and `peak_rss_mb=155.36`. Lesson learned: on top of the accepted iteration-18/19 incumbent, same-molecule special-neighbor handling is no longer a meaningful bottleneck for this benchmark; even a narrowly gated all-zero specialization failed to recover its own added control/setup cost and slightly underperformed the incumbent on both held-out cases. Decision: reject for incumbent promotion because the candidate is correct but regresses the primary metric and does not satisfy the controller's 2.0% minimum-improvement gate, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` remains in place. Next hypothesis: stop spending iterations on finer same-molecule special-bond micro-specializations in `NPairBin<1,1,0,0,0>::build()` and instead probe another cost center that composes with the accepted fast path, such as traversal/load reduction that does not add new special-case branches or a localized `Neighbor` rebuild-accounting/memory-movement optimization.
- [2026-04-12T21:02:07Z] Specialize the incumbent `NPairBin<1,1,0,0,0>::build()` fast path for all-zero special-bond weights so same-molecule pairs use a cheaper boolean special-neighbor membership test while preserving the existing different-molecule accept path and periodic-image ghost handling. [Correctness held, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.28503 (+0.06% vs incumbent), so `11db748934527306a5e9d2d8c0c51eb47b03158a` remains incumbent.] (708f9b2d16b1, weighted_median_neigh_seconds=0.28503)
- [2026-04-12T21:03:33.513135Z] Specialize the incumbent `NPairBin<1,1,0,0,0>::build()` fast path for all-zero special-bond weights so same-molecule pairs use a cheaper boolean special-neighbor membership test while preserving the existing different-molecule accept path and periodic-image ghost handling. [Correctness held, but the all-zero special-neighbor fast path slightly regressed `weighted_median_neigh_seconds` from 0.28487 to 0.28503, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` stays.] (708f9b2d16b1, weighted_median_neigh_seconds=0.28503)
- [2026-04-12T21:32:35Z] iter_0023 review: rejected candidate `a3f1052997d87db23fe9f9a0009cafa02c4ae1c9` after authoritative benchmarking. Hypothesis was that adding a runtime uniform-cutoff fast path to the specialized `NPairBin<1,1,0,0,0>` molecular/no-exclusion builder would let equal-cutoff systems skip per-pair `type[j]` and cutoff-matrix loads, and that pairing it with local `Neighbor` cadence cleanups would reduce rebuild overhead without disturbing correctness. Changed files: `src/neighbor.cpp`, `src/neighbor.h`, and `src/npair_bin.cpp` (211 insertions, 65 deletions). Benchmark outcome: correctness passed for both cases with zero failures, and the candidate slightly improved `weighted_median_neigh_seconds` from 0.28487 to 0.28469 (-0.06% vs incumbent, -21.27% vs baseline), with `test-16r-short` at `neigh_seconds=0.98174`, `test-64r-short` at `neigh_seconds=0.27682`, `weighted_median_loop_seconds=7.30585`, and `peak_rss_mb=156.46`. Lesson learned: on top of the accepted iteration-18/19 incumbent, equal-cutoff specialization and extra every-step `Neighbor` fast-check plumbing produce only noise-level benefit; the remaining hot cost is no longer meaningfully dominated by per-pair cutoff loads, and bundling the `Neighbor` and `npair_bin` changes together makes it harder to attribute the tiny gain to either component. Decision: reject for incumbent promotion because the candidate is correct but misses the benchmark contract's 2.0% minimum-improvement gate by a wide margin, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` remains in place. Next hypothesis: avoid further runtime-guarded equal-cutoff or same-molecule micro-specializations in `NPairBin<1,1,0,0,0>::build()` unless isolated evidence shows clear headroom, and either test the `Neighbor` cadence fast-check idea on its own or pivot to another self-contained traversal/data-movement reduction that removes unconditional work from the incumbent path.
- [2026-04-12T21:32:35Z] Add a runtime uniform-cutoff fast path to the specialized `NPairBin<1,1,0,0,0>` molecular/no-exclusion builder so equal-cutoff systems skip per-pair `type[j]` and cutoff-matrix loads, while retaining the local Neighbor cadence cleanups that compiled and smoked correctly. [Correctness held and `weighted_median_neigh_seconds` improved slightly from 0.28487 to 0.28469, but the 0.06% gain versus incumbent is far below the 2.0% promotion threshold, so `11db748934527306a5e9d2d8c0c51eb47b03158a` remains incumbent.] (a3f1052997d8, weighted_median_neigh_seconds=0.28469)
- [2026-04-12T21:34:19.641634Z] Add a runtime uniform-cutoff fast path to the specialized `NPairBin<1,1,0,0,0>` molecular/no-exclusion builder so equal-cutoff systems skip per-pair `type[j]` and cutoff-matrix loads, while retaining the local Neighbor cadence cleanups that compiled and smoked correctly. [Correctness held, but the candidate’s 0.06% improvement to 0.28469 `weighted_median_neigh_seconds` is far below the 2.0% promotion threshold, so incumbent `11db74893452` stays.] (a3f1052997d8, weighted_median_neigh_seconds=0.28469)
- [2026-04-12T21:54:07Z] iter_0024 review: rejected candidate `84babbbb36e3b112c5751b47e12a078b73110c15` after authoritative benchmarking. Hypothesis was that adding an axis-aligned early reject to the specialized `NPairBin<1,1,0,0,0>` fast path would let pairs that already exceed `neighbor->cuttype[itype]` on one coordinate skip the `type[j]` load and full squared-distance check, reducing per-pair work in the incumbent hot loop. Changed files: `src/npair_bin.cpp`. Benchmark outcome: correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.49372 (+73.31% vs incumbent, -36.55% vs baseline), with `test-16r-short` at `neigh_seconds=1.7855`, `test-64r-short` at `neigh_seconds=0.49217`, `weighted_median_loop_seconds=7.50006`, and `peak_rss_mb=156.69`. Lesson learned: the incumbent fast path is extremely sensitive to additional coordinate-wise reject logic; three extra absolute-value/compare branches per candidate pair overwhelm any savings from avoiding `type[j]` loads and occasional squared-distance checks, so this class of micro-optimization is not viable on this benchmark. Decision: reject for incumbent promotion because the candidate is correct but catastrophically slower on the primary metric and fails the benchmark contract's 2.0% minimum-improvement gate by a large margin, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` remains in place. Next hypothesis: keep the accepted iteration-18/19 structure intact and focus future experiments on simplifying traversal/data movement or amortizing invariant work without adding new per-pair coordinate tests or extra hot-loop branches.
- [2026-04-12T21:54:07Z] Add an axis-aligned early reject in the specialized `NPairBin<1,1,0,0,0>` fast path so pairs exceeding `neighbor->cuttype[itype]` on any single coordinate skip the `type[j]` load and full squared-distance check. [Correctness held, but `weighted_median_neigh_seconds` regressed sharply from 0.28487 to 0.49372 (+73.31% vs incumbent), so incumbent `11db74893452` stays.] (84babbbb36e3, weighted_median_neigh_seconds=0.49372)
- [2026-04-12T21:56:46.634828Z] Add an axis-aligned early reject in the specialized `NPairBin<1,1,0,0,0>` fast path so pairs exceeding `neighbor->cuttype[itype]` on any single coordinate skip the `type[j]` load and full squared-distance check. [Correctness held, but the axis-aligned early-reject path regressed weighted_median_neigh_seconds from 0.28487 to 0.49372 (+73.31% vs incumbent), so the incumbent stays.] (84babbbb36e3, weighted_median_neigh_seconds=0.49372)
- [2026-04-12T22:20:24Z] iter_0025 review: rejected candidate `b78acabc90a5fdca630260494c4f619ada7f551f` after authoritative benchmarking. Hypothesis was that isolating the iteration-23 `Neighbor` cadence fast-check in `src/neighbor.cpp`/`src/neighbor.h` by caching the benchmark-common `every=1`, `delay=0`, `dist_check=yes`, no-box path inside `Neighbor::decide()` and `Neighbor::check_distance()` would preserve the accepted iteration-19 contiguous movement-tracking win while shaving residual rebuild-cadence bookkeeping overhead without touching the accepted iteration-18 `npair_bin` fast path. Changed files: `src/neighbor.cpp` and `src/neighbor.h` (98 insertions, 62 deletions). Benchmark outcome: correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.28592 (+0.37% vs incumbent, -20.92% vs baseline), with `test-16r-short` at `neigh_seconds=1.0079`, `test-64r-short` at `neigh_seconds=0.29117`, `weighted_median_loop_seconds=7.30287`, and `peak_rss_mb=156.47`. Lesson learned: the isolated cadence-cache path in `Neighbor::decide()`/`check_distance()` does not pay for itself on top of the accepted iteration-19 straight-line scan/bulk-copy implementation; any saved condition evaluation is outweighed by the added state management/branching, and the regression shows up in both held-out ranks rather than as a meaningful signal. Decision: reject for incumbent promotion because the candidate is correct but slower than incumbent and does not satisfy the benchmark contract's 2.0% minimum-improvement gate, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` remains in place. Next hypothesis: stop pursuing `Neighbor` cadence fast-check caching in this form and instead test `Neighbor` changes that remove unconditional memory traffic or rebuild-accounting work from the accepted iteration-19 path, or return to `npair_bin` only for simplifications that reduce existing traversal work without adding new per-pair branches.
- [2026-04-12T22:20:24Z] Isolate the iteration-23 `Neighbor` cadence fast-check by caching the benchmark-common every=1/delay=0/dist_check=yes no-box path in `Neighbor::decide()`/`check_distance()` without any accompanying `npair_bin` specialization. [Correctness held, but the isolated `Neighbor` cadence-cache path regressed `weighted_median_neigh_seconds` from 0.28487 to 0.28592 (+0.37% vs incumbent), so incumbent `11db74893452` stays.] (b78acabc90a5fdca630260494c4f619ada7f551f, weighted_median_neigh_seconds=0.28592)
- [2026-04-12T22:21:56.843926Z] Isolate the iteration-23 Neighbor cadence fast-check by caching the benchmark-common every=1/delay=0/dist_check=yes no-box path in Neighbor::decide()/check_distance() without any accompanying npair_bin specialization [Correctness held, but the isolated Neighbor cadence-cache path regressed weighted_median_neigh_seconds versus incumbent and missed the 2.0% promotion gate.] (b78acabc90a5, weighted_median_neigh_seconds=0.28592)
- [2026-04-12T22:45:45Z] iter_0026 review: rejected candidate `ba18979d7807a6952080db256151d45cd80fe595` after authoritative benchmarking. Hypothesis was that adding a benchmark-common `NPairBin<1,1,0,0,0>` fast path in `src/npair_bin.cpp` for the uniform-cutoff plus all-special-encoded case, using encoded-special lookup and pointer-style neighbor writes, would remove residual generic cutoff and special-flag work from the hot loop and extend the accepted iteration-18/19 incumbent. Changed files: `src/npair_bin.cpp` (133 insertions). Benchmark outcome: correctness passed for both cases with zero failures, and `weighted_median_neigh_seconds` improved only from 0.28487 to 0.28444 (-0.15% vs incumbent, -21.33% vs baseline), with `test-16r-short` at `neigh_seconds=0.97938`, `test-64r-short` at `neigh_seconds=0.28196`, `weighted_median_loop_seconds=7.28182`, and `peak_rss_mb=157.11`. Lesson learned: even when uniform-cutoff and encoded-special assumptions are fused into a dedicated fast path, the remaining generic cutoff/special-flag work is too small to move the held-out metric materially; pointer-style neighbor writes may recover a little overhead, but this kind of `npair_bin` micro-specialization is now producing only noise-level gains on top of the incumbent. Decision: reject for incumbent promotion because the candidate is correct but misses the benchmark contract's 2.0% minimum-improvement gate by a wide margin, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` remains in place. Next hypothesis: stop investing in narrower `NPairBin<1,1,0,0,0>` uniform-cutoff/all-special-encoded branches and instead target changes that remove unconditional traversal or memory-movement cost across the incumbent path as a whole, either in `Neighbor` rebuild accounting/data motion or in `npair_bin` traversal simplifications that do not add more benchmark-specific runtime guards.
- [2026-04-12T22:46:57.607368Z] Add a benchmark-common `NPairBin<1,1,0,0,0>` fast path for the uniform-cutoff plus all-special-encoded case, using encoded-special lookup and pointer-style neighbor writes to remove generic cutoff and special-flag work from the hot loop. [Correctness passed, but the candidate’s weighted_median_neigh_seconds improved only 0.15% versus incumbent, far below the 2.0% promotion threshold.] (ba18979d7807, weighted_median_neigh_seconds=0.28444)
- [2026-04-12T23:27:18Z] iter_0027 review: rejected candidate `321fb756a8361c8c74c0f7854e10bbb62179ffae` after authoritative benchmarking. Hypothesis was that caching each visited neighbor's coordinates, tag, and molecule once and inlining the orthogonal half-box minimum-image test inside the specialized `NPairBin<1,1,0,0,0>` molecular/no-exclusion fast path would compound earlier data-access wins by cutting repeated loads and the `domain->minimum_image_check()` call without changing list semantics. Changed files: `src/npair_bin.cpp` (45 insertions, 23 deletions). Benchmark outcome: correctness passed for both cases with zero failures, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.28559 (+0.25% vs incumbent, -21.02% vs baseline), with `test-16r-short` at `neigh_seconds=1.0073`, `test-64r-short` at `neigh_seconds=0.28461`, `weighted_median_loop_seconds=7.30732`, and `peak_rss_mb=156.02`. Lesson learned: on top of the accepted iteration-18/19 incumbent, reintroducing j-side scalar caching and hand-inlined orthogonal minimum-image logic does not create a useful win; the extra scalar plumbing/control flow outweighs any saved loads or helper-call overhead, and the worker's small reduced-smoke improvement did not survive the held-out benchmark. Decision: reject for incumbent promotion because the candidate is correct but slower than incumbent and does not satisfy the benchmark contract's 2.0% minimum-improvement gate, so incumbent `11db748934527306a5e9d2d8c0c51eb47b03158a` remains in place. Next hypothesis: avoid further `NPairBin<1,1,0,0,0>` micro-tuning that just reorders cached `j`-side data or inlines existing orthogonal checks, and instead probe broader unconditional work removal in the incumbent traversal or isolated `Neighbor` data-movement/accounting reductions that do not add more per-pair bookkeeping.
- [2026-04-12T23:27:18Z] Cache j-side neighbor coords/tag/molecule once and inline orthogonal half-box minimum-image checks in the specialized `NPairBin<1,1,0,0,0>` molecular/no-exclusion fast path. [Correctness held, but `weighted_median_neigh_seconds` regressed from 0.28487 to 0.28559 (+0.25% vs incumbent), so incumbent `11db74893452` stays.] (321fb756a836, weighted_median_neigh_seconds=0.28559)
- [2026-04-12T23:30:40.819488Z] Cache j-side neighbor coords/tag/molecule once and inline orthogonal half-box minimum-image checks in the specialized NPairBin&lt;1,1,0,0,0&gt; molecular/no-exclusion fast path [Correctness held, but weighted_median_neigh_seconds regressed 0.25% versus incumbent and missed the 2.0% promotion gate.] (321fb756a836, weighted_median_neigh_seconds=0.28559)
### Open hypotheses
- Treat the accepted iteration-19 contiguous `Neighbor` movement-tracking cleanup together with the accepted iteration-18 explicit-`jnext`/`_noalias` `NPairBin` fast path as the incumbent shape, and avoid revisiting `Neighbor::check_distance()` cadence-side micro-optimizations such as wider unroll/reduction or cached every=1/delay=0/dist_check=yes fast-check plumbing unless new evidence shows a clear benefit.
- The rejected iterations 22-27 suggest same-molecule special-handling micro-tuning, runtime equal-cutoff gating, axis-aligned early-reject logic, the uniform-cutoff/all-special-encoded fast path, and renewed j-side coord/tag/molecule caching plus inline orthogonal minimum-image checks inside `NPairBin<1,1,0,0,0>::build()` are exhausted or counterproductive on this benchmark; future `npair_bin` work should favor broader traversal/load simplifications that remove unconditional work rather than add more benchmark-specific runtime-conditioned branches or scalar bookkeeping.
- Future candidates should start from `11db748934527306a5e9d2d8c0c51eb47b03158a` and probe self-contained `Neighbor` rebuild-accounting or memory-movement reductions that preserve the accepted iteration-19 straight-line scan/bulk-copy structure without adding new cadence-cache state, or test `npair_bin` changes that simplify the existing traversal without introducing extra per-pair coordinate comparisons; treat sub-1% local smoke wins in this micro-tuning space as non-predictive unless they remove a clearly larger chunk of unconditional work.
