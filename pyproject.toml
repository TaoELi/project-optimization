[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "project-optimization-docs"
version = "0.0.0"

[project.optional-dependencies]
docs = [
  "furo",
  "myst-parser",
  "sphinx",
]

[tool.setuptools]
py-modules = []
